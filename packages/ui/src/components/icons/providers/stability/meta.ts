import { type IconMeta } from '../../types'
import lightWebp from './light.webp'

export const meta: IconMeta = {
  id: 'stability',
  colorPrimary: '#E80000',
  colorScheme: 'color',
  webp: {
    light: lightWebp,
    size: 256
  }
}
