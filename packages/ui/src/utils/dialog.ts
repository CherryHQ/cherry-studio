/** Close-animation duration of DialogContent's closed-state animation class. */
export const DIALOG_CLOSE_DURATION_MS = 220

/** Delay before imperative hosts unmount a closing Dialog, including a final-frame paint buffer. */
export const DIALOG_UNMOUNT_DELAY_MS = 250
