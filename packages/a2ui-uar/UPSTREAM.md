# UAR renderer provenance

This package is copied from `frontend/packages/a2ui-uar` in
`Prometheus-AGS/universal-agent-runtime` at commit `8f82c90c`.

It is the approved renderer for UAR's `uar.a2ui/1` profile. Update it by copying
the package source from a reviewed UAR commit together with `a2ui-core`, then
record the new source commit here. The Boss owns only the administration preview
integration; renderer behavior remains upstream-owned by UAR.
