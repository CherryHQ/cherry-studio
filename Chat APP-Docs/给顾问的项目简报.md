# 给顾问的项目简报

## 1. 我是谁 / 项目是什么

我正在 Cherry Studio v2 fork 上做一个 AI 对话产品原型，核心功能是 **Text Anchor Branch / 文本锚点分支**：

> 用户选中 AI 回复中的一段文字 → 右键打开分支 → 在当前聊天界面右侧并列展开一个 branch conversation → AI 针对选中的那段内容继续回答。

产品目标不是普通“复制消息开新对话”，而是更接近：
- 对 AI 回复里的某句话、某个概念、某个段落继续追问；
- 主对话不离开；
- 右侧并列展开局部追问；
- 源文本有高亮，让用户知道 branch 是从哪里来的。

团队目前 2 人：
1. 我：项目负责人 / 架构判断 / 主力决策；
2. 一个低经验但听话的助手：负责低风险 UI 壳子、文案、样式、局部组件等任务。

---

## 2. 你（Claude）的角色

你是第三方 **产品 + 工程顾问**。

你不直接写功能代码，不直接执行 git 操作。你的任务是：

1. 帮我审查当前方案是否合理；
2. 帮我判断下一步该做什么；
3. 帮我发现架构风险、数据流风险、UI/UX 风险；
4. 帮我把需求拆成适合 Claude Code 执行的小任务；
5. 帮我写给 **Claude Code** 的英文 prompt。

说明：
- Claude Code 是我的编码 agent，跑在另一个终端；
- 给 Claude Code 的 prompt 请用英文写；
- 但 prompt 里要明确要求它 **用中文回复我**；
- prompt 要求它做完后停下，等我确认；
- 不允许 Claude Code 自己 git commit；
- git commit / push 由我通过 GitHub Desktop 手动操作。

---

## 3. 工作纪律 / 技术边界

项目基于 Cherry Studio v2 fork。

开发原则：

- v2 优先走 DataApi / Cache / v2 数据结构；
- UI 优先使用 `@cherrystudio/ui`、Tailwind、Shadcn 风格；
- 不要主动扩展 v1 Redux / Dexie 体系；
- Redux / Dexie 相关路径目前很多是 throwaway / 兼容层，除非为了桥接现有功能，不要把新功能深绑到 v1；
- 每次改动要小步推进，先方案、再实施、再验证；
- 不要一次性塞大改；
- 对高风险架构变更，先让我确认方案；
- Conventional Commits；
- commit 必须 `--signoff`；
- 不提交：
  - `Chat APP-Docs/.obsidian/workspace.json`
  - `Chat APP-Docs/.obsidian/graph.json`
- 不让 Claude Code 自动 commit。

---

## 4. 当前已完成状态

目前已经完成并提交：

1. **文本选区捕获**
   - 用户可以在 assistant 回复中选中一段文字；
   - 右键菜单可以识别 messageId / blockId / selectedText；
   - 只对 assistant 文本启用 branch 相关菜单。

2. **Side-by-side Branch Panel**
   - branch 不再跳转到一个新 topic 页面；
   - 当前主对话保持在左侧；
   - 右侧并列打开 branch panel；
   - 用户可以在右侧看到 selected passage 和 branch 回复。

3. **隐藏 prompt 模板**
   - 用户不会看到系统拼接的隐藏模板；
   - branch AI 回复会围绕 selectedText 和 follow-up 聚焦回答。

4. **Regenerate / edit / delete 保持 branch focus**
   - branch 对话里的 regenerate 不会丢失 selected passage 语义；
   - 已通过 BranchAssistantContext 等方式保持局部上下文。

5. **可拖拽分隔线**
   - 右侧 Branch Panel 宽度可以拖拽；
   - 拖动时主聊天列会 reflow；
   - 宽度在 session 内保留，刷新后重置。

6. **源选区高亮**
   - CSS Custom Highlight API 在当前 Electron/Chromium 环境中注册成功但不可见，已经弃用；
   - 当前改为 `span.branch-anchor-highlight` wrap 方案；
   - 高亮可见；
   - A → B 切换不会累积；
   - 关闭 branch 后 `document.querySelectorAll('span.branch-anchor-highlight').length === 0`；
   - 空 span 检查为 0；
   - span wrap 清理由 `replaceWith(...children)` 实现。

---

## 5. 当前窗口要处理的任务

当前窗口要处理：

> 设计下一阶段的 **多 branch 并存 + branch 对话折叠** 功能方案。

重点不是马上写代码，而是先做产品和工程设计。

我希望你重点帮我判断：

1. 多 branch 是否应该同时显示多个右侧 panel，还是一个 panel 内多个 branch card；
2. branch 折叠应该折叠成什么形态：
   - 右侧 panel 内折叠；
   - 主文中高亮旁边显示小标记；
   - 底部/侧边显示 branch list；
3. branch 与源文本高亮之间如何建立可见关系；
4. 多 branch 的状态应该放在哪里：
   - React local state；
   - cache；
   - SQLite；
   - 还是阶段性保持 ephemeral；
5. 是否应该先做 v1 MVP：
   - 单 panel；
   - 多 branch card；
   - 当前展开一个，其他折叠；
   - 不做持久化；
6. 哪些任务可以交给低经验助手做 UI 壳子；
7. 哪些任务必须由主力 / Claude Code 做数据流与架构。

---

## 6. 相关已知坑 / 不要重复踩

1. **CSS Custom Highlight API 不要再建议**
   - 在当前环境中 `CSS.highlights.set()` 成功，Range 也正确，但页面不可见；
   - 已经切换到 span wrap；
   - 不要再回到 CSS Custom Highlight API。

2. **关闭 branch 会留下 SQLite orphan topic**
   - 当前 branch fork 是真实 `POST /topics`；
   - 关闭右侧 branch panel 只清 UI state，不会 DELETE topic；
   - 这是已知 deferred issue；
   - 后续归入 cleanup task，例如 T-006D-2C-5；
   - 不要误以为“关闭即删除数据库 topic”。

3. **不要把 branch panel 当 active topic 跳走**
   - 早期 D-2A 方案是创建 topic 后 `setActiveTopic(newTopic)`；
   - 这不符合产品目标；
   - 现在正确方向是 side-by-side，主对话不离开。

4. **BranchMessageStream 和主 Messages 的渲染来源要小心**
   - 之前担心 v1 Redux 写、v2 DataApi 读不一致；
   - 当前已能在右侧显示 branch 流式回复；
   - 后续多 branch 时仍要小心不要拆出完全不同的数据源。

5. **高亮清理必须防止 DOM 污染**
   - 之前出现过空 `<span class="branch-anchor-highlight"></span>` 残留；
   - 后来 `clearSourceHighlight()` 改为 `span.replaceWith(...children)` 后通过验证；
   - 后续做多 branch / 折叠时不能破坏这个清理不变量。

---

## 7. 我希望你接下来怎么帮我

请你先不要写代码 prompt。

请先做三件事：

1. 用产品视角给我分析：
   - 多 branch 并存到底应该怎么设计；
   - 折叠功能应该长什么样；
   - 第一版 MVP 应该做到哪里就停。

2. 用工程视角给我分析：
   - 当前 single branch state 如何演进到 multi branch state；
   - 哪些状态应该本地保存；
   - 哪些状态暂时不该持久化；
   - 是否需要 branch registry / branch list / activeBranchId。

3. 最后给我一个推荐实施路线：
   - Phase 1：最小可用多 branch；
   - Phase 2：折叠 / 展开；
   - Phase 3：持久化 / cleanup / orphan topic；
   - 哪些给 Claude Code；
   - 哪些给低经验助手。

请用中文回复我，尽量白话，但要有工程判断。