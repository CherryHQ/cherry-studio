# T-004 验证

## 自动化验证（已通过）

| 项 | 命令 | 状态 |
|---|---|---|
| 格式 | `pnpm biome format --write src/renderer/src/services/AssistantService.ts` | ✅ |
| 类型 | `pnpm typecheck:web` | ✅ |
| oxlint | `pnpm oxlint src/.../AssistantService.ts src/.../Inputbar.tsx` | ✅ 0/0 |
| eslint | `npx eslint --no-warn-ignored src/.../AssistantService.ts` | ✅ |
| 邻近 vitest | `pnpm vitest run --project renderer src/.../services/__tests__/{ConversationService,ModelMessageService}.test.ts src/.../Messages/__tests__` | ✅ 15/15 |

## 手动验证（需用户执行）

### 端到端复现 baseline 修复

```bash
# 1. 清掉本地数据库（彻底 fresh install 状态）
rm -rf ~/Library/Application\ Support/CherryStudioDev

# 2. 启动 dev
cd "/Users/sam/Library/CloudStorage/GoogleDrive-finneganwatts4@gmail.com/My Drive/APP_Dev/ChatApp/cherry-studio"
pnpm dev
```

### 期望现象

- ✅ 启动正常
- ✅ 主界面可见
- ✅ 点击"新建话题"按钮 / 触发 `addNewTopic`：
  - 控制台日志：`POST /topics body: { name: 'Default Topic', assistantId: null }`（关键差异：**`null` 而非 `'default'`**）
  - **不再**出现 `SQLITE_CONSTRAINT_FOREIGNKEY: FOREIGN KEY constraint failed`
  - **不再**出现 `Topic with id ... not found`
  - topic 进入侧栏，可点击切换
- ⚠️ 仍可能出现：`Assistant preset with id default not found in state.` warning —— 这是 `useAssistantPresets` 找 preset 的同源 bug，但**不阻塞** topic 创建，留给后续任务

### 不期望出现

- ❌ FK 错误
- ❌ topic 无法在数据库 INSERT 成功

### 退化测试（确认未引入回归）

- 已有 topic（如果 fresh install 之外的场景，例如改完后切到非 fresh install profile）：UI 是否仍能正常显示？因为 v2 SQLite 里若已有助手行，`topic.assistantId !== 'default'` → 此分支不进入 null 转换 → 行为与改前一致 → ✅ 无回归

### 后续观察

修完 baseline 后立即可能发现的「下一层」问题（不属于 T-004 范围，记录到 [问题与Debug记录.md](../../问题与Debug记录.md) 新 D-NNN）：
1. UI 进入 topic 后 `useAssistant(null)` 的回退路径是否健壮（应回到 runtime `defaultAssistant`）
2. 发送消息 → `POST /topics/:topicId/messages` 的 `parentId` 三态行为
3. AI 调用 `fetchChatCompletion` 是否能取到 model / provider（不应依赖 `topic.assistantId`）

## 验证签收

- 自动化 ✅
- 手动 ⏳ 待用户执行 `rm + pnpm dev` 验证
