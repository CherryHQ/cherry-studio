# T-004 实施记录

## 准备阶段（修改前验证）

### 1. 确认调用面

```bash
grep -rn "mapLegacyTopicToDto" src/renderer/src --include="*.ts" --include="*.tsx"
```

结果（仅 2 处）：
- `src/renderer/src/services/AssistantService.ts:173` — 定义
- `src/renderer/src/pages/home/Inputbar/Inputbar.tsx:329` — 唯一调用方

✅ 改动不扩散。

### 2. 确认 schema 允许 null

`packages/shared/data/types/topic.ts:26`：
```ts
assistantId: z.string().nullable().optional(),
```

`CreateTopicDto` 由 `TopicSchema.pick({ ..., assistantId: true }).partial()` 派生 —— `assistantId` 类型为 `string | null | undefined`。

✅ 返回 `null` 类型兼容。

### 3. 确认 renderer `Topic.assistantId` 类型

`src/renderer/src/types/index.ts:271-274` —— v1 renderer `Topic` 的 `assistantId: string`（非空）。

入参 `string`，返回 `string | null`，TS 推断 OK。

### 4. 确认无既有单元测试

`grep mapLegacyTopicToDto src/renderer/src/services/__tests__/*` —— 无命中。
`find src/renderer/src/services/__tests__ -name "AssistantService*"` —— 无文件。

→ 不需要更新既有 test。是否补新 test？决定不补 —— 该函数有 `// TODO: remove it in v2`，v2 完整切换时整体删除。

## 实施

### 改动

`src/renderer/src/services/AssistantService.ts:172-181`：

```diff
 // TODO: remove it in v2
 export function mapLegacyTopicToDto(topic: Topic): CreateTopicDto {
+  // v1 'default' is a runtime sentinel synthesized by getDefaultAssistant(),
+  // not a row in the v2 assistant table — sending it as a FK would violate
+  // SQLITE_CONSTRAINT_FOREIGNKEY. Map to null (Topic.assistantId is nullable
+  // in v2 schema; matches ChatMigrator's orphan-topic fallback).
   return {
     name: topic.name,
-    assistantId: topic.assistantId
+    assistantId: topic.assistantId === 'default' ? null : topic.assistantId
   }
 }
```

实质改动 = 1 行（三元判断）；附 4 行注释解释 why（CLAUDE.md：注释只在 why 非显然时写，这里跨 v1/v2 边界 + FK + 迁移器一致性，确实非显然）。

### 风险评估

| 副作用 | 影响 | 缓解 |
|---|---|---|
| 创建出的 topic 在 SQLite 中 `assistantId = NULL` | UI 进入时 `useAssistant(null)` 返回 undefined，应 fallback 到 runtime `defaultAssistant` | 与 ChatMigrator 的 orphan-topic 处理一致；fresh install 阶段本无真实助手可选，问题暂不显著 |
| chat completion 路径是否读 `topic.assistantId` | 排查表明取自 Redux `defaultAssistant` 或 mention，不依赖 topic 字段 | 用户实测验证（见 [验证.md](./验证.md)） |
| 多窗口下 topic 创建并发 | 不变（此函数不引入新竞态） | — |

## 校验

按 CLAUDE.md「Lint, test, and format before completion」执行：

| 步骤 | 命令 | 结果 |
|---|---|---|
| Format 变更文件 | `pnpm biome format --write src/renderer/src/services/AssistantService.ts` | ✅ Formatted 1 file in 7ms. No fixes applied |
| Typecheck renderer | `pnpm typecheck:web` | ✅ 退出码 0，无输出 |
| oxlint 变更文件 + 调用方 | `pnpm oxlint src/.../AssistantService.ts src/.../Inputbar.tsx` | ✅ Found 0 warnings and 0 errors |
| ESLint 变更文件 | `npx eslint --no-warn-ignored src/.../AssistantService.ts` | ✅ 静默通过 |
| Vitest（邻近测试） | `pnpm vitest run --project renderer src/renderer/src/services/__tests__/ConversationService.test.ts src/.../ModelMessageService.test.ts src/.../Messages/__tests__` | ✅ 3 files, 15 tests passed |

> **未执行 `pnpm build:check` 全套**：理由 — 改动 1 行，无 i18n 变化、无新增 export、无影响 main 进程；renderer 邻近 test + typecheck:web + oxlint + eslint 已覆盖直接影响面。如需稳妥可在 PR 前补一次 `pnpm lint` 全跑。

## 最终 diff

```
 src/renderer/src/services/AssistantService.ts | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)
```

## 未 commit

按 CLAUDE.md「Only create commits when requested by the user」+ 用户未明示提交 —— 暂存但不 commit。
