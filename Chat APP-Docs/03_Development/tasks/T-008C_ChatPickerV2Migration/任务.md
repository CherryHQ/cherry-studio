# T-008C 任务 brief

## 输入

- D-003B fresh install 验证：Provider 设置页 ✅ / Chat 弹窗 ❌
- T-008 诊断：picker 读 v1 Redux，v2 sync 不写 v1
- T-008B 评估：方案 B 改动可控（1 文件 ~50 行）
- 用户决定：**走方案 B，CHERRYAI fallback 选 option A（picker 内保留硬编码注入）**

## 约束

- 只动 chat-model-popup.tsx + 它的测试
- 不动下游调用方（Chat.tsx / MessageMenubar / SelectModelButton / AssistantModelSettings / PromptSection / ModelSelectButton）
- 不动 base-popup.tsx 视觉层
- 不动 messageThunk / StreamingService / AssistantService（T-005B FK 守卫保留）
- 不动 v1 Redux schema、initialState
- 跑必要校验：format / lint / typecheck / focused vitest + 邻近回归 / i18n

## 交付物

- 代码：`chat-model-popup.tsx` 数据源换 v2 + shim + fallback；`chat-model-popup.test.tsx`（新建）
- 文档：本 task 目录 + 更新 D-003 / T-008 状态 + 4 份滚动文档

## 完成标准

- [x] chat-model-popup.tsx 用 v2 hooks（useProviders plural + useModels）
- [x] 经 toV1ProviderShim/toV1ModelShim 转 v1 shape 喂 base-popup
- [x] 保留 CHERRYAI_PROVIDER fallback（v2 cherryai 空时注入）
- [x] 单元测试 ≥ 4 用例覆盖 Ollama 出现 / CHERRYAI 在/不在 / hidden / filter
- [x] format + oxlint + eslint + typecheck:web + vitest 全过
- [x] i18n 通过
- [x] 用户 fresh install 实测通过（2026-05-21）—— Ollama Provider 同步 ✅ / Chat picker 能选 Ollama ✅ / gemma4:e4b 正常流式回复 ✅ / T-004 / T-005B baseline FK 未复现
