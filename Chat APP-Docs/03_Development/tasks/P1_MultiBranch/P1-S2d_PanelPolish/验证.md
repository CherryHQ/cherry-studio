# P1-S2d 验证

## 自动化（focused 134/134 绿）
- typecheck:web → exit 0、0 error。
- i18n:check → 通过。
- biome format → 仅对 1 个测试文件做换行格式化。
- oxlint → 0 warning / 0 error。
- eslint → 0 error；仅 1 条**既有** `--color-text-3` warning（在未触碰的 topic.rename PromptPopup，非本次引入）。
- 完整 `pnpm test` 未跑（已知 vitest 主进程 shutdown SIGSEGV 环境坑；renderer focused 全绿）。

## 手动冒烟（用户实跑，4/4 通过）✅

- [x] **item 1 联动**：hover 一张卡 → 其主线高亮被强调；hover 一处高亮 → 其卡被强调；点高亮 → 对应卡展开并滚入面板视野。→ **卡↔高亮双向联动正常**。
- [x] **item 2 自动滚入**：开第 N 个分支 → 卡自动滚入面板视野，且**不带动主线滚动条**（容器相对滚动）。→ **新分支只滚分支面板、不带动主线**。
- [x] **item 3 每卡 loading（必验两条，缺一即停）**：
  - (1) 流式途中，loading spinner **只亮在那一张卡**（多卡并存时其余卡不亮）；✅
  - (2) 流**真正结束后** spinner 才灭、**不提前灭**；✅（瞄 Network/Ollama 确认"灭"对齐后端真结束，非前端假 success）。
  - → 信号选对：`loadingByTopic` / queue-drain 边界，不是会被 B6 提前标 success 的 `message.status`。
- [x] **item 4 B4 兜底**：不适用态（跨块选区 / 选用户消息 / 无选区）打开 assistant 右键菜单 → Ask/Open **变灰不可点**，而非消失或报错。→ **正确置灰**。

## 用户结论
> 四项冒烟全部通过；S2d 功能验收通过，进入 Phase C。色条浓淡保持 0.45 不动。

## 未覆盖 / 仍 deferred
- B2（第三个分支打不开）：本轮未纳入范围、未复现验证，**仍挂 deferred** 待单独 triage（[[../../问题与Debug记录#B2]]）。
