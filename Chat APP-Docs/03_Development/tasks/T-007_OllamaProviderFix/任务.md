# T-007 任务 brief

## 输入

- 用户报告 D-003：Ollama Provider 在 Cherry Studio v2 中显示 "Model list 0/0 Enabled"，Select Model 搜不到，日志 `Provider auto model sync failed` / `Invalid JSON response`；Ollama 设置页疑似显示 `Anthropic API Host` 和 `/v1/messages`。
- 用户已确认本机 Ollama 正常（`ollama list` 有模型；`curl /v1/models` 正常 JSON）。

## 约束

- **只诊断，不大改**。
- 如果能定位明显错位，给最小修复方案，由用户决定是否实施。
- 不进入 T-006 / Text Anchor 代码。
- 不动业务代码。

## 交付物

- [诊断.md](./诊断.md) —— 调用链、根因、修复候选、验证步骤。
- 更新 [`../../问题与Debug记录.md`](../../问题与Debug记录.md) D-003 记录：把「假设空间」替换为已确认根因 + 修复指引。

## 完成标准

- [x] 找出为什么 form 渲染 Anthropic 字段
- [x] 找出为什么 auto sync 报 Invalid JSON
- [x] 把两个现象归到同一根因
- [x] 给出一行 JSON 的最小修复
- [x] 用户接受修复方案（D-003B）
- [x] 应用 1 行 JSON 修复（`providers.json:425` 加 `"defaultChatEndpoint": "ollama-chat"`）
- [x] 自动化校验：seeder 测试（real RegistryLoader + zod schema + DB row transform）4/4 通过；ApiHost / useProviderAutoModelSync / useProviderEndpoints 16/16 通过；typecheck:web + typecheck:node 静默通过
- [ ] 用户手动 fresh install 验证（详见 [验证.md](./验证.md)）
