# 任务拆解

## T-001 聊天模块源码地图

### 子任务清单（实际执行轨迹）

| ID | 子任务 | 验证 | 状态 |
|---|---|---|---|
| T-001.1 | 摸清顶层目录 + 现有 docs 结构 | `ls` + 读 CLAUDE.md / README / architecture-overview.md | ✅ |
| T-001.2 | 调查数据结构（Message/Topic/Block/Assistant，v1+v2） | Explore agent，比对 `src/renderer/src/types/newMessage.ts` 与 `packages/shared/data/types/` | ✅ |
| T-001.3 | 调查聊天 UI 组件树（pages/home） | Explore agent，列出 HomePage/Chat/Messages/Blocks/Markdown/Inputbar/Tabs | ✅ |
| T-001.4 | 调查 Redux slices + thunk + selector | Explore agent，覆盖 newMessage/messageBlock/assistants/migrate | ✅ |
| T-001.5 | 调查 AI 调用入口 + streaming 链路 | Explore agent，串起 Inputbar → thunk → aiCore → Vercel SDK → Chunk → BlockManager → UI | ✅ |
| T-001.6 | 调查 v2 标记 / 迁移痕迹 / 双轨态 | Explore agent grep `@deprecated` / `TODO(v2)` / `v2-blocked`，读迁移器与 classification.json | ✅ |
| T-001.7 | 创建 `/Docs` 目录骨架 | `mkdir -p Docs/{01_Project,02_Architecture,03_Development}` | ✅ |
| T-001.8 | 写 `源码地图.md` | 文件落盘 | ✅ |
| T-001.9 | 写 `模块说明.md` | 文件落盘 | ✅ |
| T-001.10 | 写 `数据模型.md` | 文件落盘 | ✅ |
| T-001.11 | 写 `当前任务/任务拆解/开发日志/下一步.md` | 文件落盘 | ✅ |
| T-001.12 | 写 `风险与限制.md` | 文件落盘 | ✅ |

### 执行策略

**并行化：** 用 5 个 Explore agent 同时跑 T-001.2 ~ T-001.6，避免顺序探索。每个 agent 限定输出长度（500–600 行），避免污染主上下文。

**主上下文只做：**
1. 总体把控、目录创建
2. 汇总 agent 报告
3. 写 8 份文档

**Trade-off：** Explore agent 的行号未做逐一手工校验。已在每份文档头部 / 风险文档中明确标出，避免误导后续读者。

### 没有触及的范围（边界划清）

- ❌ 没有跑过应用（`pnpm dev`）
- ❌ 没有改任何代码 / 配置
- ❌ 没有跑 `pnpm lint` / `pnpm test` / `pnpm format`（按用户约束，本阶段纯只读）
- ❌ 未深入 main 进程的 lifecycle 服务细节（DataApi handler、消息推送等）— 留给后续任务
- ❌ 未画出 main / renderer / preload 三进程间的 IPC 调用全表 — 留给后续任务
