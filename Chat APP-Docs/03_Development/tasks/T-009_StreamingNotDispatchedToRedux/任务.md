# T-009 任务 brief

## 输入

T-008C 已经把 Chat picker 切到 v2 + 用户 fresh install 实测通过；但用户随后报告 2 个新问题：

1. **D-004**：选中 assistant 回复文本右键，Copy/Quote 可用，但 Ask about this / Open as branch 仍 disabled
   - DevTools `$$('[data-message-id][data-block-id]').map(...)` 只返回 user message
   - 没有任何 `role: "assistant"` 的 MainTextBlock wrapper

2. **D-005**：assistant 回复文本已显示、后端日志说 PATCH success，但 UI 底部三个点（BeatLoader）一直转，没有切换到操作栏（复制 / 重生 / 翻译 / 笔记 / 删除）

## 约束

- 只**诊断**，不大改
- 不动 Ollama / Provider / Chat picker（T-007 + T-008C）
- 不修 regenerate（D-007）和默认模型策略（D-006），都是单独 issue
- 目标：找出 D-004 和 D-005 是不是同源；给修复路径

## 交付物

- [诊断.md](./诊断.md) —— 流式 pipeline 的完整调用链 + 同源解释 + 3 条修复路径
- 在 [问题与Debug记录.md](../../问题与Debug记录.md) 加 D-004 / D-005 / D-006 / D-007 四个 issue

## 完成标准

- [x] 找出 D-004 现象的渲染路径根因 —— 诊断初版误判（实际 addBlock 路径已 dispatch；D-004 可能是 D-005 副作用 / 用户查询时机问题）
- [x] 找出 D-005 现象的根因 —— `newMessage.ts:275` SUCCESS 转换被注释掉了
- [x] 给 3 条修复路径 + 推荐
- [x] 用户选定方案 A → 实际实施时发现 bridge 已大部分就位，修了根因 1 行 + 防御层 finalize dispatch（共 +24 行）
- [x] 自动化校验：format / oxlint / eslint / typecheck:web / vitest 6/6 + 74/74 全过
- [x] 用户 fresh install 实测 D-005 消失（2026-05-21）：回复完成后 BeatLoader 消失 + 操作栏正常出现
- [x] 用户复测 D-004（2026-05-21）：DevTools 能看到 `role: assistant` 的 wrapper；选中文本后 Ask about this / Open as branch 可点击 → D-004 也 closed（同源验证成立，无需 T-009B）
