# T-005B 验证

## 自动化验证（已通过）

| 项 | 命令 | 状态 |
|---|---|---|
| 格式 | `pnpm biome format --write src/.../StreamingService.ts` | ✅ |
| 类型 | `pnpm typecheck:web` | ✅ |
| oxlint | `pnpm oxlint src/.../StreamingService.ts` | ✅ 0/0 |
| eslint | `npx eslint --no-warn-ignored src/.../StreamingService.ts` | ✅ 静默通过 |
| 邻近 vitest | `pnpm vitest run --project renderer src/.../services/__tests__/{ConversationService,ModelMessageService}.test.ts src/.../Messages/__tests__` | ✅ 15/15 |

## 手动验证（需用户执行）

### 端到端复现 + 修复确认

```bash
# 干净状态（保证两轮 fix 都生效）
rm -rf ~/Library/Application\ Support/CherryStudioDev

cd "/Users/sam/Library/CloudStorage/GoogleDrive-finneganwatts4@gmail.com/My Drive/APP_Dev/ChatApp/cherry-studio"
pnpm dev
```

### 期望现象

- ✅ 启动正常
- ✅ 新建 topic（T-004 已修，日志 `POST /topics body: { ..., assistantId: null }`）
- ✅ 输入消息 + 发送：
  - 用户消息显示
  - **assistant message INSERT 成功** —— DB 行 `message.modelId = NULL`
  - 控制台日志类似：`Created assistant message via Data API { topicId: '...', messageId: '...' }`
  - **不再**报 `SQLITE_CONSTRAINT_FOREIGNKEY`
- ⚠️ AI 调用是否真的返回回复 = 取决于用户**有没有配 provider + API key**。
  - 若没配：可能会看到 provider config / network 类报错 —— 这是 v2 设计要求，**不属于本任务 baseline 范围**
  - 若配了：消息流式出现，端到端通

### 不期望出现

- ❌ FK 错（关于 modelId 的）
- ❌ assistant message 行无法 INSERT

### 退化测试（确认未引入回归）

- **若 caller 传了合法 UniqueModelId**（如 `"openai::gpt-4o"`）：`isUniqueModelId` 返回 true → `safeModelId = modelId` → 透传 → message.modelId 落库为真值，行为与改前一致 ✅
- 已有 topic / 已有消息：不受影响（本改动只在新建 assistant message 时生效）

## 已知遗留（不阻塞，需要单独 ticket）

| 现象 | 同源原因 | 处理 |
|---|---|---|
| `assistant.id = 'default'` warning (`useAssistantPresets`) | 同 T-004 sentinel | 留待方案 C 整治 |
| message.modelId 落 NULL → SQL 统计/分析无法 JOIN user_model | 设计选择；renderer 内存的 `message.model` 仍持完整对象 | 长远应填 `modelSnapshot` |
| Agent session 写路径不走 DataApi | 已知迁移残留 | 独立分支 |
| 用户未配 provider/API key 时 AI 请求失败 | v2 设计要求 | 不属于 baseline；UI 引导用户配置 |

## 验证签收

- 自动化 ✅
- 手动 ⏳ 待用户 `rm + pnpm dev` + 发消息 + 确认 assistant message 行能落库
