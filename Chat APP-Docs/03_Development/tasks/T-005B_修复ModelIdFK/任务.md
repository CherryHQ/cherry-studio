# T-005B 任务 brief

## 任务编号 & 标题
T-005B：修复 assistant message 写入时 modelId 导致的 FK 失败

## 阶段
Phase 2 / Baseline Fix

## 起止
- 开始：2026-05-20 深夜
- 状态：✅ 代码 + 自动化完成；⏳ 待用户端到端验证

## 任务输入（用户原始描述）

> 现在动手 StreamingService 一处改 + commit。
>
> 目标：StreamingService 创建 assistant message 时，不要把 v1 短 modelId（如 qwen）写入 v2 message.modelId FK。
>
> 请做最小修复：
> - 修改 `src/renderer/src/services/messageStreaming/StreamingService.ts`
> - 使用 `isUniqueModelId` 判断，只有合法 UniqueModelId 才传入 modelId，否则传 undefined
> - 不改 schema、不改 seeder、不改模型选择逻辑、不做重构

## 约束
- ✅ 用户已明确授权改代码
- 遵守 CLAUDE.md（lint/test/format before completion；commits use Conventional + signoff）
- **不**改 schema、seeder、模型选择逻辑、不重构

## 完成标准

- [x] `StreamingService.createAssistantMessage` 用 `isUniqueModelId` 清洗 `modelId`
- [x] `pnpm biome format` 通过
- [x] `pnpm typecheck:web` 通过
- [x] `pnpm oxlint` 变更文件 0/0
- [x] `pnpm eslint` 变更文件通过
- [x] 邻近 vitest（ConversationService / ModelMessageService / MessageGroup）通过
- [ ] 用户端到端验证（`rm + pnpm dev` → 发消息 → assistant message 行能 INSERT；不再报 FK）
- [ ] D-002 在 [问题与Debug记录.md](../../问题与Debug记录.md) 移到「已 closed」
- [ ] R0.2 在 [../../../01_Project/风险与限制.md](../../../01_Project/风险与限制.md) 降级为 ✅

## 交付物
- 代码：`src/renderer/src/services/messageStreaming/StreamingService.ts`（一处 DTO 清洗 + 一行 import）
- vault：本文件夹 + 全套滚动文档更新
