# T-006D-1 任务 brief

## 任务编号 & 标题
T-006D-1：Branch Panel 壳子原型

## 任务输入（用户原始描述）

> 在 T-006C 的「展开为分支 / 针对此处提问」菜单点击后，打开一个 Branch Panel，显示当前选区信息。
>
> 要求：
> 1. 基于 T-006B/T-006C 已有的 selectedText、messageId、blockId
> 2. 新增最小 Branch Panel UI
> 3. Panel 显示：selectedText / sourceMessageId / sourceBlockId / 追问输入框 / 取消按钮 / 创建分支按钮（disabled 或只打日志）
> 4. 暂时不做 POST /topics fork
> 5. 暂时不做高亮
> 6. 暂时不做 inputbar 注入
> 7. 不改数据库、不改模型调用
> 8. 更新 T-006D 文档、当前状态、下一步、会话日志
> 9. 跑必要的 typecheck / lint / vitest

## 约束
- 用户已明确授权代码改动
- 严格守 scope：不做超出"壳子"的事
- 遵守 CLAUDE.md：`@cherrystudio/ui`、`loggerService`、i18next、Conventional Commits

## 完成标准
- [x] BranchPanel 组件可被打开 / 关闭
- [x] 显示 selectedText / messageId / blockId 三个字段
- [x] 追问输入框（受控）
- [x] 取消按钮调 `onOpenChange(false)`
- [x] 创建分支按钮 `logger.debug` + 关闭 panel
- [x] 菜单点击触发 Panel 打开（端到端：SelectionContextMenu callback → Messages.tsx state → BranchPanel render）
- [x] BranchPanel.test.tsx 覆盖 5+ 场景
- [x] SelectionContextMenu.test.tsx 新增 callback prop 测试
- [x] format / typecheck / oxlint / eslint / i18n / vitest 全过

## 交付物

代码（5 文件 + 1 目录）：
- `src/renderer/src/pages/home/Messages/BranchPanel/{BranchPanel.tsx, types.ts, index.ts}`（新）
- `src/renderer/src/pages/home/Messages/BranchPanel/__tests__/BranchPanel.test.tsx`（新）
- `src/renderer/src/components/SelectionContextMenu.tsx`（改：加 `onOpenBranchPanel?` prop）
- `src/renderer/src/components/__tests__/SelectionContextMenu.test.tsx`（改：+1 callback 用例）
- `src/renderer/src/pages/home/Messages/Messages.tsx`（改：state + wire + render）
- `src/renderer/src/i18n/locales/{en-us,zh-cn,zh-tw}.json`（改：+ panel.* 5 keys × 3 locale）

vault：本文件夹 + 滚动文档更新。

## 不在范围
- ❌ POST /topics fork（D-2）
- ❌ 高亮 `<mark>` 注入（T-006E）
- ❌ inputbar 注入（D-3）
- ❌ `useCache` / `useBranchAnchors` hook（D-2）
- ❌ 数据库 / 模型调用
