# T-006D-2A 验证清单

## 自动化（已通过）

```bash
pnpm vitest run \
  src/renderer/src/hooks/__tests__/useBranchFork.test.ts \
  src/renderer/src/pages/home/Messages/BranchPanel/__tests__/BranchPanel.test.tsx
# → 16/16 passed (5 useBranchFork + 11 BranchPanel)

pnpm vitest run src/renderer/src/components/__tests__/SelectionContextMenu.test.tsx
# → 11/11 passed（回归保证 SelectionContextMenu 未受影响）

pnpm typecheck:web      # ✅
pnpm i18n:check         # ✅
oxlint <changed files>  # ✅
eslint  <changed files> # ✅ 0 errors（仅 1 个 pre-existing warning）
biome check --write     # ✅ 排版 collapse，无语义变更
```

## 手动 E2E（待用户在 fresh install 上跑）

```bash
rm -rf ~/Library/Application\ Support/CherryStudioDev && pnpm dev
```

| # | 步骤 | 期望 |
|---|---|---|
| 1 | 选一个 provider（Ollama / CherryAI 都行），打开默认 topic，输入"什么是知识蒸馏？" | assistant 流式回复完成，BeatLoader 消失，文本可选 |
| 2 | 用鼠标选中 assistant 回复里的一句话（比如"老师模型把行为转移给学生模型"） | 右键菜单出现 `Ask about this` + `Open as branch` |
| 3 | 点 `Open as branch` | Dialog 弹出，selectedText preview + messageId/blockId 字段 + 空的 follow-up Textarea |
| 4 | follow-up 留空，点 Create branch | 不创建分支；Textarea 下方红字 "请输入追问内容。"；Create 按钮立即可再点 |
| 5 | 输入"学生模型是什么意思？"，点 Create branch | Create 按钮变 spinner + disabled；约 0.5–1 s 后 Dialog 自动关闭；侧边栏出现新 topic；新 topic 自动选中；模型开始流式回复 |
| 6 | 检查新 topic 的对话历史 | 顶上是源 assistant message 的 parent lineage（视具体 v2 渲染而定，至少能看到自己刚发的 user message + 模型回复）|
| 7 | 关掉网络 / 让 DataApi 报错，重复 2–5 | Dialog 显示红字 "创建分支失败，请重试。"；followUp 文本不丢；Create 恢复可点 |
| 8 | 在流式回复**未完成时**做步骤 2–3 | 当前 D-2A 范围内**不会拦截** —— 用户能拿到截断的 selectedText 并照常 fork。这是 D-2B 处理范围（已记录） |

## 测试边界（明确不测）

- v1 `NEW_BRANCH`（MessageMenubar 整条消息分支）：与本任务正交
- 切到新 topic 后是否能继续聊、能否切回源 topic：属于既有 v2 topic 切换路径，不在本子任务
- anchor 持久化、跨 topic 列表：T-006E 范围
- groupId 透传：已知限制，未来 v2 完整迁移修

## Closure 条件

- [x] 代码 + 测试入库
- [x] 自动化全过
- [ ] **用户 fresh install 跑过手动 E2E 第 1–7 步且未发现回归**
- [ ] commit + 文档关闭 D-2A，更新 T-006D / 当前状态 / 下一步
