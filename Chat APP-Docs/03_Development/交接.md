# 项目交接（Handoff Summary）

> 写于 2026-05-21。会话即将 `/clear`，下次接手用这份从零起步。
> 阅读顺序：本文 → [当前状态.md](./当前状态.md) → [下一步.md](./下一步.md) → [tasks/README.md](./tasks/README.md)。

## 1. 项目快照

| 维度 | 值 |
|---|---|
| 仓库 | `cherry-studio`（v2 重构中）|
| 当前分支 | `feature/expand-branch-chat-v2` |
| 主分支 | `main` |
| 工作产品 | 在 Cherry Studio v2 基础上做 Conversation Graph 原型（主线 + 分支 + 时间线 + Workspace）|
| 当前会话目标 | T-006 Text Anchor Branch UI 原型实施（B/C/D-1 已 commit + 端到端可用，剩 D-2/A/E/F）|
| Obsidian vault 根 | `Chat APP-Docs/`（注意不是仓库根 `docs/`，那是 Cherry 自带英文官方 docs）|

## 2. 已完成（按时间）

### Phase 2 — Baseline FK（D-001 / D-002 closed）

| 任务 | Commit | 内容 |
|---|---|---|
| T-003 / T-004 | `15ad2eb08` | `topic.assistantId === 'default'` 哨兵兜底（fresh install 创建 Topic FK 失败）|
| T-005A / T-005B | `15ad2eb08` | `StreamingService.createAssistantMessage` 用 `isUniqueModelId` 守卫 modelId；v1 raw id 写 null |
| 之前已有 | `5a783d6f5` | `fix(chat): unblock fresh install topic and message creation` |

### Phase 2.5 — Ollama 链路（D-003 A/B/C closed）

| 任务 | Commit | 内容 |
|---|---|---|
| T-007 D-003B | `99c24f5ba` | `packages/provider-registry/data/providers.json` Ollama 加 `"defaultChatEndpoint": "ollama-chat"`（1 行 fix）|
| T-008C D-003C | `99c24f5ba` | `chat-model-popup.tsx` 数据源切 v2 DataApi + `toV1ProviderShim`/`toV1ModelShim` + CHERRYAI fallback（option A）|

### Phase 2.6 — 消息渲染（D-004 / D-005 closed）

| 任务 | Commit | 内容 |
|---|---|---|
| T-009 | `c5019162f` | `newMessage.ts:275` 取消注释 SUCCESS 状态转换 + `StreamingService.finalize` 末尾追加防御性 `dispatch(updateMessage({ status }))` + 6 单元测试 |

### Phase 3（在做）— Text Anchor Branch UI（T-006）

| 子任务 | Commit | 状态 |
|---|---|---|
| T-006B 文本选中捕获 | `d579fdcf2` | ✅ `findBlockContext` + MainTextBlock 加 `data-message-id`/`data-block-id` |
| T-006C SelectionContextMenu 扩展（含 role 收口）| `76ee326a0` | ✅ Ask/Open 菜单 + 仅 assistant block 启用 + i18n + 11 测试 |
| T-006D-1 BranchPanel 壳子 | `76ee326a0` | ✅ Dialog + 字段显示 + 创建按钮 log only + 6 测试 |
| T-006A 测试数据 | — | ⏳ 可选 |
| **T-006D-2 真实 fork + cache** | — | ⏳ **下一步推荐** |
| T-006D-3 Sheet 改造 + inputbar 注入 | — | ⏳ |
| T-006E 高亮 `<mark>` 注入 | — | ⏳ |
| T-006F 折叠/展开 | — | ⏳ v1.1 候选 |

### 端到端验证（2026-05-21 用户 fresh install 通过）

```bash
rm -rf ~/Library/Application\ Support/CherryStudioDev && pnpm dev
```

- ✅ 新建 topic / 发消息 / 流式回复 / 无 FK
- ✅ Ollama Provider 设置页显示模型
- ✅ Chat picker 能选 Ollama
- ✅ `gemma4:e4b` 流式回复
- ✅ BeatLoader 流完消失 + 操作栏出现
- ✅ DevTools 看到 assistant `data-message-id` wrapper
- ✅ 选中 assistant 文本 → Ask about this / Open as branch 可点击

## 3. 仍 open（低优先级，用户要求先记录）

| ID | 标题 | 何时再看 |
|---|---|---|
| **D-006** | fresh install 默认模型仍是 CherryAI Qwen，不自动选 Ollama | v2 默认模型策略迁移时一起处理 |
| **D-007** | Regenerate 切到 Ollama 后点旧回复 refresh 无明显反应 | D-005 已 closed，可单独复测；如仍存在则开 T-010 |

详见 [问题与Debug记录.md](./问题与Debug记录.md)。

## 4. Git 状态

```
分支：feature/expand-branch-chat-v2
最近 8 commits（旧→新）：
  9e4feb1e8 docs(project): organize chat app workflow and notes
  15ad2eb08 fix(message): ignore legacy model id when creating assistant message       (D-001 + D-002)
  5a783d6f5 fix(chat): unblock fresh install topic and message creation
  d579fdcf2 feat(messages): tag MainTextBlock DOM with messageId/blockId ...           (T-006B)
  76ee326a0 feat(messages): wire SelectionContextMenu to branch-anchor handlers       (T-006C + T-006D-1 + role 收口)
  b0789781a docs                                                                       (T-007/T-008 docs sweep)
  99c24f5ba fix(provider-registry): set Ollama defaultChatEndpoint to ollama-chat     (T-007 + T-008C + 包含全部 closure 文档)
  c5019162f fix(messages): transition message status to SUCCESS when streaming ...    (T-009 D-004 + D-005)

工作区未 commit：
  M .gitignore                                  ← 用户自行管理，不在本次任务范围
  MM Chat APP-Docs/.obsidian/graph.json         ← Obsidian 私人 vault 设置，不入 commit
  MM Chat APP-Docs/.obsidian/workspace.json     ← 同上
```

**注意**：所有任务相关代码 + 文档都已入 commit。剩下三个未 commit 文件全部是个人/工具状态，按用户惯例**不要**入 commit。

## 5. 本次会话动过的关键文件（按目录）

### v1 / v2 数据层桥接

- `src/renderer/src/services/messageStreaming/StreamingService.ts` — T-009 在 `finalize` 末尾加防御 `dispatch(updateMessage({ status }))`
- `src/renderer/src/store/newMessage.ts` — T-009 在 `upsertBlockReference` 取消注释 PROCESSING→SUCCESS 转换（line 275）
- `src/renderer/src/components/Popups/SelectModelPopup/chat-model-popup.tsx` — T-008C 切 v2 DataApi 数据源 + CHERRYAI fallback
- `src/renderer/src/components/Popups/SelectModelPopup/__tests__/chat-model-popup.test.tsx` — 新 6 用例
- `src/renderer/src/store/__tests__/newMessage.upsertBlockReference.test.ts` — T-009 新 6 用例
- `src/renderer/src/services/AssistantService.ts` — T-004 `mapLegacyTopicToDto` 把 sentinel 'default' 映射为 null
- `src/renderer/src/services/messageStreaming/StreamingService.ts:572` — T-005B `safeModelId = isUniqueModelId(modelId) ? modelId : undefined`

### T-006 Text Anchor Branch（已 commit，下次接手要继续）

- `src/renderer/src/utils/branchAnchor/findBlockContext.ts` — DOM helper：Range → `{messageId, blockId, role}`
- `src/renderer/src/components/SelectionContextMenu.tsx` — 右键菜单 + role 收口（仅 assistant 启用 Ask/Open）
- `src/renderer/src/pages/home/Messages/Blocks/MainTextBlock.tsx` — `data-message-id` / `data-block-id` / `data-message-role` wrapper
- `src/renderer/src/pages/home/Messages/Blocks/index.tsx` — block 类型分发；MAIN_TEXT/CODE → MainTextBlock
- `src/renderer/src/pages/home/Messages/BranchPanel/{BranchPanel,types,index}.tsx` + `__tests__/` — D-1 壳子，等 D-2 接 fork

### 数据 / Provider

- `packages/provider-registry/data/providers.json:425` — Ollama 加的 `defaultChatEndpoint` 那一行（T-007）

## 6. 关键文档（vault `Chat APP-Docs/`）

| 文档 | 用途 | 优先级 |
|---|---|---|
| 本文（`交接.md`）| 一份钟看完接手 | ⭐⭐⭐ |
| `当前状态.md` | 当前阻塞 / open issues / 阶段定位 / 最近时间线 | ⭐⭐⭐ |
| `下一步.md` | 唯一未完成动作 + 候选路径 | ⭐⭐⭐ |
| `问题与Debug记录.md` | D-XXX issue 索引（open + closed）| ⭐⭐ |
| `tasks/README.md` | 全部 T-XXX 任务表 | ⭐⭐ |
| `tasks/T-006_TextAnchorBranchUI/README.md` | T-006 设计 / 子任务进度 | ⭐⭐⭐ |
| `tasks/T-006_TextAnchorBranchUI/设计.md` | 主设计文档（捕获点 / 菜单 / Panel / 高亮 + rationale）| ⭐⭐ |
| `tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/README.md` | D-1/D-2/D-3 子步进度 | ⭐⭐⭐ |
| `tasks/T-009_StreamingNotDispatchedToRedux/` | T-009 全套（含诊断更正记录）| ⭐ |
| `02_Architecture/源码地图.md` | 模块全景 | ⭐⭐ |
| `02_Architecture/分支对话.md` | 分支对话相关上下文 | ⭐ |
| `01_Project/风险与限制.md` | 红黄灯清单 | ⭐ |
| `../../CLAUDE.md` | 工程纪律（v2 throwaway / Tailwind+Shadcn / `loggerService` / Conventional Commits + `--signoff`）| ⭐⭐⭐ |

## 7. 下一步推荐：T-006D-2 真实 fork

**目标**：把 T-006D-1 的"创建按钮 log only"升级为真实创建分支 topic。

**参考**：[`tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/README.md` §3](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/README.md)。

骨架：

```ts
// BranchPanel.tsx 的 onCreate 替换 log 为：
const newTopic = await dataApiService.post('/topics', {
  body: {
    name: anchor.selectedText.slice(0, 30),
    assistantId: currentAssistant.id === 'default' ? null : currentAssistant.id, // T-004 兼容
    sourceNodeId: anchor.sourceMessageId
  }
})
setActiveTopic(newTopic)
// 把 selectedText + 用户输入注入新 topic 的第一条 user message
// 发送动作走现有 messageThunk.sendMessage
```

**还需要**：

- 用 `useCache('branch-anchor.${topicId}.${anchorId}', ...)` 把 BranchAnchor 持久化（v1.0 ephemeral 即可）
- 流式中 disable 「展开为分支」按钮（参考 T-006C 设计 §2 的 `data-block-streaming` 思路）
- 不要做：anchor 跨 topic 列表 / 折叠展开（留 D-3 / F）

## 8. 工程纪律必记（来自 CLAUDE.md）

1. **v1 是 throwaway**：`Redux / Dexie / ElectronStore` 不修 v1 bug；新功能走 v2（DataApi / Cache / Preference + Tailwind+Shadcn `@cherrystudio/ui`）
2. **schemas + drizzle SQL 是 throwaway**：随便改 schema；不写 patch migration
3. **路径走 `application.getPath()`**；日志走 `loggerService`；i18n 全字符串走 `i18next`
4. **Conventional Commits + `--signoff`**：scope kebab-case 模块名，禁泛 `main`
5. **保持线性历史**：`git pull --rebase`；push 前 `git fetch` + rebase 本地
6. **完成代码后**：`pnpm lint && pnpm test && pnpm format`（已修 = `pnpm build:check`）
7. **新 UI 组件**禁 antd / HeroUI / styled-components — 用 `@cherrystudio/ui`
8. **数据 / Window / Lifecycle**：分别看 `docs/references/{data,window-manager,lifecycle}/README.md`
9. **测试用 unified mock**：见 `tests/__mocks__/README.md`；DB 测试用 `setupTestDatabase()`（`@test-helpers/db`）
10. **不要 commit `.obsidian/{workspace,graph}.json`**：个人 vault 设置；本仓库 `.gitignore` 应该已经覆盖（若没有可一并入 commit）

## 9. 本次会话已确认的关键决策（避免重复讨论）

| 决策 | 何处生效 | 何时回收 |
|---|---|---|
| Ollama Anthropic compat 端点保留 | `providers.json` 仍含 `anthropic-messages`；只补 `defaultChatEndpoint` 主端点 | Ollama 删 anthropic compat 后再清 |
| chat-model-popup `CHERRYAI_PROVIDER` fallback option A | picker 内部硬编码注入直到 v2 catalog seed cherryai user_model | v2 cherryai 真有内容时去掉 |
| `assistant.model` 仍是 v1 shape | T-008C shim 在 picker 出口转 v1，下游 0 改 | v2 完整迁移 assistant 时整体迁 |
| T-005B 的 `isUniqueModelId` 守卫 | StreamingService.createAssistantMessage 写 modelId 时 fallback null | aiCore 接 runtime provider 后可删 |
| T-009 `newMessage.ts:275` 单一 SUCCESS 转换 | reducer 单块 SUCCESS 即转 message.status | finalize 防御 dispatch 兜底；v2 渲染层迁完后整段重写 |

## 10. 不要做的事（红灯）

- ❌ 在 v1 路径上修 bug（除非阻塞 v2）
- ❌ 手改 codegen 4 产物（[CLAUDE.md § Data Classification Toolchain](../../CLAUDE.md)）
- ❌ patch migration on `migrations/sqlite-drizzle/*.sql`
- ❌ 新组件写 styled-components / 引入 antd / HeroUI
- ❌ 先删 Redux 再迁 UI（顺序反了会黑屏）
- ❌ 绕过 `messageThunk` / `StreamingService` 直接写 DataApi
- ❌ 给 user_model / assistant 表 seed 静态行
- ❌ commit `.obsidian/{workspace,graph}.json`

## 11. 新会话第一动作建议

```
1. 读 Chat APP-Docs/03_Development/交接.md（本文）
2. 读 Chat APP-Docs/03_Development/当前状态.md
3. 读 Chat APP-Docs/03_Development/下一步.md
4. 用户提需求 → 检查是否落在 T-006D-2 / 其他 T-006 子任务 / 新 D-XXX issue
5. 写代码前：先读直接相关的 task 文件夹下的 README + 设计 / 实施记录
```

如果用户说「继续 T-006D-2」：

```
1. 读 tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/README.md §3 fork 调用骨架
2. 读 src/renderer/src/pages/home/Messages/BranchPanel/BranchPanel.tsx （T-006D-1 壳子，现在的 createBranch 是 log）
3. 读 packages/shared/data/api/schemas/topics.ts 看 POST /topics { sourceNodeId } 的真实签名
4. 读 src/renderer/src/hooks/useBranchAnchors.ts —— 如果不存在就要新建（设计参考 T-006D-1）
5. 实施：替换 onCreate log 为真 fork → useCache 持久化 anchor → 流式 disable
6. 测试：扩 BranchPanel.test.tsx
7. 跑 build:check
8. 更新 T-006D / 当前状态 / 下一步 / 开发日志 / 会话日志
```
