# 当前状态

> 更新于 2026-06-03（**P1-S2c-accordion — 面板改 accordion 手风琴 + locate + compose label**）：S2c 的 master/detail 修了 B1,但 dev-smoke 暴露"标题与正文分家"不符预期(§3b 定稿=accordion)。本步把**两区并成单滚动区** `branch-pane-scroll` + **标题贴内容**:每分支=新 `BranchAccordionItem`(`branch-item-${id}` 真实盒子 `shrink-0`)= `BranchTab`(头)+`{!collapsed && BranchDetail}`(内容,同 item 内);`BranchDetail` 去自身盒子(盒子/shrink-0 移到 item);**删 master/detail 两区**。加 **locate**(`scrollItemToTop` = `[data-branch-item-id].scrollIntoView({block:'start'})`:新建滚 / 点折叠标题展开滚 / 折叠不滚)。**compose label** 新 i18n `compose_label` 修误标。**绝无** sticky/display:contents;B3(composerKeyboard)+ shrink-0 原样保留。focused **75/75 绿**;M1(结构:detail 移出 item→RED)+ M2(locate:no-op→新建/展开 RED、折叠仍绿)mutation 非空转;视觉(标题滚走/不重叠/滚到顶)= manual-smoke;保护文件 git diff 全空;`pnpm lint` 全过。**未 commit**。详见 [[tasks/P1_MultiBranch/P1-S2c_MasterDetail/README]]。
> 
> （下面 2026-06-03 的 master/detail 条目为 S2c 中间态,已被上面的 accordion 取代。）：S2b-3 在 dev 实测暴露 5 缺陷(B1 重叠 / B2 开不出 / B3 Enter 不发 / B4 菜单灰 / B5 不中止),其中 **B1** 由 S2b-3 的 `display:contents`+手搓 sticky 引入。本步**整体丢弃 S2b-3 sticky-stacking**,改 **master/detail**:`BranchPane` 重写为 flex 列两兄弟区 —— **MASTER**(`branch-pane-master`,flex-none max-h-40% overflow,N 个 `BranchTab`,是 detail 的兄弟非祖先 → 滚 detail 时常驻)+ **DETAIL**(`branch-pane-detail`,flex-1 overflow,未折叠分支各渲 `BranchDetail` **真实盒子** → 普通盒子顺序排不重叠,修 B1)。**绝不用 sticky/display:contents**。拆旧 `BranchCard`→`BranchTab`+`BranchDetail`(删 BranchCard)。**B3**:新 `composerKeyboard.ts` 唯一 `handleBranchComposerKeyDown`(`isSendMessageKeyPressed(event,'Enter',true)`+IME 守卫),`BranchComposer`(初始,之前**完全无 keydown**)+ `BranchFollowUpComposer` **都调它** → 单一来源、无第三份。**丢弃 S2b-3 越界**:`Chat.tsx`/`index.css` 回 HEAD、删 `useHighlightEmphasis`(hover 归后续);保留 `input.ts` 的 `forceEnterToSend` + `BranchMessageStream` 无内部滚动。focused **125/125 绿**;B3 mutation 非空转(no-op handler → 4 RED);IME 两 composer 均不提交;**B1 视觉(重叠/滚动 tab 常驻)= 显式 manual-smoke,单测只断言结构不变量、不用样式代理冒充覆盖**;保护文件(含 Chat.tsx)git diff 全空;`pnpm lint` 全过。**未 commit**。详见 [[tasks/P1_MultiBranch/P1-S2c_MasterDetail/README]]。
> 
> ⛔ 2026-06-01 P1-S2b-3（sticky-stacking via display:contents）**已废弃、从未入库** —— dev 实测 B1 重叠,被上面 S2c 取代。下面这条 S2b-3 记录仅作历史。
> 
> 更新于 2026-06-01（**P1-S2b-2 — multi-branch follow-up：conversation-state composer + per-card 路由**；**已入库 commit `1c7f4ba20`**）：补 S2b-1 推后的 follow-up composer。新 `useBranchFollowUp` hook **复用现有 `sendMessage` thunk**（无 POST /topics，不改 streaming/messageThunk/provider/model-picker 内部）；新 `BranchFollowUpComposer`（极简 textarea + Send，本地 draft = 每卡输入隔离）挂在 conversation 态卡片底部；`BranchPane` + prop `onSendFollowUp(branchId,f)` 每卡闭包 `(f)=>onSendFollowUp(branch.id,f)`；`Chat.tsx` `handleSendBranchFollowUp` 把 branchId 解析成 `branch.topic` 再调 hook。**关键发现**：`messageThunk:853` 每次 send 都从 `assistant.topics.find(topicId).prompt` 重建 system prompt → follow-up 也必须喂带该 branch topic 的 synthetic assistant，否则第二轮丢 selectedText 上下文。**每卡隔离三条腿** = 本地 draft + 路由只命中 B.topic.id + 每卡 `useTopicMessages(topic.id)` stream，未建并发流处理。focused **94/94 绿**；**路由 mutation 双向**：`→branches[0]` 让 `[A,B,C] 在 B` 测试 RED（`在 A` GREEN 是 A==branches[0] 构造使然、非 fake-green），`→branches[last]` 让 A、B 都 RED（证 A 测试非空转）；`sourceHighlight.ts` / `BranchAnchorContext.tsx` git diff 空；`pnpm lint` 全过。**未 commit**。详见 [[tasks/P1_MultiBranch/P1-S2b-2_FollowupRouting/README]]。
> 
> 更新于 2026-06-01（**P1-S2b-1 — multi-branch UI：append + folder-tab card stack + color cycling + N>1 真路径**；**已入库 commit `5c7071a69`**）：P1 三步法细化为 S1 / S2a / S2b-1 / S2b-2 / S3，本轮做 S2b-1（S2a 准备好的 N-capable highlight 实现真正接 UI）。openBranchAnchor 从 replace 改 APPEND；新分支按 `pickNextColor` 分配独立颜色；BranchPane 重写为 N 张 BranchCard 的纵向卡片栈（每张卡 = 彩色 tab header + #N 徽章 + 选区 snippet + chevron + X + body）；每卡独立 collapse、独立 close（targeted `clearSourceHighlight(branchId)`）。**sourceHighlight.ts 全程禁止永久改动**（mutation 临时改 + revert，`git diff` 空已确认）；`__BRANCH_ANCHOR_CTX_CACHE__` 保留；fork / streaming / Redux / Dexie 未触；BranchPane **保持在 BranchAnchorContext Provider 外**（防 cross-contamination）。focused **84/84 绿**（sourceHighlight 19 + MainTextBlock 23 + BranchCard 12 + BranchPane 10 + constants 7 + 其它）；**data-hl mutation 8 expected-red 命中 + 0 fake-green 残留**：首跑 6 RED + 2 个 "after === before" preserve-pattern 属性断言被 mutation 旁路（vacuous-GREEN）→ follow-up grep 全仓后把两处都改成绝对断言（`expect(...).toBe('cN')`），重跑 mutation 得 8 RED；revert 后 `sourceHighlight.ts` git diff 仍空。详见 [[tasks/P1_MultiBranch/P1-S2b-1_MultiBranchUI/README]]。**未 commit**。
> 
> 更新于 2026-06-01（**P1-S2a — sourceHighlight.ts 加 data-branch-id + per-branch color + targeted clear；UI 仍 ≤ 1 分支**）：P1 三步法细化为 S1 / S2a / S2b / S3，本轮做 S2a（实现层升级，UI 不动）。`paintSourceHighlight(blockEl, start, end, branchId, colorKey)` 给每个注入 span stamp `data-branch-id` + `data-hl`；`clearSourceHighlight(branchId)` 改 attribute-selector targeted（不再全擦）；paint 体内 first-line 也按 id 擦旧（关键：S2b 上 UI 时 paint(B) 不会隐式擦 A，silent regression 被消除）；6 色 CSS palette 注入到 head。MainTextBlock effect / Chat.tsx close path 全改 targeted clear。多分支隔离能力由测试用双 branchId 直接构造 fixture 验证（4 个新增 multi-branch 用例 + 6 个改写的 paint/clear 用例 + 9 个 round-trip 未动）。focused 40/40 绿；**mutation M1（break targeting）+ M2（break stamping）全部 expected-red 命中、零 fake-green**；`pnpm lint` 全过；`__BRANCH_ANCHOR_CTX_CACHE__` 模式保留；fork / streaming / provider / Redux / Dexie 全未触。**未 commit**。详见 [[tasks/P1_MultiBranch/P1-S2a_HighlightImpl/README]]。
> 
> 更新于 2026-06-01（**P1 开工 — S1 state shape 推广 branches[] 数组，零行为变化**）：P1 三步法 = S1 state foundation / S2 multi-branch UI + multi-highlight / S3 disposition。本轮 S1：把 `branchAnchor + branchTopic` 双 useState 推广成 `branches: Branch[]` + `collapsedBranchIds: Set<string>`，BranchAnchorContext value 改成 `{ anchors: BranchAnchorHighlight[] }`（字段 `{branchId, blockId, selectionStart, selectionEnd}`），MainTextBlock effect 改成 filter+迭代，BranchAssistantContext synthetic.topics 改成 spread。**`branches.length ≤ 1` invariant 保持**（没有 UI 能开第 2 个分支），所有派生值在 length ≤ 1 时与之前一字不差。`sourceHighlight.ts` git diff **空**；`__BRANCH_ANCHOR_CTX_CACHE__` 模式保留。focused 36/36 绿；non-vacuous check 通过；`pnpm lint` 全过。**已入库（commit `f915938ec`）**。详见 [[tasks/P1_MultiBranch/P1-S1_StateFoundation/README]]。
> 
> 更新于 2026-05-31（**S6'/T-006E 测试 mutation 验证 + 修一个新被发现的假绿**：P0（commit `f3bf0dbf7`）已入库；本轮对那批新测试做 mutation 验证 —— `paintSourceHighlight` 和 `clearSourceHighlight` 分别临时改 no-op，跑 focused 测试看应红是否真红。结果：paint 破坏时 8 个 RED 全符合预期；clear 破坏时 3 个 sourceHighlight 用例 RED 符合预期，**但 MainTextBlock "removes all spans when component unmounts (effect cleanup)" 假绿** —— testing-library `unmount()` 会把整个 React 子树连同 span 一起拆掉，根本测不到 `clearSourceHighlight` 是否真被 effect cleanup 调用。1-for-1 替换为 deps-change cleanup 用例（用 `rerender` 翻 Provider value，组件保持 mounted，spans 只能通过 effect cleanup 消失）。新用例在 Mutation 2 下立刻 RED，覆盖目的达成。`sourceHighlight.ts` 全程字节不变，`git diff` 空。focused 36/36 仍绿。**未 commit**。
> 
> 更新于 2026-05-26（**S6'/T-006E paint/clear 链获得真实测试覆盖 + 删除 dangling `data-branch-anchored` 属性**：原 MainTextBlock 4 个 highlight 用例只断言不参与渲染的 `data-branch-anchored` 属性 —— 假绿，13 轮 D-013 debug 都没捕到任何高亮失效。本轮重写为断言 `span.branch-anchor-highlight` 的真实注入与卸载清扫；sourceHighlight.test.ts 新增 paint/clear DOM 突变链覆盖（单/多节点 span 文本、replaceWith + normalize 字节级复原、A→B 切换与循环不累积）；两份测试顶部都写明 SCOPE：jsdom 无 layout/paint → 绿不等于用户可见。`pnpm build:check` 全绿（9448 passed）。**已入库（commit `f3bf0dbf7`）**。
> 
> 更新于 2026-05-23（S6' source-passage highlight 全链路完成 + cleanup 验证通过：CSS Custom Highlight API 在本 Electron/Chromium + markdown DOM 环境零渲染 → 切换到 `<span class="branch-anchor-highlight">` 跨节点 wrap + `replaceWith` 全局清扫 + Branch Panel 关闭路径连通。D-2B side-by-side S1'-S4' commits `c278f5c0a` + `e49d1cc0f` + `3e2ecec41` 已入库；D-008 scroll + D-009 regenerate + Task 3 resizable divider + S6' highlight 全部视觉验证通过。**完整决策史见 [[问题与Debug记录#D-013-CLOSED]] 与 [[tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/README]]**）。

## 当前阻塞

✅ **无**。Baseline + Ollama 端到端 2026-05-21 fresh install 验证全部通过。

| 任务 | 代码 | commit | 手动验证 |
|---|---|---|---|
| T-004（D-001 / topic 创建） | ✅ | ✅ `15ad2eb08` | ✅ 2026-05-21 |
| T-005B（D-002 / assistant message 写入） | ✅ | ✅ `15ad2eb08` | ✅ 2026-05-21 |
| T-007（D-003A/B / Ollama provider 同步） | ✅ | ✅ `99c24f5ba` | ✅ 2026-05-21 |
| T-008C（D-003C / Chat picker 切 v2） | ✅ | ✅ `99c24f5ba` | ✅ 2026-05-21 |
| T-009（D-005 + D-004 / message.status SUCCESS 转换） | ✅ | ⏳ 代码未 commit（newMessage.ts:275 + StreamingService.finalize + test） | ✅ 2026-05-21 |

> 验证现场：用户两轮 fresh install + `pnpm dev` 实测 Ollama 同步、Chat picker 可选 Ollama、gemma4:e4b 正常流式回复、baseline FK 未复现；T-009 之后 BeatLoader 消失 + 操作栏出现 + assistant wrapper 在 DOM + Ask/Open 可点。

## 🟢 已知 open 问题（仅低优先级候选）

| ID | 标题 | 影响 | 详情 |
|---|---|---|---|
| **D-006** | fresh install 默认模型仍 CherryAI | 小毛刺，可手动切；用户明确要求**先记录、不优先修** | [问题与Debug记录.md](./问题与Debug记录.md) |
| **D-007** | Regenerate 切到 Ollama 后无反应 | 候选；D-005 已 closed 后可单独复测；如仍存在则开 T-010 | 同上 |

## 现在可以做的事

### ✅ 推荐路径

1. **Phase 3：T-006 Text Anchor Branch UI 实施进度**：
   - ✅ T-006B 文本选中捕获 —— commit `d579fdcf2`
   - ✅ T-006C / D-1 —— commit `76ee326a0`
   - ❌ T-006D-2A 跳转式 fork —— 技术通但产品方向错（手动验证：自动切走主对话 + 新 topic 模型像白纸），废弃，代码未 commit
   - ✅ **T-006D-2B Side-by-side branch S1'–S4' 实施完，commits `c278f5c0a` / `e49d1cc0f` / `3e2ecec41` 已入库；S4' 产品 go/no-go 闸门通过**（用户视觉验证：模型回复围绕 selectedText；prompt 通过 `topic.prompt` 系统提示槽位隐藏，用户在 panel 只看到自己输入的 followUp；侧边栏全程不出现 branch topic）
     - 设计源：[[preflight.md|T-006D-2_RealFork/preflight.md]]
     - 关键决策（来自 preflight）：W1 复用 `MessageGroup` 不复用 Messages.tsx / W2 `buildBranchSystemPrompt` 强制注入 / W3 `topic.prompt` 模式 A 隐藏 / W4 路径 Y 关闭即 DELETE（未实施，留 D-2C-2）
   - ✅ **D-008 closed**：分支 panel 内消息流无法滚动（高度链断裂） — 修 = `RowFlex h-full` + `BranchPane motion.div h-full` + `BranchMessageStream` 自滚 `overflow-y-auto`；删除 jsdom 跑通但实际无效的 false-green scroll 单测
   - ✅ **D-009 closed**：分支内 regenerate/edit/delete 模型瞎 — 修 = **Option 1 BranchAssistantContext** Provider 仅包 BranchPane 子树 + `resolveAssistantSource` 纯函数 strict-match + 4 用例回归测试 + 撤 watch#1/watch#3 调试日志。**2026-05-22 用户视觉验证通过**（regenerate 仍聚焦 selectedText、主聊天行为零变化）。代码未 commit — 用户自管 git
   - ✅ **Task 3 MAIN \| BRANCH resizable divider**：镜像 `useSidebarResize` 写右锚 `useBranchPaneResize`；BranchPane width state（默认 420，clamp [280, 700]）+ framer-motion 拖动 `transition.duration=0` 与 0.3s 开关动画解耦。用户 2026-05-22 视觉验证通过。代码未 commit
   - ✅ **S6' / T-006E source-passage highlight 已完成** —— 最终方案 = `<span class="branch-anchor-highlight">` 跨节点 wrap + `replaceWith` 全局清扫 + `globalThis` Context singleton + Chat 关闭路径显式调 clear。**Verification 通过**（2026-05-23）：baseline 0 → 开 A 5 → 切 B 3（不累积）→ 关闭 0 → 空 shell 0 → 反复 open/switch/close 5-6 轮 → 永远 close 回 0、永无 shell。**完整决策史 D-010→D-013-CLOSED**（颜色错 → block-级整段亮 → CSS Custom Highlight API 注册成功但零像素 → 切 `<span>` → cleanup 泄漏修 → 关闭路径连通）见 [[问题与Debug记录]] 与 [[tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/README|T-006E README]]。代码未 commit
   - ✅ **Branch Panel 关闭路径连通**（D-013-CLOSE-PATH）—— X 按钮在 compose 与 conversation 两态都启用；`onComposeCancel` 扩展为 `clearSourceHighlight() + setBranchAnchor(null) + setBranchTopic(null) + branchFork.reset()`
   - ⏳ **未做（剩余功能）**：分支折叠 / 多分支共存（同时打开多个 anchor，每个一种颜色或编号）—— **未开始**
   - ⏳ **已知 deferred**：
     - 关闭分支不删 SQLite topic 行 = orphan（path Y / T-006D-2C-5 cleanup）
     - 切 topic 离开再回来高亮不恢复（anchor 不持久化、未来 v1.1）
   - ⏳ T-006D-2C-0 流式 disable Ask/Open（与 D-2B 正交，可平行）
   - ⏳ T-006D-2C-1..5 cleanup（含 [[preflight.md|D-2C-5 分支侧 Dexie 0-rows]] + path Y / delete-on-close）/ 2D 主目标注入开关
   - ⏳ T-006A 测试数据（可选） / T-006D-3 / T-006F

### ✅ 验证通过后可以做

3. **Expand Branch UI 原型** —— 现在 baseline 通了，可基于真实 topic/message 数据驱动而不只是 mock
4. **找 `DeJeune/ai-service` 分支** —— 看 v2 `SiblingNavigator` / `MessageGroup` 是否能合
5. **跑端到端发消息流程** —— 验证 chat completion 路径（AI 调用本身可能要先配 provider，那不属于 baseline）

### 🔮 后续 baseline 同源候选（不阻塞，按需）

| 候选 | 性质 |
|---|---|
| `useAssistantPresets` 'default' warning | 同源 sentinel，纯 warning，不阻塞 |
| `message.modelSnapshot` 填充 | T-005B 让 modelId=NULL，长远应填 snapshot |
| 方案 C：renderer 启动同步真实 UniqueModelId | 终局解；做完后 T-004 + T-005B 的 boundary 清洗都可删 |
| Agent session 写路径迁 DataApi | 已知迁移残留 |

### ❌ 不要做

- v1 路径上修 bug（v1 是 throwaway）
- 给 user_model / assistant 表 seed 静态行（违反 v2 设计）
- 给 5 个 messageThunk 调用点逐个清洗（StreamingService 边界已兜底，重复劳动）
- patch migration on `migrations/sqlite-drizzle/*.sql`
- 新组件引入 antd / styled-components / HeroUI

## 已完成（按时间倒序）

| 日期 | 任务 | 入口 |
|---|---|---|
| 2026-06-03 | **P1-S2c — Master/Detail 重写修 B1 + 统一 composer 键盘修 B3**：丢弃 S2b-3 sticky-stacking；`BranchPane` 改 master(`branch-pane-master` 兄弟区,N 个 `BranchTab`)/detail(`branch-pane-detail` 唯一 body 滚动,`BranchDetail` 真实盒子)；拆删 `BranchCard`；新 `composerKeyboard.ts` 唯一 handler,初始+follow-up composer 共用 Enter→submit；丢弃 hover/auto-scroll(回退 Chat/index.css、删 useHighlightEmphasis)。focused 125/125 绿；B3 mutation 非空转;B1 视觉 = manual-smoke；保护文件 git diff 全空。**未 commit** | [[tasks/P1_MultiBranch/P1-S2c_MasterDetail/README\|P1-S2c]] + [开发日志 2026-06-03](./开发日志.md) |
| ~~2026-06-01~~ | ~~**P1-S2b-3 — 面板交互精修**~~（❌ 已废弃,sticky-stacking → B1 重叠,未入库,被 S2c 取代）| [[tasks/P1_MultiBranch/P1-S2b-3_Polish/README\|P1-S2b-3]] |
| 2026-06-01 | **P1-S2b-3 revised — 面板交互精修**：A 单滚动容器 + sticky-stacking（`display:contents` 卡 + header 递增 top + `BranchMessageStream` 去内部滚动 + stack 去 gap/padding）；B 共享 `isSendMessageKeyPressed` 加可选 `forceEnterToSend`（默认 false → 主输入不变），composer 覆盖为 Enter=发送；C 单态 `hoveredBranchId` 双向（`useHighlightEmphasis` + Chat 委托 + 卡 border-primary）；D 新分支 scrollIntoView。focused 131/131 绿；A+B mutation 非空转；IME 不发已验；主输入不受影响；守护文件 git diff 全空；lint 全过。11 src + 4 docs。**未 commit** | [[tasks/P1_MultiBranch/P1-S2b-3_Polish/README\|P1-S2b-3]] + [开发日志 2026-06-01 S2b-3](./开发日志.md) |
| 2026-06-01 | **P1-S2b-2 — multi-branch follow-up：conversation-state composer + per-card 路由**：新 `useBranchFollowUp` hook 复用 `sendMessage` thunk（无 POST /topics、不改 streaming 内部）；新 `BranchFollowUpComposer`（本地 draft 隔离）挂 conversation 态卡底；`BranchPane`+`onSendFollowUp(branchId,f)` 每卡闭包；`Chat.handleSendBranchFollowUp` 解析 branchId→topic。focused 94/94 绿；路由 mutation 双向（`→branches[0]` B-RED / `→branches[last]` A+B-RED，A 在 M1 GREEN 非 fake-green）；sourceHighlight.ts/BranchAnchorContext.tsx git diff 空；lint 全过。12 src + 5 docs。**已入库 commit `1c7f4ba20`** | [[tasks/P1_MultiBranch/P1-S2b-2_FollowupRouting/README\|P1-S2b-2]] + [开发日志 2026-06-01 S2b-2](./开发日志.md) |
| 2026-06-01 | **P1-S2b-1 — multi-branch UI：append + folder-tab card stack + color cycling + N>1 真路径**：openBranchAnchor 从 replace 改 APPEND；`pickNextColor` 给新分支选未用调色板键；BranchPane 重写为 N 张 BranchCard 纵向栈（彩色 tab + #N 徽章 + snippet + chevron + X + body）；每卡独立 collapse + 独立 close（targeted `clearSourceHighlight(branchId)`）。**sourceHighlight.ts 全程禁止永久改动**（mutation 临时改 + revert，`git diff` 空已验证）；BranchPane 保持在 BranchAnchorContext Provider 外（cross-contamination 防御）。focused 84/84 绿；**data-hl mutation 8 expected-red 命中 + 0 fake-green 残留**（首跑 6 RED + 2 个 attribute preserve-pattern vacuous-GREEN → follow-up 全仓 grep + 改绝对断言 → 重跑 8 RED）；lint 全过；i18n 加 `panel.card.toggle_collapse`。13 src + 5 doc 改动。**已入库 commit `5c7071a69`** | [[tasks/P1_MultiBranch/P1-S2b-1_MultiBranchUI/README\|P1-S2b-1]] + [开发日志 2026-06-01 S2b-1](./开发日志.md) |
| 2026-06-01 | **P1-S2a — sourceHighlight.ts 加 data-branch-id + per-branch color + targeted clear；UI 仍 ≤ 1 分支**：给 paint 加 `(branchId, colorKey)` 参数 + 每个 span stamp `data-branch-id` + `data-hl`；`clearSourceHighlight(branchId)` 改 attribute-selector targeted（不再全擦）；paint 内部 first-line clear 也按 id；6 色 CSS palette 注入；MainTextBlock effect / Chat.tsx close path 全改 targeted clear；多分支隔离能力由测试 fixture（双 branchId 直接构造）验证（4 新增 multi-branch 用例）。focused 40/40 绿；**mutation M1+M2 全部 expected-red 命中、零 fake-green**；lint 全过；fork / streaming / provider / Redux / Dexie 全未触；`__BRANCH_ANCHOR_CTX_CACHE__` 保留。9 个 src + 4 个 doc 改动。**已入库（commit `14edaa0f7`）** | [[tasks/P1_MultiBranch/P1-S2a_HighlightImpl/README\|P1-S2a]] + [开发日志 2026-06-01 S2a](./开发日志.md) |
| 2026-06-01 | **P1-S1 — branches[] state shape 推广（零行为变化）**：开 P1（多分支共存）三步法的 S1 = state foundation。把 `branchAnchor + branchTopic` 双 useState 推广成 `branches: Branch[]` + `collapsedBranchIds: Set<string>`；新建 `Branch` 类型（id / source / topic / createdAt）；BranchAnchorContext value 改 `{ anchors: BranchAnchorHighlight[] }`；MainTextBlock effect 改 filter+迭代；synthetic.topics 改 spread。**branches.length ≤ 1 invariant 保持**，所有派生值在 length ≤ 1 时与现网一字不差。`sourceHighlight.ts` git diff 空；`__BRANCH_ANCHOR_CTX_CACHE__` 模式保留；focused 36/36 绿；non-vacuous check 通过；`pnpm lint` 全过。6 个 src + 4 个 doc 改动。**未 commit** | [[tasks/P1_MultiBranch/README\|P1 概览]] + [[tasks/P1_MultiBranch/P1-S1_StateFoundation/README\|P1-S1]] + [开发日志 2026-06-01](./开发日志.md) |
| 2026-05-31 | **S6'/T-006E 测试 mutation 验证 + 修一个新被发现的假绿**：对 P0 那批新测试做 mutation 验证（paint / clear 分别临时 no-op）—— Mutation 1 paint 破坏时 8 RED 全符合预期；Mutation 2 clear 破坏时 sourceHighlight 3 RED 符合预期，**但 MainTextBlock 的 unmount cleanup 用例假绿**（testing-library `unmount()` 拆子树掩盖了 cleanup 是否真跑）。1-for-1 替换为 deps-change cleanup 用例（`rerender` 翻 Provider value、组件保持 mounted、span 只能由 effect cleanup 清掉）；新用例对 Mutation 2 立刻 RED ✓。`sourceHighlight.ts` 字节不变全程；`git diff` 空；focused 36/36 仍绿。**未 commit** | [开发日志#2026-05-31](./开发日志.md) + [tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/](./tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/) |
| 2026-05-26 | **S6'/T-006E 测试覆盖重写 + 删除 dangling `data-branch-anchored`**：原 MainTextBlock 4 个 highlight 用例只断言不参与渲染的 `data-branch-anchored`（无 CSS 规则、无 querySelector 消费者）—— 假绿，13 轮 D-013 debug 都没捕到任何高亮失效。grep 全仓确认无消费者后删除该属性；MainTextBlock 5 个用例改为断言 `span.branch-anchor-highlight` 真实注入 + 卸载清扫；sourceHighlight.test.ts 保留 9 个 round-trip 用例 + 删过时「browser-only」注释 + 新增 6 个 paint/clear DOM 突变链用例（单节点 span 文本对齐、跨节点 span 数 ≥ 2 + 拼接对齐、clear 后 spans=0 + textContent + childNodes 字节级复原、idempotent clear、A→B 不累积、5 轮循环不累积）；两份测试顶部 SCOPE 注释明确写「jsdom 无 layout/paint，绿不等于用户能看见」。`pnpm build:check` 666 file / 9448 tests passed。**已入库（commit `f3bf0dbf7`）** | [tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/](./tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/) |
| 2026-05-23 | **S6' / T-006E 高亮整线收官 + close path 连通 + cleanup 修 + 验证通过**：CSS Custom Highlight API 在本 Electron/Chromium + markdown DOM 环境零渲染 → 切到 `<span class="branch-anchor-highlight">` 跨节点 wrap；旧 `clearSourceHighlight` 用 `while/insertBefore/removeChild` 两步操作 race 出 shell + leftover → 改单原子 `replaceWith(...Array.from(span.childNodes))` + 全文 `document.querySelectorAll`；Branch Panel X 在 conversation 状态被 `disabled={!isComposing}` 锁死 → 删 disable + Chat.onComposeCancel 扩展为 `clearSourceHighlight + setBranchAnchor(null) + setBranchTopic(null) + branchFork.reset()`；BranchPane.test.tsx 反转 close-disabled-in-conversation 断言。Verification：baseline 0 → 开 A 5 → 切 B 3 → 关闭 0 → 空 shell 0 → 反复 5-6 轮永远 close 回 0、永无 shell。T-006E README 重写（淘汰 surroundContents/`<mark>`/Highlight-API-as-v1.1 旧计划）。**未 commit** | [tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/](./tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/) + [问题与Debug记录 D-013-CLOSED](./问题与Debug记录.md) |
| 2026-05-22 | **S6' D-013-FIX 根因落定**：trace 终判 —— 每一条都 `insideProvider:false`，连主对话块也不在 Provider 内。逐文件再核验全链路无 portal/root，树连续。根因 = `BranchAnchorContext.tsx` 非 Fast-Refresh-eligible（全部导出非组件）→ 本会话多次编辑致 Vite 反复重新执行该模块 → `createContext` 每次产生新对象 → Provider/consumer 持不同 context。修：context + default 走 `globalThis ??=` 单例化。typecheck ✅；用户重启 dev 验证。**注：冷启动后此修不能单独解释残留失效；最终修在 D-013-CLOSED 切 `<span>` wrap**。**未 commit** | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **S6' D-013 实跑静态搜索 + step-4 引用判别器**：grep 全仓 `BranchAnchorContext` —— 确认**单一 provider**（Chat.tsx:265 包 `<Messages>`），结构上源 block 必在内 →「读 null」基本是 trace 误读。误读来源 = `BranchMessageStream` 复用同一 `MainTextBlock` 而 BranchPane 在 Provider 外。加 `BRANCH_ANCHOR_DEFAULT` 导出 + `insideProvider`/`earlyReturn`/`writtenSelectionStart/End` 日志，下轮一次判 provider+offsets。**未 commit** | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **S6' D-013 trace 突破 + context-read 诊断**：trace 推翻「Stage 0 必 null」演绎 —— `findBlockContext`/`branchAnchorHighlight set` 都写有效 `d13a7253`，但 `effect fired` 全 `cf1ecb91`、选中的 `d13a7253` 从未 fire ⇒「setter 写、reader 读不到」。调查渲染链连续 / 无 portal / 无双 module → 静态找不到 break。本轮加 `MainTextBlock` context-read 诊断日志。等用户跑 dev 贴 context read + findBlockContext + Stage 0。**未 commit** | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **S6' 精确高亮 D-012 修复**：高亮不显示 → capture/rebuild 统一 `flattenTextNodes` 遍历 + `::highlight()` 颜色换 concrete `rgb(251 191 36 / 0.45)` + rAF 兜底。仍不显示 → D-013 | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **S6' 精确选区高亮 D-011 修复**：blockId 匹配仍亮整回复（一条回复=单 MAIN_TEXT block）→ 改 CSS Custom Highlight API 精确画选区 char range（`captureSelectionOffsets` + `resolveBranchHighlightRange` + `BranchAnchor.selectionStart/End`） | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **S6' 高亮 D-010 修复**：首版 `bg-accent/60` 白底不可见（≈3% 黑叠加）→ 换 DESIGN.md `warning-bg/base` 暖琥珀 | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **S6' source-passage highlight 首版实施**：新 `BranchAnchorContext` + `MainTextBlock` block 高亮 + Chat.tsx 包主 `<Messages>` 子树。**未 commit** | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **Task 3 MAIN \| BRANCH resizable divider — 视觉验证通过**：新 `useBranchPaneResize` hook（镜像 `useSidebarResize` 右锚版）+ `constants.ts`（DEFAULT 420 / MIN 280 / MAX 700）+ BranchPane 改 width state + 拖动时 framer-motion `transition.duration=0` + inner div `style={{ width }}` + 2 渲染测试用例。**未 commit** | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **D-009 closed**：用户视觉验证通过 — 分支 regenerate/edit/delete 仍聚焦 selectedText、主聊天零变化。Option 1 (BranchAssistantContext) 代码实施（含 `resolveAssistantSource` 4 用例回归测试 + 撤 watch#1/watch#3 调试日志）；Dexie 0-row 不一致登记为 [[preflight.md\|D-2C-5]] cleanup | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | **D-008 closed**：分支 panel 滚动修 — 根因 = `RowFlex` 没 `h-full`，高度链断在那一级；主聊天靠 `Main` inline `style.height` 强撑不依赖此链。修 = RowFlex + BranchPane motion.div 都加 h-full；删 jsdom false-green scroll 单测 | [问题与Debug记录.md](./问题与Debug记录.md) |
| 2026-05-22 | **T-006D-2B Side-by-side branch S1'–S4' 全部入库 + 用户视觉验证产品闸门通过**：commits `c278f5c0a` (S1' useBranchFork + buildBranchSystemPrompt) + `e49d1cc0f` (S2' state lift + S3' BranchPane/Composer) + `3e2ecec41` (S4' BranchMessageStream 复用 MessageGroup) | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/) |
| 2026-05-22 | T-006D-2A 跳转式 fork MVP — 手动验证暴露产品方向错（自动 setActiveTopic 切走 + 新 topic 模型像白纸）；**废弃，代码不 commit**；转 D-2B 重设计 | 同上 |
| 2026-05-21 | T-006D-2B preflight：四承重墙调查 + 架构修正 + S1'–S7' 计划 | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/preflight.md](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/preflight.md) |
| 2026-05-21 | **D-004 + D-005 closed** —— 用户 fresh install 实测 BeatLoader 消失 + 操作栏出现 + assistant wrapper 在 DOM + Ask/Open 可点；T-006 Text Anchor 渲染链路全通 | [问题与Debug记录.md](./问题与Debug记录.md) |
| 2026-05-21 | T-009 实施 D-005 修复（+24 行 src + 170 行测试）—— newMessage.ts:275 取消注释 SUCCESS 转换 + StreamingService.finalize 末尾追加防御性 dispatch；诊断初版误判"全链路不 dispatch"被纠正；自动化 74/74 全过 | [tasks/T-009_StreamingNotDispatchedToRedux/实施.md](./tasks/T-009_StreamingNotDispatchedToRedux/实施.md) |
| 2026-05-21 | T-009 诊断 D-004 + D-005（初版） —— 后续实施时纠正了根因 | [tasks/T-009_StreamingNotDispatchedToRedux/](./tasks/T-009_StreamingNotDispatchedToRedux/) |
| 2026-05-21 | **D-001 / D-002 / D-003 三个 issue 全部 closed** —— 用户 fresh install 同轮端到端验证（baseline + Ollama + Chat picker + gemma4:e4b 流式回复）全部通过 | [问题与Debug记录.md](./问题与Debug记录.md) |
| 2026-05-21 | T-008C 方案 B 实施 —— chat-model-popup 数据源切 v2 + CHERRYAI fallback 保留；下游 0 改动；6 焦点 + 98 回归测试全过 | [tasks/T-008C_ChatPickerV2Migration/](./tasks/T-008C_ChatPickerV2Migration/) |
| 2026-05-21 | T-008B 方案 B 最小实现可行性核实 —— 1 文件 ~50 行（复用 toV1ProviderShim/toV1ModelShim）；推荐直接做、跳过 A | [tasks/T-008_ChatPickerV1V2Gap/方案B评估.md](./tasks/T-008_ChatPickerV1V2Gap/方案B评估.md) |
| 2026-05-21 | T-008 D-003C 诊断 Chat 弹窗 v1/v2 数据断层 —— 根因 = picker 读 v1 Redux + v2 sync 不写 v1；给方案 A/B | [tasks/T-008_ChatPickerV1V2Gap/诊断.md](./tasks/T-008_ChatPickerV1V2Gap/诊断.md) |
| 2026-05-21 | T-007 D-003B Ollama Provider 修复实施 + 用户手测 Provider 设置页 ✅；Chat 弹窗失败 → T-008 | [tasks/T-007_OllamaProviderFix/验证.md](./tasks/T-007_OllamaProviderFix/验证.md) |
| 2026-05-21 | T-007 D-003A Ollama 自动模型同步失败诊断 —— 根因 = providers.json 漏 `defaultChatEndpoint` | [tasks/T-007_OllamaProviderFix/诊断.md](./tasks/T-007_OllamaProviderFix/诊断.md) |
| 2026-05-21 | T-006C 收口补丁：data-message-role 注入 + 仅 assistant 启用 Ask/Branch（staged 未 commit） | [tasks/T-006_TextAnchorBranchUI/T-006C_Menu/](./tasks/T-006_TextAnchorBranchUI/T-006C_Menu/) |
| 2026-05-20 深夜 | T-006D-1 BranchPanel 壳子原型（Dialog + state 串联 + 占位 handler + 6 测试，staged 未 commit） | [tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-1_Shell/](./tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-1_Shell/) |
| 2026-05-20 深夜 | T-006C 扩展 SelectionContextMenu（接 findBlockContext + 2 个菜单项 + i18n + 8 个测试，staged 未 commit） | [tasks/T-006_TextAnchorBranchUI/T-006C_Menu/](./tasks/T-006_TextAnchorBranchUI/T-006C_Menu/) |
| 2026-05-20 深夜 | T-006B 文本选中捕获（findBlockContext + DOM data 属性）— ✅ commit `d579fdcf2` | [tasks/T-006_TextAnchorBranchUI/T-006B_TextSelection/](./tasks/T-006_TextAnchorBranchUI/T-006B_TextSelection/) |
| 2026-05-20 深夜 | T-006 Text Anchor Branch UI 设计 + 子任务拆分 | [tasks/T-006_TextAnchorBranchUI/](./tasks/T-006_TextAnchorBranchUI/) |
| 2026-05-20 深夜 | T-005B 修 assistant message modelId FK | [tasks/T-005B_修复ModelIdFK/](./tasks/T-005B_修复ModelIdFK/) |
| 2026-05-20 深夜 | T-005A assistant message FK 诊断 | [tasks/T-005A_AssistantMessageFK/](./tasks/T-005A_AssistantMessageFK/) |
| 2026-05-20 晚 | T-004 修 default assistant sentinel | [tasks/T-004_修复DefaultAssistantSentinel/](./tasks/T-004_修复DefaultAssistantSentinel/) |
| 2026-05-20 晚 | T-003 baseline 诊断 | [tasks/T-003_BaselineDebug/](./tasks/T-003_BaselineDebug/) |
| 2026-05-20 下午 | 源码地图增补（分支对话/服务算法/事件IPC） | [../02_Architecture/](../02_Architecture/) |
| 2026-05-20 上午 | T-002 DataApi 端点 | [tasks/T-002_DataApi端点/](./tasks/T-002_DataApi端点/) |
| 2026-05-19 | T-001 源码地图初版 | [tasks/T-001_源码地图/](./tasks/T-001_源码地图/) |

## 阶段定位

- **Phase 1** 源码地图 ✅
- **Phase 2** Baseline Debug ✅ commit `15ad2eb08` + 端到端验证 ✅（2026-05-21；D-001 / D-002 closed）
- **Phase 2.5** Ollama provider 链路 ✅（T-007 + T-008C；D-003 closed 2026-05-21）—— Chat 里能用 Ollama 测真实回复
- **Phase 2.6** 消息渲染链路 ✅（T-009；D-004 + D-005 closed 2026-05-21）—— BeatLoader 正常退、操作栏正常出、assistant 文本 Ask/Open 可点
- **Phase 3** Expand Branch UI 原型 — 🧠 设计 ✅；🔧 T-006B / T-006C / D-1 已 commit；**T-006D-2A 废弃（产品方向错）**；**T-006D-2B Side-by-side S1'–S4' 已 commit 入库 + 产品闸门通过**；scroll 修完（D-008 closed）；regenerate Option 1 实施完视觉验证通过（D-009）；resizable divider 视觉验证通过（Task 3）；**S6' / T-006E source-passage highlight 视觉验证 + cleanup 计数验证通过 + 2026-05-31 测试 mutation 验证通过**；T-006A 测试数据可选
- **Phase 4 / P1** Multi-Branch 共存 — 🧠 拆为 S1 state foundation / S2a highlight 实现 / S2b-1 multi-branch UI 基础 / S2b-2 精修 / S3 disposition；**S1 入库 `f915938ec`**；**S2a 入库 `14edaa0f7`**；**S2b-1 入库 `5c7071a69`**；**S2b-2 follow-up 路由 入库 `1c7f4ba20`**；~~S2b-3 sticky-stacking~~（❌ 废弃,B1 重叠,未入库）→ **S2c Master/Detail 重写 已实施（2026-06-03）**：`BranchPane` 改 master(`BranchTab` 兄弟区)/detail(`BranchDetail` 真实盒子) 修 B1 + 统一共享 composer 键盘 handler 修 B3,纯前端、不碰保护文件,focused 125/125 绿、B3 mutation 非空转、B1 视觉 = manual-smoke,未 commit；**剩余 = S2d（hover 联动 / auto-scroll / folder-tab 视觉 / 每卡 loading / B4 菜单兜底）+ B5 关闭中止流式（abort，需批准）+ S3 disposition**

## 一句话

D-2B side-by-side commits `c278f5c0a` + `e49d1cc0f` + `3e2ecec41` 入库；**D-008 scroll + D-009 regenerate + Task 3 resizable divider + S6'/T-006E source-passage highlight 四项视觉 + cleanup 验证全通过**。S6' 最终方案 = `<span class="branch-anchor-highlight">` 跨节点 wrap + `replaceWith` 全局清扫 + `globalThis` Context singleton + Chat 关闭路径显式 clear。**2026-05-26**：S6'/T-006E paint/clear 链获得真实测试覆盖（替换原 `data-branch-anchored` 假绿用例），dangling 属性已删 —— **已入库 commit `f3bf0dbf7`**。**2026-05-31**：对该批新测试做 mutation/red-test 验证，捕到 1 个新假绿（MainTextBlock unmount cleanup 用例被 testing-library `unmount()` 拆子树掩盖），1-for-1 替换为 `rerender` 翻 Provider value 的 deps-change cleanup 用例，新用例对 Mutation 2 立刻 RED；sourceHighlight.ts 字节不变；focused 36/36 绿（已 commit）。**2026-06-01**：P1 开工 —— S1 state shape 推广 `branches[]` 数组（commit `f915938ec`）→ S2a sourceHighlight 加 stamping + targeted clear + 6 色 palette（commit `14edaa0f7`）→ **S2b-1 multi-branch UI 真路径**（append + folder-tab card stack + color cycling，sourceHighlight.ts 字节不变，focused 84/84 绿、mutation 6 expected-red + 1 subtle fake-green 显式 flag，**未 commit**）。完整决策史 D-010 → D-013-CLOSED 见 [[问题与Debug记录]] + [[tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/README|T-006E README]]；P1 计划见 [[tasks/P1_MultiBranch/README|P1 概览]]。**剩余功能未做** = P1-S2b-2（hover / auto-scroll / folder-tab 视觉 / follow-up composer）+ P1-S3 disposition。**已知 deferred** = ① 关闭分支不删 SQLite topic 行（path Y / T-006D-2C-5 cleanup）② 切 topic 离开再回来高亮不恢复（anchor 不持久化、未来 v1.1）。
