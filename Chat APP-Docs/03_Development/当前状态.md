# 当前状态

> 更新于 2026-05-20 深夜（T-005B 自动化校验后）。

## 当前阻塞

✅ **无（baseline 二连修已 commit `15ad2eb08`；仍待手动端到端验证）**。

| 任务 | 代码 | commit | 手动验证 |
|---|---|---|---|
| T-004（D-001 / topic 创建） | ✅ | ✅ `15ad2eb08` | ⏳ |
| T-005B（D-002 / assistant message 写入） | ✅ | ✅ `15ad2eb08` | ⏳ |

> 注：用户把 T-004 + T-005B 合并为单 commit 入库（commit msg 用了 T-005B 的命名）。手动验证 = `rm -rf ~/Library/Application Support/CherryStudioDev && pnpm dev` 后实际跑一次发消息看是否不再报 FK。

## 🟡 已知 non-blocking 问题

| ID | 标题 | 影响 | 详情 |
|---|---|---|---|
| **D-003** | Ollama Provider 自动模型同步失败 | 阻塞用 Ollama 测真实回复；**不**阻塞 Phase 3 UI 原型；不阻塞 baseline 验证 | [问题与Debug记录.md#D-003 详细记录](./问题与Debug记录.md) |

**临时绕路**：要测真实 AI 回复，用别的 provider（OpenAI / Anthropic 只要 API key 通）。

## 现在可以做的事

### ✅ 推荐路径

1. **手动验证**：
   ```bash
   rm -rf ~/Library/Application\ Support/CherryStudioDev
   pnpm dev
   # 新建 topic ✅（T-004）
   # 输入消息 + 发送 → assistant message 行能 INSERT，无 FK 错（T-005B）
   ```
2. **commit baseline 套**（两个独立 commit，scope 清晰）：
   ```
   fix(assistant): map v1 'default' sentinel to null in topic DTO  (T-004, D-001)
   fix(message):   drop non-UniqueModelId in assistant message DTO (T-005B, D-002)
   ```
   均 `--signoff` per CLAUDE.md。

### ✅ 验证通过后可以做

3. **Expand Branch UI 原型** —— 现在 baseline 通了，可基于真实 topic/message 数据驱动而不只是 mock
4. **找 `DeJeune/ai-service` 分支** —— 看 v2 `SiblingNavigator` / `MessageGroup` 是否能合
5. **跑端到端发消息流程** —— 验证 chat completion 路径（AI 调用本身可能要先配 provider，那不属于 baseline）

### 🔮 后续 baseline 同源候选（不阻塞，按需）

| 候选 | 性质 |
|---|---|
| `useAssistantPresets` 'default' warning | 同源 sentinel，纯 warning，不阻塞 |
| `message.modelSnapshot` 填充 | T-005B 让 modelId=NULL，长远应填 snapshot |
| 方案 C：renderer 启动同步真实 UniqueModelId | 终局解；做完后 T-004 + T-005B 的 boundary 清洗都可删 |
| Agent session 写路径迁 DataApi | 已知迁移残留 |

### ❌ 不要做

- v1 路径上修 bug（v1 是 throwaway）
- 给 user_model / assistant 表 seed 静态行（违反 v2 设计）
- 给 5 个 messageThunk 调用点逐个清洗（StreamingService 边界已兜底，重复劳动）
- patch migration on `migrations/sqlite-drizzle/*.sql`
- 新组件引入 antd / styled-components / HeroUI

## 已完成（按时间倒序）

| 日期 | 任务 | 入口 |
|---|---|---|
| 2026-05-20 深夜 | T-005B 修 assistant message modelId FK | [tasks/T-005B_修复ModelIdFK/](./tasks/T-005B_修复ModelIdFK/) |
| 2026-05-20 深夜 | T-005A assistant message FK 诊断 | [tasks/T-005A_AssistantMessageFK/](./tasks/T-005A_AssistantMessageFK/) |
| 2026-05-20 晚 | T-004 修 default assistant sentinel | [tasks/T-004_修复DefaultAssistantSentinel/](./tasks/T-004_修复DefaultAssistantSentinel/) |
| 2026-05-20 晚 | T-003 baseline 诊断 | [tasks/T-003_BaselineDebug/](./tasks/T-003_BaselineDebug/) |
| 2026-05-20 下午 | 源码地图增补（分支对话/服务算法/事件IPC） | [../02_Architecture/](../02_Architecture/) |
| 2026-05-20 上午 | T-002 DataApi 端点 | [tasks/T-002_DataApi端点/](./tasks/T-002_DataApi端点/) |
| 2026-05-19 | T-001 源码地图初版 | [tasks/T-001_源码地图/](./tasks/T-001_源码地图/) |

## 阶段定位

- **Phase 1** 源码地图 ✅
- **Phase 2** Baseline Debug ✅（代码 + 自动化都过；剩下用户端到端验证）
- **Phase 3** Expand Branch UI 原型 ⏳ 验证通过后即可启动

## 一句话

**baseline 两层 FK 都修好了**（topic 创建 + assistant message 创建）；你跑一遍 `rm + pnpm dev + 发消息`，OK 就 commit 两条 `fix(...)` + `--signoff`，然后开 Phase 3。
