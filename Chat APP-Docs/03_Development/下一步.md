# 下一步

> 更新于 2026-07-01（**P2 Step 2D-3 implemented — immediate persisted highlight continuity**）：P2 Step 2D-3 已完成。**当前唯一下一步 = P2 Step 2D-3 review + manual smoke**。
>
> 已落地：`POST /branch-anchors` 成功返回的 row 会被同会话 locally promoted 进 persisted highlight projection；关闭 kept branch card 后 live highlight 正常 cleanup，但 promoted persisted candidate 继续 repaint source highlight。topic revisit/readback 仍通过 server GET rows 支持。
>
> 边界保持：没有 click-to-reopen；没有 BranchPane auto-open；`persisted:<anchorId>` highlight click 仍 no-op；没有 summary generation；没有 backend schema/migration 改动；`sourceHighlight.ts` 保持 untouched。
>
> 更新于 2026-07-01（**P2 Step 2D-1 implemented — persisted anchor resolver/helper only**）：Step 2D-1 已完成；新增纯 DOM resolver `resolvePersistedBranchAnchorRange(blockEl, anchor)`，可用 rendered `textContent` 验证 persisted offsets，并在 offset 失效时用唯一 `selectedText` fallback。**当前唯一下一步 = P2 Step 2D-2 projection + paint integration**。
>
> Step 2D-1 没有 paint highlight、没有调用 `paintSourceHighlight` / `clearSourceHighlight`、没有传 persisted anchors 到 `BranchAnchorContext`、没有 click-to-reopen、没有 summary generation。它只提供后续 paint 前的安全 range 判定：hydrated 返回 `resolvedSelectionStart/resolvedSelectionEnd`，skipped 返回 `skippedReason`。
>
> 更新于 2026-07-01（**P2 Step 2C implemented — persisted branch_anchor read path**）：P2 Step 2C 已实现；persisted `branch_anchor` rows 现在可在 renderer 读取并归一化为 read-only anchor state。**当前唯一下一步 = P2 Step 2D persisted highlight hydration / repaint**。
>
> Step 2C 已落地 topic-scoped `usePersistedBranchAnchors(parentTopicId)`，用现有 typed DataApi `useQuery` pattern 读取 `GET /topics/:id/branch-anchors`，并设置 `keepPreviousData: false`。persisted anchor read state 没有放入 `BranchPane` live card state，也没有传入 `BranchAnchorContext`；`BranchAnchorContext` 在 Step 2D 前继续只承载当前打开分支的 paintable highlight projections。Step 2C **不是 highlight restoration / highlight painting、不是 click-to-reopen、不是 summary generation**。
>
> UX note：`Open as branch` disabled 可由选区端点不在有效 assistant `MainTextBlock` / `data-block-id` 边界内触发；这是 selection anchor resolution 行为，不是 active branch count limit。manual smoke 应选择 assistant 回复内部 substring，不要选 line boundary / newline。
>
> 更新于 2026-06-29（**P2 Phase B Step 1 后端持久化层已完成**）。P1 多分支 UX 已完成且不再是 active stage。P2 资产化设计 LOCKED：**无 topic metadata、独立 `branch_anchor` 表、write-on-keep、pending branches 不持久化、`summary` nullable 存在 `branch_anchor`、`disposition` 存储但非权威**。Step 1 已落地 `branch_anchor` schema / generated migrations / shared DTO/types/API schema / DataApi handlers / service / real DB integration tests / gates green；`branchTopicId` 唯一约束防止同一个 kept branch topic 重复 persisted anchor；`messageId` 有意无 FK，source message deletion / `blockId` drift / offset mismatch 留给后续 hydration degradation。**当时下一步 = P2 Step 2A：renderer write-on-keep API hookup**。Step 2A 不是 hydration，不是 click-to-reopen，不是 summary generation。
>
> 更新于 2026-06-29（**P2 Step 2A Phase 1 已完成**）：生命周期接点和一次性 guard 已就位；当前下一步收窄为 **Phase 2 persistence wiring**，即在首次满足 `shouldWriteBranchAnchorOnce` 时调用现有 DataApi `POST /branch-anchors`。仍不做 hydration、click-to-reopen、summary generation。
>
> 更新于 2026-06-29（**P2 Step 2A Phase 2 已完成**）：`POST /branch-anchors` 已接入 renderer write-on-keep 路径；当前下一步收窄为 **Phase 3 race hardening**，确认 keep/fork 顺序和重复点击都只会产生一次写入候选/一次 POST。
>
> 更新于 2026-06-29（**P2 Step 2A Phase 3 已完成**）：race hardening 已落地，写入由单一 `branches` post-commit effect 扫描，去重 key 对齐 `branchTopicId`。当前下一步为 **Phase 4 payload validation**。
>
> 更新于 2026-06-29（**P2 Step 2A Phase 4 已完成**）：create body 已用 shared `CreateBranchAnchorSchema` 做 runtime validation；当前下一步为 **Phase 5 test coverage**。
>
> 更新于 2026-06-29（**P2 Step 2A Phase 5 已完成 / Step 2A complete**）：renderer write-on-keep API hookup 已完成并有测试覆盖；当时下一步是 review / manual smoke。不要自动进入 hydration、click-to-reopen、summary generation。
>
> 更新于 2026-07-01（**P2 Step 2A.1 unkeep cleanup 已完成**）：Step 2A acceptance blocker 已修复：`kept -> pending` 会用本会话 POST 返回的 `anchor.id` 调 `DELETE /branch-anchors/:id`，DELETE 成功后清本地 lifecycle，允许同一 `branchTopicId` 后续再次 Keep 创建新 anchor。POST failure 不再永久 suppress retry；DELETE failure 保留 anchor id 供后续 cleanup retry。当前仍不做 hydration、click-to-reopen、summary generation。

> **SIGSEGV 调查结论**（2026-06-01）：把 S2a 全部 stash 走 → clean HEAD `f915938ec` 跑 `pnpm build:check` —— vitest main 进程跑完所有测试后仍然段错误 SIGSEGV 11，跟带 S2a 改动时一模一样。**确认环境闪问题**，不是 S1 / S2a / S2b-1 代码。从此后续 build:check 在 main 进程 vitest shutdown 阶段段错误归类为已知环境问题、不阻塞 renderer 改动 commit。Renderer focused 测试稳定全绿，typecheck/lint/format/i18n 全过。

## 当前唯一下一步

1. **P2 Step 2D-3 review + manual smoke**。
   - 验证 Keep POST 成功后关闭 kept branch card，主线 source highlight 仍可见。
   - 验证 live card 打开期间同 `branchTopicId` promoted persisted candidate 被过滤，不 double paint。
   - 验证 unkeep DELETE success 后 promoted highlight 移除；DELETE failure 时不提前移除。
   - 验证 active topic change 不泄漏 promoted anchors。
   - 验证没有 BranchPane auto-open。
   - 验证点击 `persisted:<anchorId>` highlight 不重开 branch card。
   - 下一产品步骤仍是 **Step 3 click-to-reopen kept branch topic**。

## CHECKPOINT: P2_STEP_2D_3_IMMEDIATE_PERSISTED_HIGHLIGHT_CONTINUITY

- Status
  - P2 Step 2D-3 implemented。
- Implemented behavior
  - `useBranchAnchorPersistence` 成功 create/delete 后通知 `Chat.tsx`。
  - `Chat.tsx` owns same-session promoted persisted anchors for current parent topic。
  - server GET rows 与 promoted rows 合并后进入 existing persisted projection。
  - close kept branch card 后 live span cleanup 仍执行，但 promoted persisted highlight 继续 paint。
  - POST failure 不 promote；DELETE success remove/suppress；DELETE failure 不 remove。
- Verification
  - `pnpm typecheck:web` 通过。
  - Focused renderer specs 8 files / 78 tests 通过。
  - `sourceHighlight.ts`、`SelectionContextMenu.tsx`、`newMessage.ts`、`src/main`、`migrations`、`packages/shared` 无 diff。
- Next step
  - Review + manual smoke；Step 3 仍是 click-to-reopen。

## CHECKPOINT: P2_STEP_2D_2B_PERSISTED_HIGHLIGHT_PAINT_INTEGRATION

- Status
  - P2 Step 2D-2B implemented。
- Implemented behavior
  - `BranchAnchorContext` 保留 live `anchors`，新增 `persistedAnchors` candidates。
  - `Chat.tsx` 从 persisted read state 投影 candidates，不写入 `BranchPane` state。
  - Projection 过滤 live branch topic 已覆盖的 persisted `branchTopicId`。
  - `MainTextBlock` 使用 `resolvePersistedBranchAnchorRange` hydrate rendered DOM range，hydrated 后 repaint persisted source highlight。
  - persisted ids 使用 `persisted:${anchor.id}`。
  - `useHighlightCardLink` 对 persisted ids no-op，Step 2D-2B 不实现 click-to-reopen。
- Verification
  - `pnpm typecheck:web` 通过。
  - Focused renderer specs 7 files / 71 tests 通过。
  - `sourceHighlight.ts`、`SelectionContextMenu.tsx`、`newMessage.ts`、`src/main`、`migrations`、`packages/shared` 无 diff。
- Next step
  - Review + manual smoke；如有具体风险再做 Step 2D-3 hardening。

## CHECKPOINT: P2_STEP_2D_2A_PROJECTION_PAINT_INTEGRATION_RECON_ACCEPTED

- Status
  - P2 Step 2D-2A recon accepted。
- Accepted architecture
  - Step 2D-2B 使用 narrowed Option B。
  - `BranchAnchorContext` 窄幅扩展：保留 live paintable anchors，新增 persisted anchor candidates。
  - `MainTextBlock` / `MainTextBlock` hook 在 DOM ref 可用时 resolve persisted candidates。
  - `resolvePersistedBranchAnchorRange` 不应在 Chat-level DOM query 中调用。
  - `sourceHighlight.ts` 不改，只用 `paintSourceHighlight` / `clearSourceHighlight`。
- Implementation constraints
  - 不把 persisted anchors 放进 `BranchPane` live card state。
  - persisted highlight paint 不自动打开 BranchPane cards。
  - `branchTopicId` 已有 live branch topic 时过滤 persisted row。
  - persisted DOM id deterministic，例如 `persisted:${anchor.id}`。
  - persisted colors 使用 BranchPanel color constants。
  - missing rendered block = `pending_render` / no-op。
  - 不过度建设 hydrationStatus/skippedReason state。
- Next step
  - P2 Step 2D-2B projection + paint integration implementation。

## CHECKPOINT: P2_STEP_2D_1_PERSISTED_ANCHOR_RESOLVER

- Status
  - Step 2D-1 resolver/helper 已完成。
- Changes
  - 新增 `src/renderer/src/utils/branchAnchor/persistedAnchorResolver.ts`。
  - API：`resolvePersistedBranchAnchorRange(blockEl, anchor)`。
  - Offset validation 使用 rendered `textContent`：`selectionStart >= 0`、`selectionEnd > selectionStart`、`selectionEnd <= textContent.length`、`textContent.slice(start,end) === selectedText`。
  - Offset validation 成功则 hydrate 原始 offsets。
  - Offset validation 失败则尝试唯一 `selectedText` fallback；多次出现 skipped `selected_text_ambiguous`，找不到 skipped `selected_text_not_found`，空文本 skipped `invalid_selected_text`，offset 越界且无 fallback skipped `offset_out_of_range`。
- Tests
  - 新增 `src/renderer/src/utils/branchAnchor/__tests__/persistedAnchorResolver.test.ts`，覆盖 valid offsets、out-of-range fallback、text mismatch fallback、ambiguous、not found、empty selectedText、invalid offsets no fallback、CJK/Unicode JS string indexing、`innerHTML` 不变。
- Boundary
  - 没有 highlight painting / DOM mutation。
  - 没有传 persisted anchors 到 `BranchAnchorContext`。
  - 没有 click-to-reopen、summary generation、backend schema/migration 改动。
- Next step
  - Step 2D-2 projection + paint integration。

## CHECKPOINT: P2_STEP_2A_PHASE_1

- Changes
  - `toggleKeepBranch` 与 fork `onCreated` 两个触发点已接入一次性候选判断。
  - 新增 `canWriteBranchAnchor` / `shouldWriteBranchAnchorOnce`，只在 kept 且 `branch.topic.id` 存在时允许首个写入候选。
  - Phase 1 未接 DataApi，未调用 `/branch-anchors`。
- Files modified
  - `src/renderer/src/pages/home/Chat.tsx`
  - `src/renderer/src/pages/home/Messages/BranchPanel/branchAnchorWrite.ts`
  - `Chat APP-Docs/03_Development/当前状态.md`
  - `Chat APP-Docs/03_Development/下一步.md`
  - `Chat APP-Docs/03_Development/开发日志.md`
- Working state
  - `pnpm typecheck:web` 通过。
  - 下一步清晰限定为 Phase 2 POST wiring。
- Risks
  - 目前仅 console placeholder，没有真实持久化。
  - 后续要确保 POST 失败不破坏 P1 close/keep 行为。
- Next step
  - Phase 2：引入 `useMutation('POST', '/branch-anchors')`，只在 write-once guard 首次命中时发起持久化。

## CHECKPOINT: P2_STEP_2A_PHASE_2

- Changes
  - 已用现有 DataApi hook 接入 `useMutation('POST', '/branch-anchors')`。
  - 写入入口集中在 `persistBranchAnchorIfReady`，且先过 `shouldWriteBranchAnchorOnce`。
  - Create payload 由 `buildCreateBranchAnchorBody` 统一构造。
- Files modified
  - `src/renderer/src/pages/home/Chat.tsx`
  - `src/renderer/src/pages/home/Messages/BranchPanel/branchAnchorWrite.ts`
  - `Chat APP-Docs/03_Development/当前状态.md`
  - `Chat APP-Docs/03_Development/下一步.md`
  - `Chat APP-Docs/03_Development/开发日志.md`
- Working state
  - `pnpm typecheck:web` 通过。
  - 没有 backend / schema / migration 改动。
- Risks
  - Phase 3 前仍需系统性确认 race 路径和重复点击路径。
  - Phase 4 前 payload 只靠 TypeScript 形状，尚未 runtime parse。
- Next step
  - Phase 3：把 race/duplicate 语义固化，并补充必要的内部状态边界。

## CHECKPOINT: P2_STEP_2A_PHASE_3

- Changes
  - POST 副作用从 `setBranches` updater 内移出，避免 updater 重放/重复执行导致副作用不透明。
  - 单一 `useEffect([branches])` 扫描已提交分支状态；keep-before-fork 与 fork-before-keep 都通过同一状态收敛。
  - renderer 去重 key 改为 `branch.topic.id`，与 `branch_anchor.branchTopicId` unique 约束一致。
- Files modified
  - `src/renderer/src/pages/home/Chat.tsx`
  - `src/renderer/src/pages/home/Messages/BranchPanel/branchAnchorWrite.ts`
  - `Chat APP-Docs/03_Development/当前状态.md`
  - `Chat APP-Docs/03_Development/下一步.md`
  - `Chat APP-Docs/03_Development/开发日志.md`
- Working state
  - `pnpm typecheck:web` 通过。
  - `toggleKeepBranch` / `onCreated` 只做 state transition；写入入口集中。
- Risks
  - POST 失败不重试是当前刻意取舍，避免 duplicate writes。
  - schema validation 尚未接入。
- Next step
  - Phase 4：接入 `CreateBranchAnchorSchema` parse，保证 parent/branch/topic/source/offsets 全字段合法。

## CHECKPOINT: P2_STEP_2A_PHASE_4

- Changes
  - Create payload 由 `CreateBranchAnchorSchema.safeParse` 校验后返回。
  - POST 顺序调整为先验证 body，再执行 write-once claim，最后调用 DataApi。
  - 无效 payload 不进入 `/branch-anchors`。
- Files modified
  - `src/renderer/src/pages/home/Chat.tsx`
  - `src/renderer/src/pages/home/Messages/BranchPanel/branchAnchorWrite.ts`
  - `Chat APP-Docs/03_Development/当前状态.md`
  - `Chat APP-Docs/03_Development/下一步.md`
  - `Chat APP-Docs/03_Development/开发日志.md`
- Working state
  - `pnpm typecheck:web` 通过。
  - 仍无 backend/schema/migration 改动。
- Risks
  - 测试尚未覆盖新 helper；Phase 5 必须补上。
  - 无效 payload 当前无用户提示，符合 Step 2A 不改 UI 的边界。
- Next step
  - Phase 5：补测试并完成最终审计。

## CHECKPOINT: P2_STEP_2A_PHASE_5

- Changes
  - 新增 `useBranchAnchorPersistence` hook，将 persistence 从 `Chat.tsx` 内联逻辑抽出。
  - 新增 `branchAnchorWrite.test.ts` 与 `useBranchAnchorPersistence.test.tsx`。
  - 覆盖 write-once、payload validation、keep/fork ordering、pending discard、API failure catch。
- Files modified
  - `src/renderer/src/pages/home/Chat.tsx`
  - `src/renderer/src/pages/home/Messages/BranchPanel/branchAnchorWrite.ts`
  - `src/renderer/src/pages/home/Messages/BranchPanel/useBranchAnchorPersistence.ts`
  - `src/renderer/src/pages/home/Messages/BranchPanel/__tests__/branchAnchorWrite.test.ts`
  - `src/renderer/src/pages/home/Messages/BranchPanel/__tests__/useBranchAnchorPersistence.test.tsx`
  - `Chat APP-Docs/03_Development/当前状态.md`
  - `Chat APP-Docs/03_Development/下一步.md`
  - `Chat APP-Docs/03_Development/开发日志.md`
- Working state
  - `pnpm typecheck:web` 通过。
  - `pnpm test:renderer -- ...` 实际跑完整 renderer 项目，347 files / 4393 tests 全过。
  - 无 backend/schema/migration 改动。
- Risks
  - 自动化测试 mock renderer DataApi hook；真实 IPC/DB 写入建议 manual smoke。
  - POST failure 不重试，后续若需要 retry/repair 应单独设计，不能破坏 write-once。
- Next step
  - Step 2A 完成；等待 review / manual smoke。

## CHECKPOINT: P2_STEP_2A_1_UNKEEP_CLEANUP

- Changes
  - `branchAnchorWrite.ts` 以 `branchTopicId` keyed lifecycle Map 取代 simple Set guard，状态为 `creating` / `created` / `deleting`。
  - `useBranchAnchorPersistence` 保存 POST 返回的 `anchor.id`，并在 `kept -> pending` 时调用 `DELETE /branch-anchors/:id`。
  - DELETE 成功后清本地 persistence state，同一 `branchTopicId` 之后可再次 Keep 并创建新 anchor。
  - POST failure 会清 create guard，后续 kept render 可 retry；DELETE failure 保留 anchor id，后续 pending lifecycle 可 retry cleanup。
  - POST in-flight 期间 unkeep 时，POST 成功后立即按最新 pending 状态 DELETE 新建 anchor。
- Files modified
  - `src/renderer/src/pages/home/Messages/BranchPanel/branchAnchorWrite.ts`
  - `src/renderer/src/pages/home/Messages/BranchPanel/useBranchAnchorPersistence.ts`
  - `src/renderer/src/pages/home/Messages/BranchPanel/__tests__/useBranchAnchorPersistence.test.tsx`
  - `Chat APP-Docs/03_Development/当前状态.md`
  - `Chat APP-Docs/03_Development/下一步.md`
  - `Chat APP-Docs/03_Development/开发日志.md`
  - `Chat APP-Docs/03_Development/P2_资产化设计决策.md`
- Working state
  - `pnpm typecheck:web` 通过。
  - Focused renderer specs 通过：`branchAnchorWrite.test.ts` + `useBranchAnchorPersistence.test.tsx`，2 files / 17 tests。
  - `pnpm test:renderer -- ...` 在本仓会误跑 renderer 全量；本次运行中遇到 unrelated timeout/failure 后中止，未作为本 checkpoint gate。
- Risks
  - 只 cleanup 本 renderer session 内已创建并已知 `anchor.id` 的 rows；刷新/重启后的 hydration/readback 不在 Step 2A.1。
  - Hydration、click-to-reopen、summary generation 仍未实现。
- Next step
  - Manual smoke 已通过；P2 Step 2B read-only hydration reconnaissance、Step 2C read path 均已完成。当前下一步是 P2 Step 2D persisted highlight hydration / repaint。

## CHECKPOINT: P2_STEP_2A_MANUAL_SMOKE_ACCEPTED

- Status
  - P2 Step 2A accepted。
  - 已完成 `branch_anchor` write-on-keep persistence with unkeep cleanup。
  - 真实 renderer → main/DataApi → SQLite create/delete path 已验证。
- Smoke results
  - KEEP_WRITE：PASS，write-on-keep exactly 1 final row。
  - UNKEEP_DELETE：PASS，unkeep 通过 `DELETE /branch-anchors/:id` 删除 created anchor。
  - KEEP_AGAIN：PASS，keep → unkeep → keep again exactly 1 final row，无 duplicate `branchTopicId`。
  - PENDING_DISCARD：PASS，pending fork topic 创建后关闭 pending branch 通过 `DELETE /topics/:id` 删除 fork topic；没有 `POST /branch-anchors`；final row count = 0。
  - Global `branchTopicId` duplicate count：0。
- UX note
  - `Open as branch` disabled 可由 selection endpoints 越出有效 assistant `MainTextBlock` / `data-block-id` 边界触发。
  - 这是 selection anchor resolution 行为，不是 active branch count limit。
  - manual smoke 选择 assistant 回复内部 substring，不选 line boundary / newline。
- Next step
  - P2 Step 2B read-only hydration reconnaissance 已完成并验收。
  - P2 Step 2C read path 已完成。
  - 当前下一步是 P2 Step 2D persisted highlight hydration / repaint。

## CHECKPOINT: P2_STEP_2B_HYDRATION_READONLY_RECON_ACCEPTED

- Status
  - P2 Step 2B read-only hydration reconnaissance 已完成并验收。
  - 本步严格只读：没有 hydration implementation、没有 highlight painting、没有 click-to-reopen、没有 summary generation。
- Conclusions
  - 当时下一步是 P2 Step 2C read persisted anchors implementation；现已完成。
  - Step 2C 应新增 topic-scoped hook，例如 `usePersistedBranchAnchors(parentTopicId)`。
  - Hook 使用现有 typed DataApi `useQuery` pattern 读取 `GET /topics/:id/branch-anchors`。
  - `keepPreviousData` 应为 `false`，避免 active topic 切换时旧 anchors 泄漏。
  - persisted anchor read state 不应存入 `BranchPane` live card state。
  - `BranchAnchorContext` 在 Step 2D 前继续只承载 paintable highlight projections。
  - Step 2D 才处理 message/block/offset validation 与 highlight restoration。
  - Step 3 是 click-to-reopen。
  - Step 4 是 summary generation。
  - 已知 UX note 保持独立：`Open as branch` 可因 invalid selection anchor boundaries disabled；这不是 Step 2C。
- Current status
  - P2 Step 2B recon accepted；P2 Step 2C read path 已完成。
- Next step
  - P2 Step 2D persisted highlight hydration / repaint。
  - Step 2D 不是 click-to-reopen，也不是 summary。

## CHECKPOINT: P2_STEP_2C_READ_PERSISTED_BRANCH_ANCHORS_IMPLEMENTED

- Status
  - P2 Step 2C read path 已实现。
  - persisted `branch_anchor` rows 现在可在 renderer 读取并归一化为 read-only anchor state。
- Changes
  - 新增 `usePersistedBranchAnchors(parentTopicId)`。
  - Hook 使用 typed DataApi `useQuery('/topics/:id/branch-anchors', { params: { id: parentTopicId }, enabled: Boolean(parentTopicId), swrOptions: { keepPreviousData: false } })`。
  - 归一化后的 anchor 包含 persisted DTO 字段、`hydrationStatus: 'unresolved'`、`skippedReason/resolvedSelectionStart/resolvedSelectionEnd: undefined`。
  - `Chat.tsx` 已用 `activeTopic.id` 调用 hook；没有把 persisted anchors 放入 `BranchPane` live card state，也没有传入 `BranchAnchorContext`。
- Boundary
  - Step 2C 不是 highlight restoration / highlight painting。
  - Step 2C 不是 click-to-reopen。
  - Step 2C 不是 summary generation。
  - message/block/offset validation 留给 Step 2D。
- Verification
  - `pnpm typecheck:web` 通过。
  - 新 hook focused test 通过（1 file / 7 tests）。
  - 现有 branchAnchorWrite / useBranchAnchorPersistence focused tests 通过（2 files / 17 tests）。
- Current status
  - P2 Step 2C read path implemented。
- Next step
  - P2 Step 2D persisted highlight hydration / repaint。

## 已完成（最近）

- **P2 Step 2C implemented**：persisted `branch_anchor` rows 可在 renderer 读取并归一化；没有 highlight restoration、click-to-reopen、summary generation。下一步是 Step 2D persisted highlight hydration / repaint。
- **P2 Step 2B accepted**：read-only hydration reconnaissance 完成并验收；没有 hydration implementation、没有 highlight painting、没有 click-to-reopen、没有 summary generation。当时下一步是 P2 Step 2C read persisted anchors implementation，现已完成。
- **P2 Step 2A accepted**：manual smoke 通过；write-on-keep 创建、unkeep 删除、keep again 防重复、pending discard 不落 anchor、global `branchTopicId` 无重复均验证通过。
- **P2 Step 2A.1**：unkeep cleanup 完成；kept -> pending 删除本会话已知 anchor，POST failure 可 retry，DELETE failure 保留 cleanup 状态。
- **P2 Phase B Step 1**：后端持久化层完成（`branch_anchor` schema、生成迁移、shared DTO/types/API schema、DataApi handlers、service、真实 DB integration tests、gates green）。
- **P1 多分支 UX**：S2d 完成后已全部收尾，不再作为 active stage。
- **P2 Phase A recon**：Topic metadata 路线被排除；存储裁决为独立 `branch_anchor` 表。

## 已入库（参考用，不再追）

| commit | 日期 | 内容 |
|---|---|---|
| `14edaa0f7` | 2026-06-01 | `feat(branch-panel): targeted multi-branch highlight impl — data-branch-id + per-branch color + targeted clear (P1-S2a)` —— sourceHighlight.ts stamping + targeted clear + 6 色 palette + 14 文件 |
| `f915938ec` | 2026-06-01 | `refactor(branch-panel): generalize single branch to branches[] (P1-S1, no behavior change)` —— state foundation refactor + 4 文档 |
| `c903acb5c` | 2026-05-31 | `test(branch-panel): replace fake-green unmount cleanup with real deps-change cleanup test` —— mutation 验证 + 假绿修补 + 4 文档同步 |
| `f3bf0dbf7` | 2026-05-26 | `test(branch-panel): cover real span-wrap paint/clear and drop dangling anchor attr` —— P0 测试重写：删 dangling `data-branch-anchored` 属性 + MainTextBlock.test.tsx 5 个高亮用例 + sourceHighlight.test.ts 新增 6 paint/clear DOM 突变链用例 + 4 份文档 |
| （D-009 / Task 3 / S6' 主体源码）| 2026-05-22 ~ 2026-05-26 | 用户用 GitHub Desktop 分多次入库（D-009 BranchAssistantContext、Task 3 resize divider、S6' BranchAnchorContext + sourceHighlight.ts + paint 链 + Chat onComposeCancel 等）；具体 commit hash 见 `git log` |
| `c278f5c0a` + `e49d1cc0f` + `3e2ecec41` | 2026-05-22 | T-006D-2B Side-by-side branch S1'–S4' |

## ✅ 已 closed（参考用，不再追）

- **D-001** baseline topic FK — T-004 修复 + 用户 2026-05-21 fresh install 实测通过
- **D-002** assistant message modelId FK — T-005B 修复 + 用户 2026-05-21 实测 gemma4:e4b 流式回复正常
- **D-003** Ollama 端到端 — T-007 D-003B（providers.json）+ T-008C D-003C（chat-model-popup 切 v2）双段修复 + 用户 2026-05-21 实测
- **D-004** assistant Ask/Open disabled — T-009 同源修复 + 用户 2026-05-21 复测：DOM 看到 assistant wrapper，Ask/Open 可点
- **D-005** BeatLoader 不消失 — T-009 `newMessage.ts:275` 取消注释 + `StreamingService.finalize` 防御 dispatch + 用户 2026-05-21 实测：回复完后 BeatLoader 消失 + 操作栏出现
- **D-008** Branch panel 滚动失效 — 根因 = `RowFlex` 没 `h-full` 高度链断；修 = RowFlex + BranchPane motion.div 加 h-full；用户 2026-05-22 实测 long branch reply 可滚 ✅
- **D-009** 分支 regenerate/edit/delete 模型瞎 — 根因 = `Message.tsx:73 useAssistant(id)` 走 Redux 全局；修 = Option 1 BranchAssistantContext 边界注入；用户 2026-05-22 视觉验证通过 ✅
- **Task 3** MAIN \| BRANCH resizable divider — 镜像 `useSidebarResize` 写右锚 `useBranchPaneResize`；用户 2026-05-22 视觉验证通过（拖动 reflow / 窄宽两端 scroll / 1:1 跟手 / 开关动画）✅
- **S6' / T-006E** source-passage highlight — `<span>` wrap + `replaceWith` 全局清扫 + `globalThis` Context singleton；2026-05-23 视觉 + cleanup 计数验证通过；2026-05-26 真实测试覆盖入库；2026-05-31 mutation 验证 + deps-change cleanup 假绿修补 ✅

详见 [问题与Debug记录.md](./问题与Debug记录.md) 已 closed 区。

## 决策史完整记录

D-010 → D-013-CLOSED 完整决策史见 [[问题与Debug记录]] 与 [[tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/README|T-006E README]]。**下一个对话不应该重读这一长串、直接看 T-006E README 顶部的「不要再走的弯路」red list + "测试覆盖" 段 + mutation 验证小节即可。**


## 🟢 仍 open 但低优先级（用户要求先记录不优先修）

- **D-006** fresh install 默认模型仍 CherryAI Qwen — 等 v2 默认模型策略迁移时一起处理
- **D-007** Regenerate 切到 Ollama 后无反应 — 主聊天侧的候选 issue，与 D-009 分支侧 regenerate 是不同问题（D-009 已修代码，D-007 仍待复测）

## 历史路径（不要当作下一步）

T-006 / P1 多分支 UX 相关条目已完成或转入 deferred，不再作为“立即可做”。当前不要回到 P1 设计、不要重做 multi-branch panel、不要把 anchor 持久化重新定义为 v1.1+ 未定事项；P2 Step 2C read path 已完成，下一步只看上方 Step 2D persisted highlight hydration / repaint。

## 短期：v2 切换主线

4. **renderer 直接消费 shared 类型** —— 删除 `DataApiMessageDataSource` 桥层
5. **Topic 列表换 `useInfiniteQuery('/topics')`** —— 替代 `useAssistant().topics[]`
6. **消息列表换 `useInfiniteQuery('/topics/:topicId/messages', { query: { includeSiblings: true } })`** —— 同时接 v2 分支数据
7. **引入 / 合并 v2 `MessageGroup` + `SiblingNavigator`**（来自 `DeJeune/ai-service`）
8. **焦点态从 message 迁到 topic**：`PUT /topics/:id/active-node` 替代 `editMessage({ foldSelected })`
9. **Block 删除契约确定**：(A) 补 `PATCH /messages/:id/blocks/:blockId`，或 (B) 固化"块 ops 走 message data 整体替换"为契约
10. **删除 Redux `newMessage` + `messageBlock` slice + `messageThunk`**

## 短期：独立支线

11. **方案 C 实施**（彻底修 default assistant sentinel）：renderer 启动时 `POST /assistants` 创建真助手 → 把 UUID dispatch 到 Redux `defaultAssistant.id`；完成后可以删 T-004 的 `=== 'default'` 判断
12. ~~**诊断 D-003 Ollama 自动模型同步失败**~~ —— ✅ T-007 已诊断完成；剩下的工作 = 用户确认是否应用 1 行 JSON 修复 + fresh install 实测
13. **Agent session 写路径迁 DataApi**（消除 `StreamingService.finalize:216` 的 `isAgentSessionTopicId` 分支）
14. **UI 库迁移**（antd → `@cherrystudio/ui`）
15. **重新加回 `descend` flag**（schema + service + UI 同时改）

## 中期 / 长期

15. **EventEmitter 强类型化**
16. **DataApi 订阅消费**（`DataApi_Subscribe`）
17. **流式 throttle 测量**
18. **全文搜索端点**
19. **renderer 聊天主链路测试覆盖**

## 不要做（红灯）

- ❌ 不要在 v1 路径上修 bug（除非阻塞 v2）
- ❌ 不要手改 codegen 产物
- ❌ 不要给 `migrations/sqlite-drizzle/*.sql` 写 patch migration
- ❌ 不要在新组件里写 `styled-components` / 引入 `antd` / `HeroUI`
- ❌ 不要先删 Redux 再迁 UI（顺序反了会黑屏）
- ❌ 不要绕过 `messageThunk` / `StreamingService` 直接改消息/流式路径；`branch_anchor` 的 P2 DataApi 写入除外

## 给下一个对话的"上手提示"

接手聊天模块工作，按优先级读这五份：
1. [CLAUDE.md](../../CLAUDE.md) — 工程纪律
2. [02_Architecture/源码地图.md](../02_Architecture/源码地图.md) — 模块全景
3. [02_Architecture/DataApi端点.md](../02_Architecture/DataApi端点.md) — 写/读路径真实分工
4. [02_Architecture/分支对话.md](../02_Architecture/分支对话.md) — 当前分支真正在做的事
5. [01_Project/风险与限制.md](../01_Project/风险与限制.md) — 红黄灯
