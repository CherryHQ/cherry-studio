# 下一步

> 更新于 2026-05-21（T-006B 已入库 `d579fdcf2`；T-006C / T-006D-1 / role 收口补丁 staged 未 commit；workspace.json 不入 commit）。

## 🟡 当前唯一未完成动作

0. **手动端到端验证 baseline 二连修**（T-004 + T-005B 已 commit `15ad2eb08`，但用户尚未实测）：
   ```bash
   rm -rf ~/Library/Application\ Support/CherryStudioDev
   pnpm dev
   ```
   - 新建 topic ✅（T-004）
   - 输入消息 + 发送 → assistant message 行能 INSERT，**不再报 SQLITE_CONSTRAINT_FOREIGNKEY**（T-005B）
   - AI 调用本身需要先配 provider/API key（D-003 让 Ollama 不可用，请用别的 provider 测真实回复）

## 🟡 已知 non-blocking 问题（先记账，后续单独立任务排查）

- **D-003 Ollama 自动模型同步失败** —— Ollama 进程正常但 Cherry 显示 0/0 Enabled；设置页疑似显示 Anthropic 字段；不阻塞 Phase 3 UI 原型；要测真实 AI 回复请用别的 provider。详见 [问题与Debug记录.md#D-003 详细记录](./问题与Debug记录.md)。

## 立即可做（baseline 解除后）

1. **T-006 Text Anchor Branch UI 实施进度**：
   - ✅ T-006B 文本选中捕获 —— 已 commit `d579fdcf2`
   - ✅ T-006C SelectionContextMenu 扩展（含 2026-05-21 role 收口补丁）—— staged 未 commit
   - ✅ T-006D-1 BranchPanel 壳子原型（Dialog + 字段显示 + 创建按钮 log only）—— staged 未 commit
   - 下一步建议：**先把 staged 改动拆 commit 再进 T-006D-2**（理由见下方「Commit 拆分建议」）
   - 之后：T-006D-2 真实 fork + useCache 持久化 + 流式 disable
   - 平行可推：T-006A 测试数据（可选） / T-006E 高亮
   - 之后：T-006D-3（Sheet 改造 + inputbar 注入分流）/ T-006F（v1.1 候选）

   ### Commit 拆分建议（staged → 3 个独立 commit）

   当前 index 把 T-006C 首版 + role 收口补丁 + T-006D-1 + 配套文档全部混在一起。推荐拆分顺序：

   ```
   feat(messages): wire SelectionContextMenu to branch-anchor handlers              (T-006C 首版)
   feat(messages): restrict text-anchor branch actions to assistant blocks           (T-006C role 收口补丁)
   feat(messages): add Branch Panel shell prototype                                  (T-006D-1)
   ```

   注意：
   - 工作区另有 `Chat APP-Docs/.obsidian/workspace.json` 的 `MM` 状态（已 staged + 还有未 staged 改动），**不要**入 commit（个人 vault 设置）。
   - 拆 commit 时建议用 `git restore --staged` 配合 `git add -p` 而非粗暴 reset。
   - 均 `--signoff` per CLAUDE.md。
2. **找 `DeJeune/ai-service` 分支** —— 看 v2 `SiblingNavigator.tsx` / `MessageGroup.tsx` 是否能合并；不与 T-006 冲突（sibling 维度 vs 文本 anchor 维度）
3. **跑一次端到端发消息** —— 验证 chat completion 路径在 `topic.assistantId = null` 时仍能取到 model（理论上应该走 Redux `defaultAssistant.defaultModel` / 用户 mention，与 topic 字段无关）

## 短期：v2 切换主线

4. **renderer 直接消费 shared 类型** —— 删除 `DataApiMessageDataSource` 桥层
5. **Topic 列表换 `useInfiniteQuery('/topics')`** —— 替代 `useAssistant().topics[]`
6. **消息列表换 `useInfiniteQuery('/topics/:topicId/messages', { query: { includeSiblings: true } })`** —— 同时接 v2 分支数据
7. **引入 / 合并 v2 `MessageGroup` + `SiblingNavigator`**（来自 `DeJeune/ai-service`）
8. **焦点态从 message 迁到 topic**：`PUT /topics/:id/active-node` 替代 `editMessage({ foldSelected })`
9. **Block 删除契约确定**：(A) 补 `PATCH /messages/:id/blocks/:blockId`，或 (B) 固化"块 ops 走 message data 整体替换"为契约
10. **删除 Redux `newMessage` + `messageBlock` slice + `messageThunk`**

## 短期：独立支线

11. **方案 C 实施**（彻底修 default assistant sentinel）：renderer 启动时 `POST /assistants` 创建真助手 → 把 UUID dispatch 到 Redux `defaultAssistant.id`；完成后可以删 T-004 的 `=== 'default'` 判断
12. **诊断 D-003 Ollama 自动模型同步失败**（T-006 候选）：grep `Provider auto model sync failed`；核查 `presetProviderSeeder.ts` Ollama 行；核查 provider 设置 form 的 schema 分支（为何 Ollama 渲染出 Anthropic 字段）
13. **Agent session 写路径迁 DataApi**（消除 `StreamingService.finalize:216` 的 `isAgentSessionTopicId` 分支）
14. **UI 库迁移**（antd → `@cherrystudio/ui`）
15. **重新加回 `descend` flag**（schema + service + UI 同时改）

## 中期 / 长期

15. **EventEmitter 强类型化**
16. **DataApi 订阅消费**（`DataApi_Subscribe`）
17. **流式 throttle 测量**
18. **全文搜索端点**
19. **renderer 聊天主链路测试覆盖**

## 不要做（红灯）

- ❌ 不要在 v1 路径上修 bug（除非阻塞 v2）
- ❌ 不要手改 codegen 产物
- ❌ 不要给 `migrations/sqlite-drizzle/*.sql` 写 patch migration
- ❌ 不要在新组件里写 `styled-components` / 引入 `antd` / `HeroUI`
- ❌ 不要先删 Redux 再迁 UI（顺序反了会黑屏）
- ❌ 不要绕过 `messageThunk` / `StreamingService` 直接写 DataApi

## 给下一个对话的"上手提示"

接手聊天模块工作，按优先级读这五份：
1. [CLAUDE.md](../../CLAUDE.md) — 工程纪律
2. [02_Architecture/源码地图.md](../02_Architecture/源码地图.md) — 模块全景
3. [02_Architecture/DataApi端点.md](../02_Architecture/DataApi端点.md) — 写/读路径真实分工
4. [02_Architecture/分支对话.md](../02_Architecture/分支对话.md) — 当前分支真正在做的事
5. [01_Project/风险与限制.md](../01_Project/风险与限制.md) — 红黄灯
