# 下一步

> 更新于 2026-06-01（**P1-S2a — sourceHighlight.ts 加 data-branch-id + per-branch color + targeted clear；UI 仍 ≤ 1 分支**；P1-S1 已入库 commit `f915938ec`；本轮 S2a 未 commit = **14 个文件 = 9 src + 5 docs（4 modified + 1 new P1-S2a README）**，待审 + GitHub Desktop 提交）。

> **SIGSEGV 调查结论**（2026-06-01）：把 S2a 全部 stash 走 → clean HEAD `f915938ec` 跑 `pnpm build:check` —— vitest main 进程跑完所有测试后仍然段错误 SIGSEGV 11，跟带 S2a 改动时一模一样。**确认环境闪问题**，不是 S1 也不是 S2a 代码。从此后续 build:check 在 main 进程 vitest shutdown 阶段段错误可以名正言顺归类为已知环境问题、不阻塞 commit。Renderer focused 测试稳定全绿，typecheck/lint/format/i18n 全过。

## 未 commit 文件清单（用户自管 — 2026-06-01 S2a 现状；按 `git status` 实数 14 项）

### 9 modified src files

| 文件 | 改动 |
|---|---|
| `src/renderer/src/pages/home/Messages/BranchPanel/constants.ts` | + `BRANCH_HL_COLOR_KEYS = ['c1'..'c6'] as const` + `type BranchHlColorKey` + `BRANCH_HL_DEFAULT_COLOR` |
| `src/renderer/src/pages/home/Messages/BranchPanel/types.ts` | `Branch` + `color: BranchHlColorKey` 字段 |
| `src/renderer/src/pages/home/Messages/BranchPanel/index.ts` | 多 export `BRANCH_HL_COLOR_KEYS` / `BRANCH_HL_DEFAULT_COLOR` / `BranchHlColorKey` |
| `src/renderer/src/context/BranchAnchorContext.tsx` | `BranchAnchorHighlight` + `color: BranchHlColorKey` 字段；`__BRANCH_ANCHOR_CTX_CACHE__` 整段保留 |
| `src/renderer/src/pages/home/Chat.tsx` | `openBranchAnchor` 写 `color: BRANCH_HL_DEFAULT_COLOR`；`branchAnchorHighlight` 派生 `color: b.color`；`onComposeCancel` 改 `branches.forEach(b => clearSourceHighlight(b.id))` |
| `src/renderer/src/pages/home/Messages/Blocks/MainTextBlock.tsx` | effect 传 `branchId + color` 给 paint；cleanup 闭包捕获 `paintedBranchIds` 逐个 targeted clear |
| `src/renderer/src/utils/branchAnchor/sourceHighlight.ts` | **核心动作**：`wrapRangeWithSpans` 多 2 参 stamp `data-branch-id` + `data-hl`；`paintSourceHighlight(..., branchId, colorKey)` + 体内 first-line `clearSourceHighlight(branchId)`；`clearSourceHighlight(branchId)` 用 attribute-selector + `CSS.escape`；`ensureHighlightStyle` 注入 6 色 CSS 变量 + 6 条 `[data-hl="cN"]` 规则 |
| `src/renderer/src/utils/branchAnchor/__tests__/sourceHighlight.test.ts` | 9 round-trip 不动；6 单分支 paint/clear 改新 API + attr 断言；**新增 4 multi-branch 用例**：disjoint coexist / clear(A) leaves B then clear(B) wipes / nested overlap / repeated cycles cross-branches。15 → 19 |
| `src/renderer/src/pages/home/Messages/Blocks/__tests__/MainTextBlock.test.tsx` | `highlight()` helper 加 branchId+color 默认参；`injects span when matched` 用例加 attr 断言；21 → 21 |

### 4 modified docs

| 文件 | 改动 |
|---|---|
| `Chat APP-Docs/03_Development/tasks/P1_MultiBranch/README.md` | P1 子任务表加 S2a + S2b；触碰边界条目加 S2a 例外 |
| `Chat APP-Docs/03_Development/当前状态.md` | 顶部追加 S2a note；时间线表新增 S2a 行；阶段定位 + 一句话更新 |
| `Chat APP-Docs/03_Development/下一步.md` | 本文件 |
| `Chat APP-Docs/03_Development/开发日志.md` | 顶部追加 2026-06-01 P1-S2a 条目 |

### 1 new doc

| 文件 | 内容 |
|---|---|
| `Chat APP-Docs/03_Development/tasks/P1_MultiBranch/P1-S2a_HighlightImpl/README.md` | S2a 任务收尾：scope / 改动 / 测试矩阵 / mutation 结果 / S2a↔S2b 边界 / future-you red flags |

**合计 = 14 文件（9 src + 4 modified docs + 1 new doc）**。

**未触动（守住边界）**：
- `__BRANCH_ANCHOR_CTX_CACHE__` HMR 缓存模式（D-013-FIX 根因防御）
- fork / streaming / provider / model picker / Redux / Dexie
- 任何多分支 UI（卡片列表 / collapse / hover linkage / 第 2 分支创建）
- `collapsedBranchIds` 仍 unconsumed（S2b 才接）
- 颜色 cycling（S2a 永远 c1 默认，S2b 才 cycling）

**排除（用户惯例不入 commit）**：`.gitignore` / `.obsidian/{workspace,graph}.json` / 根杂项 / 用户自建 `P1架构设计草案.md` 等 untracked 草稿。

**拟 commit（用户参考）**：
- 单 commit：`feat(branch-panel): targeted multi-branch highlight impl — data-branch-id + per-branch color + targeted clear (P1-S2a)` —— 含 14 文件（9 src + 5 docs）+ `--signoff` + `Co-Authored-By`

## 已入库（参考用，不再追）

| commit | 日期 | 内容 |
|---|---|---|
| `f915938ec` | 2026-06-01 | `refactor(branch-panel): generalize single branch to branches[] (P1-S1, no behavior change)` —— state foundation refactor + 4 文档 |
| `c903acb5c` | 2026-05-31 | `test(branch-panel): replace fake-green unmount cleanup with real deps-change cleanup test` —— mutation 验证 + 假绿修补 + 4 文档同步 |
| `f3bf0dbf7` | 2026-05-26 | `test(branch-panel): cover real span-wrap paint/clear and drop dangling anchor attr` —— P0 测试重写：删 dangling `data-branch-anchored` 属性 + MainTextBlock.test.tsx 5 个高亮用例 + sourceHighlight.test.ts 新增 6 paint/clear DOM 突变链用例 + 4 份文档 |
| （D-009 / Task 3 / S6' 主体源码）| 2026-05-22 ~ 2026-05-26 | 用户用 GitHub Desktop 分多次入库（D-009 BranchAssistantContext、Task 3 resize divider、S6' BranchAnchorContext + sourceHighlight.ts + paint 链 + Chat onComposeCancel 等）；具体 commit hash 见 `git log` |
| `c278f5c0a` + `e49d1cc0f` + `3e2ecec41` | 2026-05-22 | T-006D-2B Side-by-side branch S1'–S4' |

## ✅ 已 closed（参考用，不再追）

- **D-001** baseline topic FK — T-004 修复 + 用户 2026-05-21 fresh install 实测通过
- **D-002** assistant message modelId FK — T-005B 修复 + 用户 2026-05-21 实测 gemma4:e4b 流式回复正常
- **D-003** Ollama 端到端 — T-007 D-003B（providers.json）+ T-008C D-003C（chat-model-popup 切 v2）双段修复 + 用户 2026-05-21 实测
- **D-004** assistant Ask/Open disabled — T-009 同源修复 + 用户 2026-05-21 复测：DOM 看到 assistant wrapper，Ask/Open 可点
- **D-005** BeatLoader 不消失 — T-009 `newMessage.ts:275` 取消注释 + `StreamingService.finalize` 防御 dispatch + 用户 2026-05-21 实测：回复完后 BeatLoader 消失 + 操作栏出现
- **D-008** Branch panel 滚动失效 — 根因 = `RowFlex` 没 `h-full` 高度链断；修 = RowFlex + BranchPane motion.div 加 h-full；用户 2026-05-22 实测 long branch reply 可滚 ✅
- **D-009** 分支 regenerate/edit/delete 模型瞎 — 根因 = `Message.tsx:73 useAssistant(id)` 走 Redux 全局；修 = Option 1 BranchAssistantContext 边界注入；用户 2026-05-22 视觉验证通过 ✅
- **Task 3** MAIN \| BRANCH resizable divider — 镜像 `useSidebarResize` 写右锚 `useBranchPaneResize`；用户 2026-05-22 视觉验证通过（拖动 reflow / 窄宽两端 scroll / 1:1 跟手 / 开关动画）✅
- **S6' / T-006E** source-passage highlight — `<span>` wrap + `replaceWith` 全局清扫 + `globalThis` Context singleton；2026-05-23 视觉 + cleanup 计数验证通过；2026-05-26 真实测试覆盖入库；2026-05-31 mutation 验证 + deps-change cleanup 假绿修补 ✅

详见 [问题与Debug记录.md](./问题与Debug记录.md) 已 closed 区。

## 决策史完整记录

D-010 → D-013-CLOSED 完整决策史见 [[问题与Debug记录]] 与 [[tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/README|T-006E README]]。**下一个对话不应该重读这一长串、直接看 T-006E README 顶部的「不要再走的弯路」red list + "测试覆盖" 段 + mutation 验证小节即可。**


## 🟢 仍 open 但低优先级（用户要求先记录不优先修）

- **D-006** fresh install 默认模型仍 CherryAI Qwen — 等 v2 默认模型策略迁移时一起处理
- **D-007** Regenerate 切到 Ollama 后无反应 — 主聊天侧的候选 issue，与 D-009 分支侧 regenerate 是不同问题（D-009 已修代码，D-007 仍待复测）

## 立即可做（baseline 解除后）

1. **T-006 + P1 实施进度**：
   - ✅ T-006B 文本选中捕获 —— commit `d579fdcf2`
   - ✅ T-006C / D-1 —— commit `76ee326a0`
   - ❌ T-006D-2A 跳转式 fork —— 废弃；产品方向错（自动切走 + 模型瞎），代码不 commit
   - ✅ **T-006D-2B Side-by-side branch S1'–S4' 已入库 + S4' 产品闸门通过**：commits `c278f5c0a` (S1') + `e49d1cc0f` (S2'+S3') + `3e2ecec41` (S4')
   - ✅ **D-008 closed**：scroll 高度链断裂修；删 jsdom false-green 单测
   - ✅ **D-009 closed**：regenerate/edit/delete 根因 + Option 1 BranchAssistantContext 实施 + 用户 2026-05-22 视觉验证通过
   - ✅ **Task 3 closed**：MAIN \| BRANCH resizable divider — 镜像 `useSidebarResize` 写右锚 `useBranchPaneResize`；用户 2026-05-22 视觉验证通过
   - ✅ **S6' / T-006E source-passage highlight closed**：最终方案 `<span>` wrap + `replaceWith` 全局清扫 + `globalThis` Context singleton + 关闭路径连通 + cleanup 计数验证通过；2026-05-26 真实测试覆盖入库（`f3bf0dbf7`）；2026-05-31 mutation 验证 + 修补 1 个 fake-green。完整决策史 D-010 → D-013-CLOSED 见 [[问题与Debug记录]] 与 [[tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/README|T-006E README]]
   - ✅ **P1-S1 state shape refactor 已入库** commit `f915938ec`：branches[] 数组化、零行为变化
   - ✅ **P1-S2a highlight 实现层升级（待 commit）**：sourceHighlight.ts 加 stamping + targeted clear + 6 色 palette；UI 仍 ≤ 1 分支
   - ⏳ **P1-S2b 多分支 UI（未开始）**：解 length ≤ 1 invariant；右键新选区 append；BranchPane 多卡片视图；hover 联动；颜色 cycling；`collapsedBranchIds` 接 UI；单分支独立关闭。**不再需要动 sourceHighlight.ts**
   - ⏳ **P1-S3 disposition（未开始）**：close 路径分裂 = 弃用（discard + DELETE topic）vs 保存（save + 留 SQLite + 可重开）
   - ⏳ T-006D-2C-0 流式 disable Ask/Open（与 D-2B 正交，可平行）
   - ⏳ T-006D-2C-1..5 cleanup（含 [[preflight.md|D-2C-5 分支侧 Dexie `db.topics.update(branchTopicId)` 静默 0-rows]] + **path Y / delete-on-close** —— 关闭分支删 SQLite topic 行；当前关闭只清前端 state、留 orphan）
   - ⏳ T-006A 测试数据（可选） / T-006D-3 / T-006F
   - ⏳ **anchor 持久化（v1.1+）**：当前 anchor 只活在 Chat.tsx useState 里，切 topic 离开再回来高亮丢；做了才能挂「点击 span 重开 panel」
2. **找 `DeJeune/ai-service` 分支** —— 看 v2 `SiblingNavigator.tsx` / `MessageGroup.tsx` 是否能合并；不与 T-006 冲突（sibling 维度 vs 文本 anchor 维度）
3. **跑一次端到端发消息** —— 验证 chat completion 路径在 `topic.assistantId = null` 时仍能取到 model（理论上应该走 Redux `defaultAssistant.defaultModel` / 用户 mention，与 topic 字段无关）

## 短期：v2 切换主线

4. **renderer 直接消费 shared 类型** —— 删除 `DataApiMessageDataSource` 桥层
5. **Topic 列表换 `useInfiniteQuery('/topics')`** —— 替代 `useAssistant().topics[]`
6. **消息列表换 `useInfiniteQuery('/topics/:topicId/messages', { query: { includeSiblings: true } })`** —— 同时接 v2 分支数据
7. **引入 / 合并 v2 `MessageGroup` + `SiblingNavigator`**（来自 `DeJeune/ai-service`）
8. **焦点态从 message 迁到 topic**：`PUT /topics/:id/active-node` 替代 `editMessage({ foldSelected })`
9. **Block 删除契约确定**：(A) 补 `PATCH /messages/:id/blocks/:blockId`，或 (B) 固化"块 ops 走 message data 整体替换"为契约
10. **删除 Redux `newMessage` + `messageBlock` slice + `messageThunk`**

## 短期：独立支线

11. **方案 C 实施**（彻底修 default assistant sentinel）：renderer 启动时 `POST /assistants` 创建真助手 → 把 UUID dispatch 到 Redux `defaultAssistant.id`；完成后可以删 T-004 的 `=== 'default'` 判断
12. ~~**诊断 D-003 Ollama 自动模型同步失败**~~ —— ✅ T-007 已诊断完成；剩下的工作 = 用户确认是否应用 1 行 JSON 修复 + fresh install 实测
13. **Agent session 写路径迁 DataApi**（消除 `StreamingService.finalize:216` 的 `isAgentSessionTopicId` 分支）
14. **UI 库迁移**（antd → `@cherrystudio/ui`）
15. **重新加回 `descend` flag**（schema + service + UI 同时改）

## 中期 / 长期

15. **EventEmitter 强类型化**
16. **DataApi 订阅消费**（`DataApi_Subscribe`）
17. **流式 throttle 测量**
18. **全文搜索端点**
19. **renderer 聊天主链路测试覆盖**

## 不要做（红灯）

- ❌ 不要在 v1 路径上修 bug（除非阻塞 v2）
- ❌ 不要手改 codegen 产物
- ❌ 不要给 `migrations/sqlite-drizzle/*.sql` 写 patch migration
- ❌ 不要在新组件里写 `styled-components` / 引入 `antd` / `HeroUI`
- ❌ 不要先删 Redux 再迁 UI（顺序反了会黑屏）
- ❌ 不要绕过 `messageThunk` / `StreamingService` 直接写 DataApi

## 给下一个对话的"上手提示"

接手聊天模块工作，按优先级读这五份：
1. [CLAUDE.md](../../CLAUDE.md) — 工程纪律
2. [02_Architecture/源码地图.md](../02_Architecture/源码地图.md) — 模块全景
3. [02_Architecture/DataApi端点.md](../02_Architecture/DataApi端点.md) — 写/读路径真实分工
4. [02_Architecture/分支对话.md](../02_Architecture/分支对话.md) — 当前分支真正在做的事
5. [01_Project/风险与限制.md](../01_Project/风险与限制.md) — 红黄灯
