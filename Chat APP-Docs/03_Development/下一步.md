# 下一步

> 更新于 2026-05-21（D-001 / D-002 / D-003 全部 closed；T-006B 已入库 `d579fdcf2`；T-006C / T-006D-1 / role 收口补丁 + T-007 fix + T-008C 仍 staged 未 commit；workspace.json 不入 commit）。

## 🟡 新发现：D-004 / D-005 同源（T-009 诊断完成）

T-008C 修了 Chat picker 的 v1/v2 断层，但更深一层的同款 bug 还在：**StreamingService + BlockManager 写 cacheService + DataApi，但不 dispatch Redux**；MessageBlockRenderer 仍读 Redux → Redux 的 assistant message 永远 `blocks: []` + `status: PENDING`。

- **D-004**：assistant 文本上 Ask/Open 永远 disabled（MainTextBlock 不渲染，data-message-id wrapper 不存在）
- **D-005**：底部 3 个点（BeatLoader）永远转（isMessageProcessing 永远 true）
- 这是同一个 bug 的两个面，修一处都修

**详见 [T-009 诊断.md](./tasks/T-009_StreamingNotDispatchedToRedux/诊断.md)。** 3 条修复路径，推荐方案 A（cache→Redux 桥，~30 行 in StreamingService）。

**D-006 / D-007** 是用户明确要求**先记录不优先修**的低优先候选 issue。

## ✅ 已 closed（参考用，不再追）

- **D-001** baseline topic FK — T-004 修复 + 用户 2026-05-21 fresh install 实测通过
- **D-002** assistant message modelId FK — T-005B 修复 + 用户 2026-05-21 实测 gemma4:e4b 流式回复正常
- **D-003** Ollama 端到端 — T-007 D-003B（providers.json）+ T-008C D-003C（chat-model-popup 切 v2）双段修复 + 用户 2026-05-21 实测 Ollama Provider 同步 / picker 可选 / gemma4:e4b 正常生成 assistant 回复

详见 [问题与Debug记录.md](./问题与Debug记录.md) 已 closed 区。

## 🟡 当前未完成动作（两条并行）

**A) T-009 拍板修复路径 + 实施** —— 推荐方案 A，~30 行 in StreamingService；D-004 + D-005 一次修；修完 T-006 Text Anchor 端到端测试就解锁

**B) commit T-007 + T-008C 的代码** —— 你已经把 T-006C + T-006D-1 + 配套文档合并成 commit `76ee326a0`，docs 单独成 `b0789781a`，剩下两段未入库：

| 段 | 文件 | 推荐 commit message |
|---|---|---|
| T-007 D-003B | `packages/provider-registry/data/providers.json`（1 行 +） | `fix(provider-registry): set Ollama defaultChatEndpoint to ollama-chat` |
| T-008C D-003C | `src/renderer/src/components/Popups/SelectModelPopup/chat-model-popup.tsx`（改）+ `chat-model-popup.test.tsx`（新）| `feat(messages): swap chat model picker data source to v2 DataApi` |

之外还有一批文档（本次 closure 加的滚动文档 + T-008C task 目录）值得单独成一个 `docs(project): close D-001/D-002/D-003 after manual verification` commit。

**注意**：
- 显式排除 `Chat APP-Docs/.obsidian/{workspace,graph}.json`（个人 vault 设置）
- `.gitignore` 改动看下是否要一起入（如果是把 `.obsidian/` 添加进去就该入）
- 均 `--signoff` per CLAUDE.md

## 立即可做（baseline 解除后）

1. **T-006 Text Anchor Branch UI 实施进度**：
   - ✅ T-006B 文本选中捕获 —— 已 commit `d579fdcf2`
   - ✅ T-006C SelectionContextMenu 扩展（含 2026-05-21 role 收口补丁）—— staged 未 commit
   - ✅ T-006D-1 BranchPanel 壳子原型（Dialog + 字段显示 + 创建按钮 log only）—— staged 未 commit
   - 下一步建议：**先把 staged 改动拆 commit 再进 T-006D-2**（理由见下方「Commit 拆分建议」）
   - 之后：T-006D-2 真实 fork + useCache 持久化 + 流式 disable
   - 平行可推：T-006A 测试数据（可选） / T-006E 高亮
   - 之后：T-006D-3（Sheet 改造 + inputbar 注入分流）/ T-006F（v1.1 候选）

   ### Commit 历史（参考）

   T-006C 首版 + T-006C role 收口补丁 + T-006D-1 已合并入 commit `76ee326a0`（feat(messages): wire SelectionContextMenu to branch-anchor handlers）；配套文档在 `b0789781a docs`。剩下 T-007 / T-008C 见上方「当前唯一未完成动作」。
2. **找 `DeJeune/ai-service` 分支** —— 看 v2 `SiblingNavigator.tsx` / `MessageGroup.tsx` 是否能合并；不与 T-006 冲突（sibling 维度 vs 文本 anchor 维度）
3. **跑一次端到端发消息** —— 验证 chat completion 路径在 `topic.assistantId = null` 时仍能取到 model（理论上应该走 Redux `defaultAssistant.defaultModel` / 用户 mention，与 topic 字段无关）

## 短期：v2 切换主线

4. **renderer 直接消费 shared 类型** —— 删除 `DataApiMessageDataSource` 桥层
5. **Topic 列表换 `useInfiniteQuery('/topics')`** —— 替代 `useAssistant().topics[]`
6. **消息列表换 `useInfiniteQuery('/topics/:topicId/messages', { query: { includeSiblings: true } })`** —— 同时接 v2 分支数据
7. **引入 / 合并 v2 `MessageGroup` + `SiblingNavigator`**（来自 `DeJeune/ai-service`）
8. **焦点态从 message 迁到 topic**：`PUT /topics/:id/active-node` 替代 `editMessage({ foldSelected })`
9. **Block 删除契约确定**：(A) 补 `PATCH /messages/:id/blocks/:blockId`，或 (B) 固化"块 ops 走 message data 整体替换"为契约
10. **删除 Redux `newMessage` + `messageBlock` slice + `messageThunk`**

## 短期：独立支线

11. **方案 C 实施**（彻底修 default assistant sentinel）：renderer 启动时 `POST /assistants` 创建真助手 → 把 UUID dispatch 到 Redux `defaultAssistant.id`；完成后可以删 T-004 的 `=== 'default'` 判断
12. ~~**诊断 D-003 Ollama 自动模型同步失败**~~ —— ✅ T-007 已诊断完成；剩下的工作 = 用户确认是否应用 1 行 JSON 修复 + fresh install 实测
13. **Agent session 写路径迁 DataApi**（消除 `StreamingService.finalize:216` 的 `isAgentSessionTopicId` 分支）
14. **UI 库迁移**（antd → `@cherrystudio/ui`）
15. **重新加回 `descend` flag**（schema + service + UI 同时改）

## 中期 / 长期

15. **EventEmitter 强类型化**
16. **DataApi 订阅消费**（`DataApi_Subscribe`）
17. **流式 throttle 测量**
18. **全文搜索端点**
19. **renderer 聊天主链路测试覆盖**

## 不要做（红灯）

- ❌ 不要在 v1 路径上修 bug（除非阻塞 v2）
- ❌ 不要手改 codegen 产物
- ❌ 不要给 `migrations/sqlite-drizzle/*.sql` 写 patch migration
- ❌ 不要在新组件里写 `styled-components` / 引入 `antd` / `HeroUI`
- ❌ 不要先删 Redux 再迁 UI（顺序反了会黑屏）
- ❌ 不要绕过 `messageThunk` / `StreamingService` 直接写 DataApi

## 给下一个对话的"上手提示"

接手聊天模块工作，按优先级读这五份：
1. [CLAUDE.md](../../CLAUDE.md) — 工程纪律
2. [02_Architecture/源码地图.md](../02_Architecture/源码地图.md) — 模块全景
3. [02_Architecture/DataApi端点.md](../02_Architecture/DataApi端点.md) — 写/读路径真实分工
4. [02_Architecture/分支对话.md](../02_Architecture/分支对话.md) — 当前分支真正在做的事
5. [01_Project/风险与限制.md](../01_Project/风险与限制.md) — 红黄灯
