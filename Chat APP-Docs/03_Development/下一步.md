# 下一步

> 更新于 2026-05-23（D-2B S1'–S4' 入库；**D-008 + D-009 + Task 3 + S6'/T-006E 四项视觉 + cleanup 验证全部通过**；D-009 + Task 3 + S6' 代码未 commit — 用户自管 git via GitHub Desktop）。

## 未 commit 文件清单（用户自管）

| 文件 | 性质 | 归属 |
|---|---|---|
| `src/renderer/src/context/BranchAssistantContext.tsx` | 新建 | D-009 |
| `src/renderer/src/context/__tests__/BranchAssistantContext.test.ts` | 新建（4 用例） | D-009 |
| `src/renderer/src/hooks/useAssistant.ts` | 改（一行接入 `resolveAssistantSource`） | D-009 |
| `src/renderer/src/hooks/useBranchFork.ts` | 改（撤 watch#1 / watch#3 调试日志） | D-009 |
| `src/renderer/src/pages/home/Messages/BranchPanel/useBranchPaneResize.ts` | 新建 | Task 3 |
| `src/renderer/src/pages/home/Messages/BranchPanel/constants.ts` | 新建（width 常量） | Task 3 |
| `src/renderer/src/pages/home/Messages/BranchPanel/__tests__/BranchPane.test.tsx` | 改（+ 2 handle 渲染用例 [Task 3]） | Task 3 |
| `src/renderer/src/i18n/locales/{en-us,zh-cn,zh-tw}.json` | 改（+ `anchor.panel.resize_handle` 文案） | Task 3 |
| `src/renderer/src/context/BranchAnchorContext.tsx` | 新建（携 `highlightedBlockId` + selectionStart/End；`globalThis` Context singleton HMR-safe） | S6' |
| `src/renderer/src/utils/branchAnchor/sourceHighlight.ts` | 新建（`captureSelectionOffsets` + `resolveBranchHighlightRange` + **`<span>` wrap paint** + `replaceWith` global clear） | S6' |
| `src/renderer/src/utils/branchAnchor/__tests__/sourceHighlight.test.ts` | 新建（7 capture↔rebuild round-trip 用例 —— **测的是旧 CSS.highlights 路径，commit 前需重写为 span 断言**） | S6' |
| `src/renderer/src/pages/home/Messages/BranchPanel/types.ts` | 改（BranchAnchor + selectionStart/End） | S6' |
| `src/renderer/src/components/SelectionContextMenu.tsx` | 改（捕获 offsets 进 anchor） | S6' |
| `src/renderer/src/pages/home/Messages/Blocks/MainTextBlock.tsx` | 改（读 BranchAnchorContext + effect 调 paintSourceHighlight/clear；`[S6 trace]` 诊断 —— commit 前撤诊断日志） | S6' |
| `src/renderer/src/pages/home/Messages/Blocks/__tests__/MainTextBlock.test.tsx` | 改（4 高亮用例改 data-branch-anchored 断言） | S6' |
| `src/renderer/src/components/__tests__/SelectionContextMenu.test.tsx` | 改（anchor 断言改 objectContaining） | S6' |
| `src/renderer/src/hooks/__tests__/useBranchFork.test.ts` / `BranchComposer.test.tsx` / `BranchPane.test.tsx` | 改（anchor fixture 补 selectionStart/End；`BranchPane.test.tsx` 反转 close-disabled-in-conversation 断言） | S6' |
| `src/renderer/src/pages/home/Messages/BranchPanel/BranchPane.tsx` | 改（width state + drag handle + framer-motion 解耦 [Task 3] + 删 X 的 `disabled={!isComposing}` + tooltip 统一 [S6' close path]） | Task 3 + S6' |
| `src/renderer/src/pages/home/Chat.tsx` | 改（D-009 `branchOverride` Provider + S6' `branchAnchorHighlight` Provider 包主 `<Messages>` + `onComposeCancel` 扩展为完整关闭：`clearSourceHighlight + setBranchAnchor(null) + setBranchTopic(null) + branchFork.reset()`） | D-009 + S6' |
| `Chat APP-Docs/03_Development/{当前状态,下一步,会话日志,开发日志,问题与Debug记录}.md` | 改（standing rule 文档同步） | 跨 D-009 / Task 3 / S6' |
| `Chat APP-Docs/03_Development/tasks/T-006_TextAnchorBranchUI/**/README.md` | 改（状态同步） | 跨 D-009 / Task 3 / S6' |
| `Chat APP-Docs/03_Development/tasks/T-006_TextAnchorBranchUI/T-006D_BranchPanel/T-006D-2_RealFork/preflight.md` | 改（D-2C-5 cleanup） | D-009 |

**排除（用户惯例不入 commit）**：`.gitignore` / `.obsidian/{workspace,graph}.json`。

**拟 commit 拆分（用户参考）**：
- `fix(branch-fork): keep regenerate/edit/delete focused on selected passage via BranchAssistantContext` — D-009 src 文件
- `feat(branch-fork): drag-resizable branch panel divider` — Task 3 5 个 src 文件 + 3 个 i18n
- `feat(branch-fork): tint source passage while branch panel is open` — S6' 3 个 src 文件（BranchAnchorContext + MainTextBlock + 测试）
- `docs(project): close D-008 / D-009; record Task 3 + S6'` — 所有文档

注：`Chat.tsx` 同时被 D-009 与 S6' 改动 — 若严格拆 commit 该文件需分两次 stage（D-009 的 branchOverride 部分 / S6' 的 branchAnchorHighlight 部分），或合并到其中一个 commit。均 `--signoff` per CLAUDE.md。

## ✅ 已 closed（参考用，不再追）

- **D-001** baseline topic FK — T-004 修复 + 用户 2026-05-21 fresh install 实测通过
- **D-002** assistant message modelId FK — T-005B 修复 + 用户 2026-05-21 实测 gemma4:e4b 流式回复正常
- **D-003** Ollama 端到端 — T-007 D-003B（providers.json）+ T-008C D-003C（chat-model-popup 切 v2）双段修复 + 用户 2026-05-21 实测
- **D-004** assistant Ask/Open disabled — T-009 同源修复 + 用户 2026-05-21 复测：DOM 看到 assistant wrapper，Ask/Open 可点
- **D-005** BeatLoader 不消失 — T-009 `newMessage.ts:275` 取消注释 + `StreamingService.finalize` 防御 dispatch + 用户 2026-05-21 实测：回复完后 BeatLoader 消失 + 操作栏出现
- **D-008** Branch panel 滚动失效 — 根因 = `RowFlex` 没 `h-full` 高度链断；修 = RowFlex + BranchPane motion.div 加 h-full；用户 2026-05-22 实测 long branch reply 可滚 ✅
- **D-009** 分支 regenerate/edit/delete 模型瞎 — 根因 = `Message.tsx:73 useAssistant(id)` 走 Redux 全局；修 = Option 1 BranchAssistantContext 边界注入；用户 2026-05-22 视觉验证通过 ✅
- **Task 3** MAIN \| BRANCH resizable divider — 镜像 `useSidebarResize` 写右锚 `useBranchPaneResize`；用户 2026-05-22 视觉验证通过（拖动 reflow / 窄宽两端 scroll / 1:1 跟手 / 开关动画）✅

详见 [问题与Debug记录.md](./问题与Debug记录.md) 已 closed 区。

## 🟡 当前唯一未完成动作

**commit D-009 / Task 3 / S6' 代码段 + 撤诊断 + 重写测试**。用户自管 git via GitHub Desktop — 助手**不**执行 `git add` / `git commit`。

### Commit 前清理（下一个对话首要做）

1. **撤所有 `[S6 trace]` 诊断日志**：
   - `MainTextBlock.tsx` —— `[S6 trace] MainTextBlock context read` / `[S6 trace] effect fired`（含 `insideProvider/earlyReturn/selectionStart/End`）/ `[S6 trace] block element`
   - `Chat.tsx` —— `[S6 trace] branchAnchorHighlight set`（Stage 0）
   - `findBlockContext.ts` —— `[S6 trace] findBlockContext`（两处 debug log）
   - `sourceHighlight.ts` —— `[S6 trace] span wrap injected`（paint 末尾）
   - 同时移除 effect 体内 `earlyReturn` 命名变量（恢复为内联条件，控制流等价）；`BRANCH_ANCHOR_DEFAULT` 导出可保留（注释里说明用途从「trace 判别」变为「无 Provider 时的默认值常量」）
   - **保留**：`globalThis.__BRANCH_ANCHOR_CTX_CACHE__ ??=` 单例化（结构性收益、prod 无副作用；删了未来编辑 `BranchAnchorContext.tsx` 会再回到 HMR context 分裂的死循环）
2. **重写 `sourceHighlight.test.ts`**：原 7 用例测的是 `CSS.highlights.set/get/has`，现已切到 span-wrap。改为：
   - capture↔resolve round-trip 保留（结构没变）
   - paint 后 `document.querySelectorAll('span.branch-anchor-highlight').length >= 1` + `firstSpan.textContent === selectedText`
   - clear 后 spans count === 0 + 空壳过滤 === 0
   - 跨节点选区 fixture（`<p>foo<strong>bar</strong>baz</p>`）span 数 >= 2
3. **`MainTextBlock.test.tsx` 复核**：4 高亮用例之前 assert `data-branch-anchored` 属性，现 paint 体里没设这个属性（被 span-wrap 替代）。改为 assert `querySelectorAll('span.branch-anchor-highlight').length`
4. **commit 拆分**（见下方拟 commit 拆分）。每个都 `git commit --signoff`

### 决策史完整记录

D-010 → D-013-CLOSED 完整决策史见 [[问题与Debug记录]] 与 [[tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/README|T-006E README]]。**下一个对话不应该重读这一长串、直接看 T-006E README 顶部的「不要再走的弯路」red list 即可。**


## 🟢 仍 open 但低优先级（用户要求先记录不优先修）

- **D-006** fresh install 默认模型仍 CherryAI Qwen — 等 v2 默认模型策略迁移时一起处理
- **D-007** Regenerate 切到 Ollama 后无反应 — 主聊天侧的候选 issue，与 D-009 分支侧 regenerate 是不同问题（D-009 已修代码，D-007 仍待复测）

## 立即可做（baseline 解除后）

1. **T-006 Text Anchor Branch UI 实施进度**：
   - ✅ T-006B 文本选中捕获 —— commit `d579fdcf2`
   - ✅ T-006C / D-1 —— commit `76ee326a0`
   - ❌ T-006D-2A 跳转式 fork —— 废弃；产品方向错（自动切走 + 模型瞎），代码不 commit
   - ✅ **T-006D-2B Side-by-side branch S1'–S4' 已入库 + S4' 产品闸门通过**：commits `c278f5c0a` (S1') + `e49d1cc0f` (S2'+S3') + `3e2ecec41` (S4')
   - ✅ **D-008 closed**：scroll 高度链断裂修；删 jsdom false-green 单测
   - ✅ **D-009 closed**：regenerate/edit/delete 根因 + Option 1 BranchAssistantContext 实施 + 用户 2026-05-22 视觉验证通过；**代码未 commit（用户自管）**
   - ✅ **Task 3 closed**：MAIN \| BRANCH resizable divider — 镜像 `useSidebarResize` 写右锚 `useBranchPaneResize`；用户 2026-05-22 视觉验证通过；**代码未 commit**
   - ✅ **S6' / T-006E source-passage highlight closed**：最终方案 `<span>` wrap + `replaceWith` 全局清扫 + `globalThis` Context singleton + 关闭路径连通 + cleanup 计数验证通过；**代码未 commit**。完整决策史 D-010 → D-013-CLOSED 见 [[问题与Debug记录]] 与 [[tasks/T-006_TextAnchorBranchUI/T-006E_Highlight/README|T-006E README]]
   - ⏳ **剩余 T-006 功能（未开始）**：
     - **分支折叠** —— 折叠 / 展开主对话里的分支区域
     - **多分支共存** —— 同时打开多个 anchor（不同颜色或编号），扩展 `branchAnchor: BranchAnchor | null` 为 `Map<branchTopicId, BranchAnchor>`，扩展 BranchPane 为多栏
   - ⏳ T-006D-2C-0 流式 disable Ask/Open（与 D-2B 正交，可平行）
   - ⏳ T-006D-2C-1..5 cleanup（含 [[preflight.md|D-2C-5 分支侧 Dexie `db.topics.update(branchTopicId)` 静默 0-rows]] + **path Y / delete-on-close** —— 关闭分支删 SQLite topic 行；当前关闭只清前端 state、留 orphan）
   - ⏳ T-006A 测试数据（可选） / T-006D-3 / T-006F
   - ⏳ **anchor 持久化（v1.1+）**：当前 anchor 只活在 Chat.tsx useState 里，切 topic 离开再回来高亮丢；做了才能挂「点击 span 重开 panel」
2. **找 `DeJeune/ai-service` 分支** —— 看 v2 `SiblingNavigator.tsx` / `MessageGroup.tsx` 是否能合并；不与 T-006 冲突（sibling 维度 vs 文本 anchor 维度）
3. **跑一次端到端发消息** —— 验证 chat completion 路径在 `topic.assistantId = null` 时仍能取到 model（理论上应该走 Redux `defaultAssistant.defaultModel` / 用户 mention，与 topic 字段无关）

## 短期：v2 切换主线

4. **renderer 直接消费 shared 类型** —— 删除 `DataApiMessageDataSource` 桥层
5. **Topic 列表换 `useInfiniteQuery('/topics')`** —— 替代 `useAssistant().topics[]`
6. **消息列表换 `useInfiniteQuery('/topics/:topicId/messages', { query: { includeSiblings: true } })`** —— 同时接 v2 分支数据
7. **引入 / 合并 v2 `MessageGroup` + `SiblingNavigator`**（来自 `DeJeune/ai-service`）
8. **焦点态从 message 迁到 topic**：`PUT /topics/:id/active-node` 替代 `editMessage({ foldSelected })`
9. **Block 删除契约确定**：(A) 补 `PATCH /messages/:id/blocks/:blockId`，或 (B) 固化"块 ops 走 message data 整体替换"为契约
10. **删除 Redux `newMessage` + `messageBlock` slice + `messageThunk`**

## 短期：独立支线

11. **方案 C 实施**（彻底修 default assistant sentinel）：renderer 启动时 `POST /assistants` 创建真助手 → 把 UUID dispatch 到 Redux `defaultAssistant.id`；完成后可以删 T-004 的 `=== 'default'` 判断
12. ~~**诊断 D-003 Ollama 自动模型同步失败**~~ —— ✅ T-007 已诊断完成；剩下的工作 = 用户确认是否应用 1 行 JSON 修复 + fresh install 实测
13. **Agent session 写路径迁 DataApi**（消除 `StreamingService.finalize:216` 的 `isAgentSessionTopicId` 分支）
14. **UI 库迁移**（antd → `@cherrystudio/ui`）
15. **重新加回 `descend` flag**（schema + service + UI 同时改）

## 中期 / 长期

15. **EventEmitter 强类型化**
16. **DataApi 订阅消费**（`DataApi_Subscribe`）
17. **流式 throttle 测量**
18. **全文搜索端点**
19. **renderer 聊天主链路测试覆盖**

## 不要做（红灯）

- ❌ 不要在 v1 路径上修 bug（除非阻塞 v2）
- ❌ 不要手改 codegen 产物
- ❌ 不要给 `migrations/sqlite-drizzle/*.sql` 写 patch migration
- ❌ 不要在新组件里写 `styled-components` / 引入 `antd` / `HeroUI`
- ❌ 不要先删 Redux 再迁 UI（顺序反了会黑屏）
- ❌ 不要绕过 `messageThunk` / `StreamingService` 直接写 DataApi

## 给下一个对话的"上手提示"

接手聊天模块工作，按优先级读这五份：
1. [CLAUDE.md](../../CLAUDE.md) — 工程纪律
2. [02_Architecture/源码地图.md](../02_Architecture/源码地图.md) — 模块全景
3. [02_Architecture/DataApi端点.md](../02_Architecture/DataApi端点.md) — 写/读路径真实分工
4. [02_Architecture/分支对话.md](../02_Architecture/分支对话.md) — 当前分支真正在做的事
5. [01_Project/风险与限制.md](../01_Project/风险与限制.md) — 红黄灯
