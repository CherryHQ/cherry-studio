# P2 资产化设计决策（branch summaries + anchor persistence + reopening）

> 一份给新会话读的「P2 大脑」。P1（多分支共存）已全部收尾并入库（S2d `dd2eac937`）。P2 = 把**已经发生的分支**「资产化」：让结论可见、让锚点和分支在关闭 / 刷新后能找回。
> 关联：[[../产品路线图#P2：资产化 —— 分支摘要 + 锚点持久化/重访]] · [[../P1架构设计草案]] · [[tasks/B6-Upstream_StreamingStatusLifecycleCleanup/README]]
> 状态：**设计决策已拍板（LOCKED）+ 待 recon 回答（OPEN）**；尚未开工。第一个工作单元是**只读 recon**（见文末）。

---

## 0. 范围（写死防越界）

**P2 做**：① 分支摘要（branch summaries）② 锚点持久化（anchor persistence）③ 分支重访 / 可点击重开（branch reopening）。合称「资产化」。

**P2 不做**：整轮对话总结 / Timeline / 标签 / Workspace —— 这些已下放 **P3**（[[../产品路线图#P3：组织与长期结构能力]]）。也不做消息分块 / Branch Map / 搜索（P3）。

**不在 P2**：B2（第三个分支打不开）仍 **deferred / 未复现**，独立 triage，不是 P2 的一部分。

---

## 1. LOCKED 决策（已拍板，按此实现）

### 1.1 锚点重定位（anchor relocation）
- **存什么**：`{ blockId, 块内 RENDERED textContent 的字符 offset start/end }` + 一份 `selectedText` 快照。
- **重开时怎么还原**：先按 offset 重建 `Range`；offset 对不上 → **回退到对 `selectedText` 快照做字符串搜索**；都失败 → **优雅降级**（分支照常打开，只是没有 live 高亮，不报错、不挡路）。
- **强制复用，不另造**：复用现有 `sourceHighlight.ts` 的 **capture↔rebuild offset 表示**，且用**同一参照系 = rendered `textContent`（不是 markdown 源）**。P1 的高亮 capture / repaint 已经是这套（块内字符 offset over flattened text nodes）；持久化只是把这套 offset 落盘 + 读回，**不引入第二套坐标方案**。

### 1.2 存储位置（storage）
- **首选目标**：把锚点（+ 摘要）存进 **topic 的 metadata，经 DataApi PATCH** 落库。
- **⚠ 这是 CONDITIONAL（条件性）**：**仅当 recon 证实** v2 `Topic` 确有一个**可写、能经 DataApi PATCH 写入并读回往返（round-trip）的 free-form JSON metadata 字段**时才走这条。
- **若没有该字段** → 退路二选一：① **session-only**（不持久化，先把重访逻辑做出来、刷新即丢）；② **DataApi 认可的独立 anchor store**（更大、且**碰「不写 patch migration」红线** —— 需要新表 / schema 改动，按 v2 纪律得走 classification/codegen 或正式 schema，谨慎）。
- **铁律**：**不要假设该 metadata 字段存在就动手实现**。recon 没确认前，存储层不落任何代码。

### 1.3 重开（reopen）
- 点击一处**源高亮** → 把**那一个** kept 分支 topic 重新加回**当前 BranchPane**；
- **按分支、点击触发** —— **不**在切 topic 时**自动加载全部 kept 分支**；
- 加回后 **best-effort 重建 live 高亮**（按 1.1 的 offset→快照搜索→降级三级）。

### 1.4 摘要（summaries）
- **先做手动触发**：分支卡片上一个按钮，点了才生成（不自动、不在每条回复后偷偷算）；
- **复用该分支自己的模型**（不另挑模型）；
- **显示在卡片上** → 折叠时也能看到分支结论；
- **与锚点一起持久化**（同一存储决策，见 1.2）。

---

## 2. OPEN 问题（由 recon 回答，回答前不写实现）

| # | 问题 | 为什么挡路 |
|---|---|---|
| Q1 | v2 `Topic` 是否有**可写的 free-form JSON metadata 字段**？能否经 **DataApi PATCH 写入并读回往返**？ | 决定 1.2 走「首选」还是退路；是 P2 能否持久化的总闸 |
| Q2 | capture↔rebuild 的 **offset 代码具体在哪**（`sourceHighlight.ts` 的 capture / repaint + Chat.tsx 捕获路径 + MainTextBlock effect？）？持久化能否**直接复用**其 offset 表示 + flattened-textContent 参照系？ | 1.1 强制复用、不另造；得先定位它在哪、接口长什么样 |
| Q3 | 锚点数组应挂在 **PARENT topic**（主对话 topic）还是**每个 branch topic**？ | 决定数据归属与读取次数 |

**顾问倾向（Q3，未拍板）**：挂 **PARENT topic** —— 一次读就能 ① 重绘该主对话的所有高亮 ② 列出这条对话名下所有 kept 分支。需 recon 确认 parent topic 的 metadata 可写性（同 Q1）后再定。

---

## 3. 第一个工作单元（READ-ONLY recon）

**只读侦察，不写功能代码**。目标 = 回答 §2 的 Q1–Q3，产出一份结论（可回填本文档「OPEN→LOCKED」）：

1. 查 v2 `Topic` schema（`src/main/data/db/schemas/topic.ts`）+ DataApi topics 端点（`PATCH /topics/:id` 的 DTO / handler，`packages/shared/data/api/schemas/topics.ts` · `src/main/data/api/handlers/topics.ts`）：有没有 free-form JSON metadata 字段、PATCH 能不能写它、读回是否往返。
2. 定位 capture↔rebuild offset 代码（`sourceHighlight.ts` + Chat.tsx 捕获 + `MainTextBlock` effect）：offset 表示 + 参照系，评估持久化复用接口。
3. 据 Q1 结果判 1.2 走首选 / session-only / 独立 store（注意红线）。

> recon 完成前：**不动 schema、不写 patch migration、不假设字段存在**。symbol / 行号引用前先 grep 核（[[01_Project/风险与限制#R6|R6]] 行号不可靠）。
