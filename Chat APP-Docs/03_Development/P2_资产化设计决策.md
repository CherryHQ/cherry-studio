# P2 资产化设计决策（branch summaries + anchor persistence + reopening）

> 一份给新会话读的「P2 大脑」。P1（多分支共存）已全部收尾并入库（S2d `dd2eac937`）。P2 = 把**已经发生的分支**「资产化」：让结论可见、让锚点和分支在关闭 / 刷新后能找回。
> 关联：[[../产品路线图#P2：资产化 —— 分支摘要 + 锚点持久化/重访]] · [[../P1架构设计草案]] · [[tasks/B6-Upstream_StreamingStatusLifecycleCleanup/README]]
> 状态：**设计决策已拍板（LOCKED）；Phase A 只读 recon 已完成（2026-06-25），三问全部有解、1.2 存储已裁决**。下一步 = Phase B 实施（建表 + 持久化），尚未开工。recon 结论见 §2、实施切入点见文末。

---

## 0. 范围（写死防越界）

**P2 做**：① 分支摘要（branch summaries）② 锚点持久化（anchor persistence）③ 分支重访 / 可点击重开（branch reopening）。合称「资产化」。

**P2 不做**：整轮对话总结 / Timeline / 标签 / Workspace —— 这些已下放 **P3**（[[../产品路线图#P3：组织与长期结构能力]]）。也不做消息分块 / Branch Map / 搜索（P3）。

**不在 P2**：B2（第三个分支打不开）仍 **deferred / 未复现**，独立 triage，不是 P2 的一部分。

---

## 1. LOCKED 决策（已拍板，按此实现）

### 1.1 锚点重定位（anchor relocation）
- **存什么**：`{ blockId, 块内 RENDERED textContent 的字符 offset start/end }` + 一份 `selectedText` 快照。
- **重开时怎么还原**：先按 offset 重建 `Range`；offset 对不上 → **回退到对 `selectedText` 快照做字符串搜索**；都失败 → **优雅降级**（分支照常打开，只是没有 live 高亮，不报错、不挡路）。
- **强制复用，不另造**：复用现有 `sourceHighlight.ts` 的 **capture↔rebuild offset 表示**，且用**同一参照系 = rendered `textContent`（不是 markdown 源）**。P1 的高亮 capture / repaint 已经是这套（块内字符 offset over flattened text nodes）；持久化只是把这套 offset 落盘 + 读回，**不引入第二套坐标方案**。

### 1.2 存储位置（storage）—— LOCKED（独立持久化表）
- **recon 结论（Phase A，2026-06-25）**：v2 `Topic` **无** free-form JSON metadata 列；`PATCH /topics/:id` 的输入 DTO 是 `TopicSchema.pick(...).partial()`、派生自 `z.strictObject`，对未知键**直接 422 拒绝**（连静默丢弃都不是），且 service 为 column-bound 更新、读回为固定字段序列化——**「既有 topic metadata PATCH 往返」路线已排除**（证据见 §2 Q1）。
- **裁决（Sam，2026-06-25）= 独立持久化表，不给 topic 加 metadata**。新建 **`branch_anchor` 表**，**`parentTopicId` 为主索引**。**目标**：刷新 / 重访 parent topic 时**一次读取**该 topic 名下全部 kept 分支 + 锚点。fork 不带回指（见 §2 Q3），故必须显式存 `branchTopicId` 才能在重访时重新挂回。

**`branch_anchor` 表结构（13 列，LOCKED）**：

| 列 | 类型 / 说明 |
|---|---|
| `id` | 主键 PK |
| `parentTopicId` | **主索引（indexed）** —— 按它一次取全 |
| `branchTopicId` | kept 分支自身 topic id（重挂回用） |
| `messageId` | 源消息 id |
| `blockId` | 源块 id |
| `selectedText` | 选区文本快照 |
| `selectionStart` / `selectionEnd` | 块内字符 offset（渲染 textContent 参照系，§2 Q2） |
| `disposition` | default `'kept'`（**前向扩展、非承重**） |
| `summary` | `TEXT NULL` —— 摘要**并入本表**，不另起表 |
| `summaryUpdatedAt` | `INTEGER NULL` |
| `createdAt` / `updatedAt` | 时间戳 |

- **summary 并入本表**（不另起表），经 **PATCH 更新既有行**。
- **迁移机制（Step 0 已以 CLAUDE.md 原文确认，2026-06-25）**：建新表 = 改 `src/main/data/db/schemas/**` + 跑 `pnpm db:migrations:generate`，是 **CLAUDE.md 获许路径**（第 157 行：`schemas in src/main/data/db/schemas/, migrations via pnpm db:migrations:generate`；第 212 行：schemas `"may change freely"`，迁移产物每次 schema 变更由 `drizzle-kit generate` 重生成）。**「不写 patch migration」红线精确禁的是**：手写一份 patch migration 去"修"开发期 DB drift（第 212 行 `do not author patch migrations to "fix" it`，it = mid-development drift）——**不禁**"改 schema 后重生成迁移"。**故本方案合规**。
- **⚠ 术语来源澄清（Step 0 副产，2026-06-25）**：本项目文档常说的「`src/main/data` 不改既有内部 / 只许 CALL」与「`sourceHighlight.ts` / `BranchAnchorContext.tsx` / `useBranchFork.ts` / `newMessage.ts` 等保护文件清单」，均为**本项目自管的工程纪律**（源自本 P2 文档 + Sam 裁决），**不是 CLAUDE.md 条文**。Step 0 核实：CLAUDE.md 全文**无**此类保护/冻结清单；唯一「禁手改」清单是第 218–223 行的四个 auto-generated 文件（`preferenceSchemas` / `bootConfigSchemas` / `PreferencesMappings` / `BootConfigMappings`，要改走 `classification.json` + regenerate），**与 P2 无关**。**结论：这些纪律依然有效、依然要守——但别把它们当成 CLAUDE.md 律条引用**。
- **路线特性**：本方案为纯**新增文件**，不修改任何既有 `src/main/data` 内部，**不碰保护文件**（保护文件均经其**现有导出 / 公开接口**复用，见 §2 Q2）。此处「不碰保护文件」依据的是上一条所述的**项目纪律**，非 CLAUDE.md。

### 1.3 重开（reopen）
- 点击一处**源高亮** → 把**那一个** kept 分支 topic 重新加回**当前 BranchPane**；
- **按分支、点击触发** —— **不**在切 topic 时**自动加载全部 kept 分支**；
- 加回后 **best-effort 重建 live 高亮**（按 1.1 的 offset→快照搜索→降级三级）。

### 1.4 摘要（summaries）
- **先做手动触发**：分支卡片上一个按钮，点了才生成（不自动、不在每条回复后偷偷算）；
- **复用该分支自己的模型**（不另挑模型）；
- **显示在卡片上** → 折叠时也能看到分支结论；
- **与锚点一起持久化**（同一存储决策，见 1.2）。

---

## 2. recon 已回答（Phase A，2026-06-25，只读侦察 + 对抗核验；证据均经 grep 自证）

| # | 问题 | 结论 |
|---|---|---|
| Q1 | v2 `Topic` 有无可写 free-form JSON metadata 字段？能否经 DataApi PATCH 往返？ | **NO**（对抗核验 CONFIRMED）。`topicTable`（`src/main/data/db/schemas/topic.ts:13-41`，表名单数 `topic`）无 json/metadata 列；PATCH 三重拦截——strict DTO 422 拒绝（`packages/shared/data/api/schemas/topics.ts:44-49` ← `types/topic.ts:18` `z.strictObject`，handler `api/handlers/topics.ts:41`）、`TopicService.update` 仅白名单 4 字段（`TopicService.ts:174-180`）、`rowToTopic` 固定 9 字段读回（`:28-40`）。→ **消解为 1.2「独立表」裁决**。 |
| Q2 | capture↔rebuild offset 代码在哪？持久化能否**只读复用**、不碰保护文件？ | **YES（存既有 `{start,end}` 经既有导出重放，无碰撞）**。代码在 `src/renderer/src/utils/branchAnchor/sourceHighlight.ts`：`captureSelectionOffsets`(L110) / `resolveBranchHighlightRange`(L131) / `paintSourceHighlight`(L314) / `clearSourceHighlight`(L288) **均已导出**；参照系 = 块内字符 offset over 扁平化**渲染 DOM textContent**（非 markdown）。捕获实发于 `SelectionContextMenu.tsx:110`（**非 Chat.tsx**，R6 修正）；接缝 = Chat.tsx state 的 `Branch.source`（`Chat.tsx:110-115`）已是纯数值、可零改动读取。**缺口**：offset 今天**未落库**（`useBranchFork.buildCreateBody` 只发 name/sourceNodeId/assistantId，`useBranchFork.ts:47-58`），持久化为净新增。**仅当**将来要持久化原始扁平帧 / 改 offset 语义才需改 `sourceHighlight.ts` → 届时需 Sam 签字。 |
| Q3 | 锚点挂 **PARENT** 还是每个 **BRANCH** topic？ | **PARENT**（已被 1.2 的 `parentTopicId 主索引`内含）。BranchPane 当前**不读 DB**、`branches` 是 Chat.tsx 临时 state（`Chat.tsx:87`）刷新即丢；topic 表**无 parentId/sourceNodeId/disposition**，fork 是无父指针顶层 topic（`TopicService.create` 仅把 sourceNodeId 设为 fork 自身 activeNodeId，`TopicService.ts:144`）；'kept' 纯内存、只在关闭时消费（`branchDisposition.ts:25-29`）。→ 父侧一读取全，per-branch 反需父索引 + N 读，更差。 |

> 详尽证据链见会话记录中的 recon 报告（每条 file:line 均附确认用 grep）。一处诚实标注：Q2 的对抗核验 agent 返回退化结果（无独立证据），故 Q2 为**单 agent 深证**（证据链完整但未双重确认）——如需可补一轮独立核验。

---

## 3. 进度与下一步切入点

**Phase A 只读 recon —— ✅ 已完成（2026-06-25）**。三问全部有解（§2）、1.2 存储已裁决为独立 `branch_anchor` 表（§1.2）。recon 全程只读、零文件改动。

**下一步 = Phase B 实施**（建表 + 持久化 + 重访接线），尚未开工。切入顺序（**草案，待细化为正式实施计划**）：

1. **Step 0 —— ✅ 已完成（2026-06-25）**：CLAUDE.md 原文已核实——改 schema + `pnpm db:migrations:generate` 为获许路径、全文无保护/冻结清单、未禁在 `src/main/data` 新增文件（详见 §1.2 的「迁移机制」+「术语来源澄清」两条）。
2. 新增 `branch_anchor` schema（列定义见 §1.2）+ `pnpm db:migrations:generate` 重生成迁移；配套 DataApi 端点（读：按 `parentTopicId` 取全；写 / 删：随 Keep / 关闭）。
3. 接线：分支创建 / Keep 时落 `branch_anchor`；切回 parent topic 时一次读取、重建卡片 + 经既有导出重放高亮（§2 Q2）；summary 列与手动摘要（§1.4）。

> 纪律：symbol / 行号引用前先 grep 核（[[01_Project/风险与限制#R6|R6]] 行号不可靠）；**不手写 patch migration**；保护文件只经公开接口复用。
