// Test file for auto-format GitHub Action
// This file intentionally has bad formatting

export function testFunction(a: number, b: number): number {
  return a + b
}

export const badlyFormatted = { foo: 'bar', baz: 123, nested: { key: 'value' } }

function anotherFunction() {
  console.log('bad spacing')
}
