diff --git a/src/ollama-embeddings.cjs b/src/ollama-embeddings.cjs
index efa2d150b6589e98410400f3009f3f2d4b7367d1..be1ab0c5efdf4d03bd83e79babca90a560e3aa59 100644
--- a/src/ollama-embeddings.cjs
+++ b/src/ollama-embeddings.cjs
@@ -1,28 +1,115 @@
 "use strict";
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.OllamaEmbeddings = void 0;
-const ollama_1 = require("@langchain/ollama");
 const embedjs_interfaces_1 = require("@cherrystudio/embedjs-interfaces");
 class OllamaEmbeddings extends embedjs_interfaces_1.BaseEmbeddings {
     model;
+    baseUrl;
+    dimensions;
+    keepAlive;
+    truncate;
+    requestOptions;
+    headers;
     constructor(options) {
         super();
-        this.model = new ollama_1.OllamaEmbeddings({
-            model: options.model,
-            baseUrl: options.baseUrl,
-            keepAlive: options?.keepAlive,
-            requestOptions: options?.requestOptions,
-        });
+        this.model = options.model;
+        this.baseUrl = options.baseUrl;
+        this.dimensions = options.dimensions;
+        this.keepAlive = options?.keepAlive;
+        this.truncate = options?.truncate ?? false;
+        this.requestOptions = options?.requestOptions;
+        this.headers = options?.headers;
     }
     async getDimensions() {
-        const sample = await this.model.embedDocuments(['sample']);
-        return sample[0].length;
+        if (this.dimensions !== void 0) {
+            return this.dimensions;
+        }
+        const embeddings = await this.embedDocuments(['sample']);
+        const sampleEmbedding = embeddings[0];
+        if (!(sampleEmbedding?.length)) {
+            throw new Error('Ollama embedding response did not include an embedding vector');
+        }
+        return sampleEmbedding.length;
     }
     async embedDocuments(texts) {
-        return this.model.embedDocuments(texts);
+        return this.embed(texts);
     }
     async embedQuery(text) {
-        return this.model.embedQuery(text);
+        const embeddings = await this.embed([text]);
+        const embedding = embeddings[0];
+        if (!(embedding?.length)) {
+            throw new Error('Ollama embedding response did not include an embedding vector');
+        }
+        return embedding;
+    }
+    async embed(texts) {
+        try {
+            const response = await this.post('/api/embed', {
+                model: this.model,
+                input: texts.length === 1 ? texts[0] : texts,
+                keep_alive: this.keepAlive,
+                truncate: this.truncate,
+                dimensions: this.dimensions,
+                options: this.requestOptions,
+            });
+            return this.normalizeEmbeddings(response);
+        }
+        catch (currentApiError) {
+            try {
+                return await Promise.all(texts.map((text) => this.embedWithLegacyApi(text)));
+            }
+            catch (legacyApiError) {
+                throw new Error('Ollama embeddings request failed using both /api/embed and legacy /api/embeddings', {
+                    cause: legacyApiError instanceof Error ? legacyApiError : currentApiError,
+                });
+            }
+        }
+    }
+    async embedWithLegacyApi(text) {
+        const response = await this.post('/api/embeddings', {
+            model: this.model,
+            prompt: text,
+            keep_alive: this.keepAlive,
+            options: this.requestOptions,
+        });
+        const embeddings = this.normalizeEmbeddings(response);
+        const embedding = embeddings[0];
+        if (!(embedding?.length)) {
+            throw new Error('Ollama legacy embeddings response did not include an embedding vector');
+        }
+        return embedding;
+    }
+    async post(path, body) {
+        const response = await fetch(new URL(path, this.baseUrl).toString(), {
+            method: 'POST',
+            headers: {
+                'Content-Type': 'application/json',
+                ...this.headers,
+            },
+            body: JSON.stringify(body),
+        });
+        if (!response.ok) {
+            const message = await response.text();
+            throw new Error(message || `Ollama request failed with status ${response.status}`);
+        }
+        return response.json();
+    }
+    normalizeEmbeddings(response) {
+        const currentEmbeddings = response?.embeddings;
+        if (this.isEmbeddingMatrix(currentEmbeddings) && currentEmbeddings.length > 0) {
+            return currentEmbeddings;
+        }
+        const legacyEmbedding = response?.embedding;
+        if (this.isEmbeddingVector(legacyEmbedding) && legacyEmbedding.length > 0) {
+            return [legacyEmbedding];
+        }
+        throw new Error('Ollama embedding response did not include embeddings');
+    }
+    isEmbeddingMatrix(value) {
+        return Array.isArray(value) && value.every((item) => this.isEmbeddingVector(item));
+    }
+    isEmbeddingVector(value) {
+        return Array.isArray(value) && value.every((item) => typeof item === 'number');
     }
 }
 exports.OllamaEmbeddings = OllamaEmbeddings;
diff --git a/src/ollama-embeddings.d.ts b/src/ollama-embeddings.d.ts
index 147a171c32e68dfa9ac245eba53a2c3a86e4a544..3ae93410569eeae2a21877a906aee13266ae77e0 100644
--- a/src/ollama-embeddings.d.ts
+++ b/src/ollama-embeddings.d.ts
@@ -1,15 +1,28 @@
-import { OllamaInput } from '@langchain/ollama';
 import { BaseEmbeddings } from '@cherrystudio/embedjs-interfaces';
 export declare class OllamaEmbeddings extends BaseEmbeddings {
-    private model;
+    private readonly model;
+    private readonly baseUrl;
+    private readonly dimensions?;
+    private readonly keepAlive?;
+    private readonly truncate;
+    private readonly requestOptions?;
+    private readonly headers?;
     constructor(options: {
         model: string;
         baseUrl: string;
-        /** Defaults to "5m" */
-        keepAlive?: string;
-        requestOptions?: Omit<OllamaInput, 'baseUrl' | 'model' | 'format' | 'headers'>;
+        dimensions?: number;
+        keepAlive?: string | number;
+        truncate?: boolean;
+        requestOptions?: Record<string, unknown>;
+        headers?: Record<string, string>;
     });
     getDimensions(): Promise<number>;
     embedDocuments(texts: string[]): Promise<number[][]>;
     embedQuery(text: string): Promise<number[]>;
+    private embed;
+    private embedWithLegacyApi;
+    private post;
+    private normalizeEmbeddings;
+    private isEmbeddingMatrix;
+    private isEmbeddingVector;
 }
diff --git a/src/ollama-embeddings.js b/src/ollama-embeddings.js
index 0902b0942d3f4f8fc8b9c3543b19d91550e7a527..71fa0a04849dc6abba4a7e1145192fca0d2d15ed 100644
--- a/src/ollama-embeddings.js
+++ b/src/ollama-embeddings.js
@@ -1,25 +1,112 @@
-import { OllamaEmbeddings as OllamaEmbedding } from '@langchain/ollama';
 import { BaseEmbeddings } from '@cherrystudio/embedjs-interfaces';
 export class OllamaEmbeddings extends BaseEmbeddings {
     model;
+    baseUrl;
+    dimensions;
+    keepAlive;
+    truncate;
+    requestOptions;
+    headers;
     constructor(options) {
         super();
-        this.model = new OllamaEmbedding({
-            model: options.model,
-            baseUrl: options.baseUrl,
-            keepAlive: options?.keepAlive,
-            requestOptions: options?.requestOptions,
-        });
+        this.model = options.model;
+        this.baseUrl = options.baseUrl;
+        this.dimensions = options.dimensions;
+        this.keepAlive = options?.keepAlive;
+        this.truncate = options?.truncate ?? false;
+        this.requestOptions = options?.requestOptions;
+        this.headers = options?.headers;
     }
     async getDimensions() {
-        const sample = await this.model.embedDocuments(['sample']);
-        return sample[0].length;
+        if (this.dimensions !== void 0) {
+            return this.dimensions;
+        }
+        const embeddings = await this.embedDocuments(['sample']);
+        const sampleEmbedding = embeddings[0];
+        if (!(sampleEmbedding?.length)) {
+            throw new Error('Ollama embedding response did not include an embedding vector');
+        }
+        return sampleEmbedding.length;
     }
     async embedDocuments(texts) {
-        return this.model.embedDocuments(texts);
+        return this.embed(texts);
     }
     async embedQuery(text) {
-        return this.model.embedQuery(text);
+        const embeddings = await this.embed([text]);
+        const embedding = embeddings[0];
+        if (!(embedding?.length)) {
+            throw new Error('Ollama embedding response did not include an embedding vector');
+        }
+        return embedding;
+    }
+    async embed(texts) {
+        try {
+            const response = await this.post('/api/embed', {
+                model: this.model,
+                input: texts.length === 1 ? texts[0] : texts,
+                keep_alive: this.keepAlive,
+                truncate: this.truncate,
+                dimensions: this.dimensions,
+                options: this.requestOptions,
+            });
+            return this.normalizeEmbeddings(response);
+        }
+        catch (currentApiError) {
+            try {
+                return await Promise.all(texts.map((text) => this.embedWithLegacyApi(text)));
+            }
+            catch (legacyApiError) {
+                throw new Error('Ollama embeddings request failed using both /api/embed and legacy /api/embeddings', {
+                    cause: legacyApiError instanceof Error ? legacyApiError : currentApiError,
+                });
+            }
+        }
+    }
+    async embedWithLegacyApi(text) {
+        const response = await this.post('/api/embeddings', {
+            model: this.model,
+            prompt: text,
+            keep_alive: this.keepAlive,
+            options: this.requestOptions,
+        });
+        const embeddings = this.normalizeEmbeddings(response);
+        const embedding = embeddings[0];
+        if (!(embedding?.length)) {
+            throw new Error('Ollama legacy embeddings response did not include an embedding vector');
+        }
+        return embedding;
+    }
+    async post(path, body) {
+        const response = await fetch(new URL(path, this.baseUrl).toString(), {
+            method: 'POST',
+            headers: {
+                'Content-Type': 'application/json',
+                ...this.headers,
+            },
+            body: JSON.stringify(body),
+        });
+        if (!response.ok) {
+            const message = await response.text();
+            throw new Error(message || `Ollama request failed with status ${response.status}`);
+        }
+        return response.json();
+    }
+    normalizeEmbeddings(response) {
+        const currentEmbeddings = response?.embeddings;
+        if (this.isEmbeddingMatrix(currentEmbeddings) && currentEmbeddings.length > 0) {
+            return currentEmbeddings;
+        }
+        const legacyEmbedding = response?.embedding;
+        if (this.isEmbeddingVector(legacyEmbedding) && legacyEmbedding.length > 0) {
+            return [legacyEmbedding];
+        }
+        throw new Error('Ollama embedding response did not include embeddings');
+    }
+    isEmbeddingMatrix(value) {
+        return Array.isArray(value) && value.every((item) => this.isEmbeddingVector(item));
+    }
+    isEmbeddingVector(value) {
+        return Array.isArray(value) && value.every((item) => typeof item === 'number');
     }
 }
 //# sourceMappingURL=ollama-embeddings.js.map
