diff --git a/dist/index.d.mts b/dist/index.d.mts
index 23c5e351eb34a71f5a19576761effd709af3dd9f..cef756bf4ace96878da1fd8a2d4fa5833603b079 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -586,6 +586,7 @@ type TelemetrySettings = {
 type DownloadFunction = (options: Array<{
     url: URL;
     isUrlSupportedByModel: boolean;
+    originalIndex?: number;
 }>) => PromiseLike<Array<{
     data: Uint8Array;
     mediaType: string | undefined;
@@ -612,6 +613,10 @@ interface ReasoningOutput {
  * A generated file.
  */
 interface GeneratedFile {
+    /**
+     * Original provider result index when available.
+     */
+    readonly originalIndex?: number;
     /**
      * File as a base64 encoded string.
      */
@@ -631,9 +636,11 @@ declare class DefaultGeneratedFile implements GeneratedFile {
     private base64Data;
     private uint8ArrayData;
     readonly mediaType: string;
+    readonly originalIndex?: number;
     constructor({ data, mediaType, }: {
         data: string | Uint8Array;
         mediaType: string;
+        originalIndex?: number;
     });
     get base64(): string;
     get uint8Array(): Uint8Array<ArrayBufferLike>;
@@ -5041,7 +5048,7 @@ type GenerateImagePrompt = string | {
  *
  * @returns A result object that contains the generated images.
  */
-declare function generateImage({ model: modelArg, prompt: promptArg, n, maxImagesPerCall, size, aspectRatio, seed, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, }: {
+declare function generateImage({ model: modelArg, prompt: promptArg, n, maxImagesPerCall, size, aspectRatio, seed, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, experimental_download: download, }: {
     /**
      * The image model to use.
      */
@@ -5100,6 +5107,12 @@ declare function generateImage({ model: modelArg, prompt: promptArg, n, maxImage
      * Only applicable for HTTP-based providers.
      */
     headers?: Record<string, string>;
+    /**
+     * Custom download function to use for URLs.
+     *
+     * By default, files are downloaded if the model returns URLs instead of binary data.
+     */
+    experimental_download?: DownloadFunction | undefined;
 }): Promise<GenerateImageResult>;
 
 /**
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 23c5e351eb34a71f5a19576761effd709af3dd9f..cef756bf4ace96878da1fd8a2d4fa5833603b079 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -586,6 +586,7 @@ type TelemetrySettings = {
 type DownloadFunction = (options: Array<{
     url: URL;
     isUrlSupportedByModel: boolean;
+    originalIndex?: number;
 }>) => PromiseLike<Array<{
     data: Uint8Array;
     mediaType: string | undefined;
@@ -612,6 +613,10 @@ interface ReasoningOutput {
  * A generated file.
  */
 interface GeneratedFile {
+    /**
+     * Original provider result index when available.
+     */
+    readonly originalIndex?: number;
     /**
      * File as a base64 encoded string.
      */
@@ -631,9 +636,11 @@ declare class DefaultGeneratedFile implements GeneratedFile {
     private base64Data;
     private uint8ArrayData;
     readonly mediaType: string;
+    readonly originalIndex?: number;
     constructor({ data, mediaType, }: {
         data: string | Uint8Array;
         mediaType: string;
+        originalIndex?: number;
     });
     get base64(): string;
     get uint8Array(): Uint8Array<ArrayBufferLike>;
@@ -5041,7 +5048,7 @@ type GenerateImagePrompt = string | {
  *
  * @returns A result object that contains the generated images.
  */
-declare function generateImage({ model: modelArg, prompt: promptArg, n, maxImagesPerCall, size, aspectRatio, seed, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, }: {
+declare function generateImage({ model: modelArg, prompt: promptArg, n, maxImagesPerCall, size, aspectRatio, seed, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, experimental_download: download, }: {
     /**
      * The image model to use.
      */
@@ -5100,6 +5107,12 @@ declare function generateImage({ model: modelArg, prompt: promptArg, n, maxImage
      * Only applicable for HTTP-based providers.
      */
     headers?: Record<string, string>;
+    /**
+     * Custom download function to use for URLs.
+     *
+     * By default, files are downloaded if the model returns URLs instead of binary data.
+     */
+    experimental_download?: DownloadFunction | undefined;
 }): Promise<GenerateImageResult>;
 
 /**
diff --git a/dist/index.js b/dist/index.js
index 5774837d8961e535a03b2037c58bdcff45cf1916..572b46f9e3654ce6c0426993ec5a50eb675c9d7d 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -3090,12 +3090,14 @@ var import_provider_utils10 = require("@ai-sdk/provider-utils");
 var DefaultGeneratedFile = class {
   constructor({
     data,
-    mediaType
+    mediaType,
+    originalIndex
   }) {
     const isUint8Array = data instanceof Uint8Array;
     this.base64Data = isUint8Array ? void 0 : data;
     this.uint8ArrayData = isUint8Array ? data : void 0;
     this.mediaType = mediaType;
+    this.originalIndex = originalIndex;
   }
   // lazy conversion with caching to avoid unnecessary conversion overhead:
   get base64() {
@@ -8468,6 +8470,16 @@ function createUIMessageStream({
 }
 
 // src/ui-message-stream/read-ui-message-stream.ts
+// PATCHED(cherry): structural-sharing snapshot (mirrors vercel/ai#18692) — structuredClone here
+// was O(message bytes) per chunk, melting the renderer on long fast streams (vercel/ai#18624).
+function cherrySnapshotUIMessage(message) {
+  const snapshot = { ...message, parts: message.parts.map((part) => ({ ...part })) };
+  if ("metadata" in message) {
+    const metadata = message.metadata;
+    snapshot.metadata = Array.isArray(metadata) ? [...metadata] : metadata !== null && typeof metadata === "object" ? { ...metadata } : metadata;
+  }
+  return snapshot;
+}
 function readUIMessageStream({
   message,
   stream,
@@ -8500,7 +8512,7 @@ function readUIMessageStream({
         return job({
           state,
           write: () => {
-            controller == null ? void 0 : controller.enqueue(structuredClone(state.message));
+            controller == null ? void 0 : controller.enqueue(cherrySnapshotUIMessage(state.message));
           }
         });
       },
@@ -9636,6 +9648,14 @@ var DefaultEmbedManyResult = class {
 
 // src/generate-image/generate-image.ts
 var import_provider_utils25 = require("@ai-sdk/provider-utils");
+function toDownloadableImageUrl(value) {
+  try {
+    const url = new URL(value);
+    return url.protocol === "http:" || url.protocol === "https:" ? url : void 0;
+  } catch (invalidUrl) {
+    return void 0;
+  }
+}
 async function generateImage({
   model: modelArg,
   prompt: promptArg,
@@ -9647,7 +9667,8 @@ async function generateImage({
   providerOptions,
   maxRetries: maxRetriesArg,
   abortSignal,
-  headers
+  headers,
+  experimental_download: download2
 }) {
   var _a21, _b;
   const model = resolveImageModel(modelArg);
@@ -9696,21 +9717,68 @@ async function generateImage({
     outputTokens: void 0,
     totalTokens: void 0
   };
+  const downloadFn = download2 != null ? download2 : createDefaultDownloadFunction();
+  let processedImageCount = 0;
   for (const result of results) {
-    images.push(
-      ...result.images.map(
-        (image) => {
-          var _a22;
+    const originalIndexOffset = processedImageCount;
+    processedImageCount += result.images.length;
+    const processedImages = await Promise.all(
+      result.images.map(async (image, index) => {
+        var _a22;
+        if (typeof image === "string" && image.startsWith("data:")) {
+          const { mediaType, base64Content } = splitDataUrl(image);
+          if (mediaType != null && base64Content != null) {
+            return new DefaultGeneratedFile({ data: base64Content, mediaType, originalIndex: originalIndexOffset + index });
+          }
+        }
+        const imageUrl = typeof image === "string" ? toDownloadableImageUrl(image) : void 0;
+        if (imageUrl != null) {
+          let downloadedData = null;
+          try {
+            downloadedData = (await downloadFn([{
+              url: imageUrl,
+              isUrlSupportedByModel: false,
+              originalIndex: originalIndexOffset + index
+            }]))[0];
+          } catch (downloadError) {
+            downloadedData = null;
+          }
+          if (downloadedData != null) {
+            return new DefaultGeneratedFile({
+              data: downloadedData.data,
+              mediaType: downloadedData.mediaType != null ? downloadedData.mediaType : "image/png",
+              originalIndex: originalIndexOffset + index
+            });
+          }
+          // A url we failed to fetch is not image data — dropping it beats persisting the
+          // url string as if it were the base64 bytes.
+          return null;
+        }
+        try {
           return new DefaultGeneratedFile({
             data: image,
             mediaType: (_a22 = detectMediaType({
               data: image,
               signatures: imageMediaTypeSignatures
-            })) != null ? _a22 : "image/png"
+            })) != null ? _a22 : "image/png",
+            originalIndex: originalIndexOffset + index
           });
+        } catch (invalidImageData) {
+          // Neither a fetchable url nor decodable base64 — drop it instead of letting one
+          // malformed entry reject the whole batch.
+          return null;
         }
-      )
+      })
     );
+    const downloadedImages = processedImages.filter((file) => file != null);
+    const droppedImageCount = processedImages.length - downloadedImages.length;
+    if (droppedImageCount > 0) {
+      warnings.push({
+        type: "other",
+        message: `${droppedImageCount} of ${processedImages.length} generated images could not be downloaded and were dropped`
+      });
+    }
+    images.push(...downloadedImages);
     warnings.push(...result.warnings);
     if (result.usage != null) {
       totalUsage = addImageModelUsage(totalUsage, result.usage);
diff --git a/dist/index.mjs b/dist/index.mjs
index 6862f01537bf5c2c5d47772fab0174a99180fdf6..eeef92b5739828eaeeaf7b93b18eebb3f7547d93 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -2991,12 +2991,14 @@ import {
 var DefaultGeneratedFile = class {
   constructor({
     data,
-    mediaType
+    mediaType,
+    originalIndex
   }) {
     const isUint8Array = data instanceof Uint8Array;
     this.base64Data = isUint8Array ? void 0 : data;
     this.uint8ArrayData = isUint8Array ? data : void 0;
     this.mediaType = mediaType;
+    this.originalIndex = originalIndex;
   }
   // lazy conversion with caching to avoid unnecessary conversion overhead:
   get base64() {
@@ -8392,6 +8394,16 @@ function createUIMessageStream({
 }
 
 // src/ui-message-stream/read-ui-message-stream.ts
+// PATCHED(cherry): structural-sharing snapshot (mirrors vercel/ai#18692) — structuredClone here
+// was O(message bytes) per chunk, melting the renderer on long fast streams (vercel/ai#18624).
+function cherrySnapshotUIMessage(message) {
+  const snapshot = { ...message, parts: message.parts.map((part) => ({ ...part })) };
+  if ("metadata" in message) {
+    const metadata = message.metadata;
+    snapshot.metadata = Array.isArray(metadata) ? [...metadata] : metadata !== null && typeof metadata === "object" ? { ...metadata } : metadata;
+  }
+  return snapshot;
+}
 function readUIMessageStream({
   message,
   stream,
@@ -8424,7 +8436,7 @@ function readUIMessageStream({
         return job({
           state,
           write: () => {
-            controller == null ? void 0 : controller.enqueue(structuredClone(state.message));
+            controller == null ? void 0 : controller.enqueue(cherrySnapshotUIMessage(state.message));
           }
         });
       },
@@ -9575,6 +9587,14 @@ import {
   convertBase64ToUint8Array as convertBase64ToUint8Array4,
   withUserAgentSuffix as withUserAgentSuffix5
 } from "@ai-sdk/provider-utils";
+function toDownloadableImageUrl(value) {
+  try {
+    const url = new URL(value);
+    return url.protocol === "http:" || url.protocol === "https:" ? url : void 0;
+  } catch (invalidUrl) {
+    return void 0;
+  }
+}
 async function generateImage({
   model: modelArg,
   prompt: promptArg,
@@ -9586,7 +9606,8 @@ async function generateImage({
   providerOptions,
   maxRetries: maxRetriesArg,
   abortSignal,
-  headers
+  headers,
+  experimental_download: download2
 }) {
   var _a21, _b;
   const model = resolveImageModel(modelArg);
@@ -9635,21 +9656,68 @@ async function generateImage({
     outputTokens: void 0,
     totalTokens: void 0
   };
+  const downloadFn = download2 != null ? download2 : createDefaultDownloadFunction();
+  let processedImageCount = 0;
   for (const result of results) {
-    images.push(
-      ...result.images.map(
-        (image) => {
-          var _a22;
+    const originalIndexOffset = processedImageCount;
+    processedImageCount += result.images.length;
+    const processedImages = await Promise.all(
+      result.images.map(async (image, index) => {
+        var _a22;
+        if (typeof image === "string" && image.startsWith("data:")) {
+          const { mediaType, base64Content } = splitDataUrl(image);
+          if (mediaType != null && base64Content != null) {
+            return new DefaultGeneratedFile({ data: base64Content, mediaType, originalIndex: originalIndexOffset + index });
+          }
+        }
+        const imageUrl = typeof image === "string" ? toDownloadableImageUrl(image) : void 0;
+        if (imageUrl != null) {
+          let downloadedData = null;
+          try {
+            downloadedData = (await downloadFn([{
+              url: imageUrl,
+              isUrlSupportedByModel: false,
+              originalIndex: originalIndexOffset + index
+            }]))[0];
+          } catch (downloadError) {
+            downloadedData = null;
+          }
+          if (downloadedData != null) {
+            return new DefaultGeneratedFile({
+              data: downloadedData.data,
+              mediaType: downloadedData.mediaType != null ? downloadedData.mediaType : "image/png",
+              originalIndex: originalIndexOffset + index
+            });
+          }
+          // A url we failed to fetch is not image data — dropping it beats persisting the
+          // url string as if it were the base64 bytes.
+          return null;
+        }
+        try {
           return new DefaultGeneratedFile({
             data: image,
             mediaType: (_a22 = detectMediaType({
               data: image,
               signatures: imageMediaTypeSignatures
-            })) != null ? _a22 : "image/png"
+            })) != null ? _a22 : "image/png",
+            originalIndex: originalIndexOffset + index
           });
+        } catch (invalidImageData) {
+          // Neither a fetchable url nor decodable base64 — drop it instead of letting one
+          // malformed entry reject the whole batch.
+          return null;
         }
-      )
+      })
     );
+    const downloadedImages = processedImages.filter((file) => file != null);
+    const droppedImageCount = processedImages.length - downloadedImages.length;
+    if (droppedImageCount > 0) {
+      warnings.push({
+        type: "other",
+        message: `${droppedImageCount} of ${processedImages.length} generated images could not be downloaded and were dropped`
+      });
+    }
+    images.push(...downloadedImages);
     warnings.push(...result.warnings);
     if (result.usage != null) {
       totalUsage = addImageModelUsage(totalUsage, result.usage);
