diff --git a/dist/index.d.mts b/dist/index.d.mts
index 877abac47ca04c6724f3811a2b92590b6eeff71b..80cdb7817db3accfb663c7d828b44d7164b49760 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -33,7 +33,7 @@ interface OpenResponsesProviderSettings {
 declare function createOpenResponses(options: OpenResponsesProviderSettings): OpenResponsesProvider;
 
 declare const openResponsesOptionsSchema: _ai_sdk_provider_utils.LazySchema<{
-    reasoningEffort?: "low" | "high" | "none" | "medium" | "xhigh" | null | undefined;
+    reasoningEffort?: string | null | undefined;
     reasoningSummary?: "auto" | "concise" | "detailed" | null | undefined;
 }>;
 type OpenResponsesOptions = InferSchema<typeof openResponsesOptionsSchema>;
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 877abac47ca04c6724f3811a2b92590b6eeff71b..80cdb7817db3accfb663c7d828b44d7164b49760 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -33,7 +33,7 @@ interface OpenResponsesProviderSettings {
 declare function createOpenResponses(options: OpenResponsesProviderSettings): OpenResponsesProvider;
 
 declare const openResponsesOptionsSchema: _ai_sdk_provider_utils.LazySchema<{
-    reasoningEffort?: "low" | "high" | "none" | "medium" | "xhigh" | null | undefined;
+    reasoningEffort?: string | null | undefined;
     reasoningSummary?: "auto" | "concise" | "detailed" | null | undefined;
 }>;
 type OpenResponsesOptions = InferSchema<typeof openResponsesOptionsSchema>;
diff --git a/dist/index.js b/dist/index.js
index 74fedcb995ae1955ba56b5bfd39e3f9785f90385..d3d6bb63de8c8dd2e3f45b8d53f87f1d9aad84d8 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -89,9 +89,19 @@ async function convertToOpenResponsesInput({
       }
       case "assistant": {
         const assistantContent = [];
+        const reasoningItems = [];
         const toolCalls = [];
         for (const part of content) {
           switch (part.type) {
+            case "reasoning": {
+              reasoningItems.push({
+                type: "reasoning",
+                summary: [],
+                content: [{ type: "reasoning_text", text: part.text }],
+                status: "completed"
+              });
+              break;
+            }
             case "text": {
               assistantContent.push({ type: "output_text", text: part.text });
               break;
@@ -108,11 +118,15 @@ async function convertToOpenResponsesInput({
             }
           }
         }
+        for (const reasoningItem of reasoningItems) {
+          input.push(reasoningItem);
+        }
         if (assistantContent.length > 0) {
           input.push({
             type: "message",
             role: "assistant",
-            content: assistantContent
+            content: assistantContent,
+            status: "completed"
           });
         }
         for (const toolCall of toolCalls) {
@@ -241,7 +255,7 @@ var import_v42 = require("zod/v4");
 var openResponsesOptionsSchema = (0, import_provider_utils3.lazySchema)(
   () => (0, import_provider_utils3.zodSchema)(
     import_v42.z.object({
-      reasoningEffort: import_v42.z.enum(["none", "low", "medium", "high", "xhigh"]).nullish(),
+      reasoningEffort: import_v42.z.string().nullish(),
       /**
        * Controls reasoning summary output from the model.
        * Valid values: 'concise', 'detailed', 'auto'.
@@ -305,6 +319,10 @@ var OpenResponsesLanguageModel = class {
       parameters: tool.inputSchema,
       ...tool.strict != null ? { strict: tool.strict } : {}
     }));
+    const providerTools = tools == null ? void 0 : tools.filter((tool) => tool.type === "provider").map((tool) => ({
+      type: tool.name,
+      ...tool.args && typeof tool.args === "object" ? tool.args : {}
+    }));
     const convertedToolChoice = toolChoice == null ? void 0 : toolChoice.type === "tool" ? { type: "function", name: toolChoice.toolName } : toolChoice.type;
     const textFormat = (responseFormat == null ? void 0 : responseFormat.type) === "json" ? {
       type: "json_schema",
@@ -323,6 +341,7 @@ var OpenResponsesLanguageModel = class {
     return {
       body: {
         model: this.modelId,
+        store: false,
         input,
         instructions,
         max_output_tokens: maxOutputTokens,
@@ -338,7 +357,7 @@ var OpenResponsesLanguageModel = class {
             summary: openResponsesOptions.reasoningSummary
           }
         } : void 0,
-        tools: (functionTools == null ? void 0 : functionTools.length) ? functionTools : void 0,
+        tools: (functionTools == null ? void 0 : functionTools.length) || (providerTools == null ? void 0 : providerTools.length) ? [...functionTools != null ? functionTools : [], ...providerTools != null ? providerTools : []] : void 0,
         tool_choice: convertedToolChoice,
         ...textFormat != null && { text: { format: textFormat } }
       },
@@ -497,6 +516,7 @@ var OpenResponsesLanguageModel = class {
       usage.raw = responseUsage;
     };
     let isActiveReasoning = false;
+    let activeReasoningId = null;
     let hasToolCalls = false;
     let finishReason = {
       unified: "other",
@@ -567,6 +587,14 @@ var OpenResponsesLanguageModel = class {
                 id: chunk.item.id
               });
               isActiveReasoning = true;
+              activeReasoningId = chunk.item.id;
+            } else if (chunk.type === "response.reasoning_summary_text.delta") {
+              const summaryChunk = chunk;
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: summaryChunk.item_id,
+                delta: summaryChunk.delta
+              });
             } else if (chunk.type === "response.reasoning_text.delta") {
               const reasoningChunk = chunk;
               controller.enqueue({
@@ -577,6 +605,7 @@ var OpenResponsesLanguageModel = class {
             } else if (chunk.type === "response.output_item.done" && chunk.item.type === "reasoning") {
               controller.enqueue({ type: "reasoning-end", id: chunk.item.id });
               isActiveReasoning = false;
+              activeReasoningId = null;
             } else if (chunk.type === "response.output_item.added" && chunk.item.type === "message") {
               controller.enqueue({ type: "text-start", id: chunk.item.id });
             } else if (chunk.type === "response.output_text.delta") {
@@ -606,8 +635,8 @@ var OpenResponsesLanguageModel = class {
             }
           },
           flush(controller) {
-            if (isActiveReasoning) {
-              controller.enqueue({ type: "reasoning-end", id: "reasoning-0" });
+            if (isActiveReasoning && activeReasoningId != null) {
+              controller.enqueue({ type: "reasoning-end", id: activeReasoningId });
             }
             controller.enqueue({
               type: "finish",
diff --git a/dist/index.mjs b/dist/index.mjs
index c3b70bc3a46afbe82f704939e4b1ccc6d47706ee..e64700aac83619670a4c05e213727ade494f3a61 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -75,9 +75,19 @@ async function convertToOpenResponsesInput({
       }
       case "assistant": {
         const assistantContent = [];
+        const reasoningItems = [];
         const toolCalls = [];
         for (const part of content) {
           switch (part.type) {
+            case "reasoning": {
+              reasoningItems.push({
+                type: "reasoning",
+                summary: [],
+                content: [{ type: "reasoning_text", text: part.text }],
+                status: "completed"
+              });
+              break;
+            }
             case "text": {
               assistantContent.push({ type: "output_text", text: part.text });
               break;
@@ -94,11 +104,15 @@ async function convertToOpenResponsesInput({
             }
           }
         }
+        for (const reasoningItem of reasoningItems) {
+          input.push(reasoningItem);
+        }
         if (assistantContent.length > 0) {
           input.push({
             type: "message",
             role: "assistant",
-            content: assistantContent
+            content: assistantContent,
+            status: "completed"
           });
         }
         for (const toolCall of toolCalls) {
@@ -230,7 +244,7 @@ import { z as z2 } from "zod/v4";
 var openResponsesOptionsSchema = lazySchema2(
   () => zodSchema2(
     z2.object({
-      reasoningEffort: z2.enum(["none", "low", "medium", "high", "xhigh"]).nullish(),
+      reasoningEffort: z2.string().nullish(),
       /**
        * Controls reasoning summary output from the model.
        * Valid values: 'concise', 'detailed', 'auto'.
@@ -294,6 +308,10 @@ var OpenResponsesLanguageModel = class {
       parameters: tool.inputSchema,
       ...tool.strict != null ? { strict: tool.strict } : {}
     }));
+    const providerTools = tools == null ? void 0 : tools.filter((tool) => tool.type === "provider").map((tool) => ({
+      type: tool.name,
+      ...tool.args && typeof tool.args === "object" ? tool.args : {}
+    }));
     const convertedToolChoice = toolChoice == null ? void 0 : toolChoice.type === "tool" ? { type: "function", name: toolChoice.toolName } : toolChoice.type;
     const textFormat = (responseFormat == null ? void 0 : responseFormat.type) === "json" ? {
       type: "json_schema",
@@ -312,6 +330,7 @@ var OpenResponsesLanguageModel = class {
     return {
       body: {
         model: this.modelId,
+        store: false,
         input,
         instructions,
         max_output_tokens: maxOutputTokens,
@@ -327,7 +346,7 @@ var OpenResponsesLanguageModel = class {
             summary: openResponsesOptions.reasoningSummary
           }
         } : void 0,
-        tools: (functionTools == null ? void 0 : functionTools.length) ? functionTools : void 0,
+        tools: (functionTools == null ? void 0 : functionTools.length) || (providerTools == null ? void 0 : providerTools.length) ? [...functionTools != null ? functionTools : [], ...providerTools != null ? providerTools : []] : void 0,
         tool_choice: convertedToolChoice,
         ...textFormat != null && { text: { format: textFormat } }
       },
@@ -486,6 +505,7 @@ var OpenResponsesLanguageModel = class {
       usage.raw = responseUsage;
     };
     let isActiveReasoning = false;
+    let activeReasoningId = null;
     let hasToolCalls = false;
     let finishReason = {
       unified: "other",
@@ -556,6 +576,14 @@ var OpenResponsesLanguageModel = class {
                 id: chunk.item.id
               });
               isActiveReasoning = true;
+              activeReasoningId = chunk.item.id;
+            } else if (chunk.type === "response.reasoning_summary_text.delta") {
+              const summaryChunk = chunk;
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: summaryChunk.item_id,
+                delta: summaryChunk.delta
+              });
             } else if (chunk.type === "response.reasoning_text.delta") {
               const reasoningChunk = chunk;
               controller.enqueue({
@@ -566,6 +594,7 @@ var OpenResponsesLanguageModel = class {
             } else if (chunk.type === "response.output_item.done" && chunk.item.type === "reasoning") {
               controller.enqueue({ type: "reasoning-end", id: chunk.item.id });
               isActiveReasoning = false;
+              activeReasoningId = null;
             } else if (chunk.type === "response.output_item.added" && chunk.item.type === "message") {
               controller.enqueue({ type: "text-start", id: chunk.item.id });
             } else if (chunk.type === "response.output_text.delta") {
@@ -595,8 +624,8 @@ var OpenResponsesLanguageModel = class {
             }
           },
           flush(controller) {
-            if (isActiveReasoning) {
-              controller.enqueue({ type: "reasoning-end", id: "reasoning-0" });
+            if (isActiveReasoning && activeReasoningId != null) {
+              controller.enqueue({ type: "reasoning-end", id: activeReasoningId });
             }
             controller.enqueue({
               type: "finish",
diff --git a/src/responses/convert-to-open-responses-input.ts b/src/responses/convert-to-open-responses-input.ts
index 2e06c35c5d13146dbb42ba9bbe61828bb4bd1a66..1f7024c2c1a572badb43c35f3b0a836df99738f1 100644
--- a/src/responses/convert-to-open-responses-input.ts
+++ b/src/responses/convert-to-open-responses-input.ts
@@ -9,6 +9,7 @@ import type {
   OpenResponsesRequestBody,
   OutputTextContentParam,
   RefusalContentParam,
+  ReasoningItemParam,
 } from './open-responses-api';
 
 export async function convertToOpenResponsesInput({
@@ -80,10 +81,21 @@ export async function convertToOpenResponsesInput({
         const assistantContent: Array<
           OutputTextContentParam | RefusalContentParam
         > = [];
+        const reasoningItems: Array<ReasoningItemParam> = [];
         const toolCalls: Array<FunctionCallItemParam> = [];
 
         for (const part of content) {
           switch (part.type) {
+            case 'reasoning': {
+              reasoningItems.push({
+                type: 'reasoning',
+                summary: [],
+                content: [{ type: 'reasoning_text', text: part.text }],
+                // Ark marks status as required on reasoning input items.
+                status: 'completed',
+              });
+              break;
+            }
             case 'text': {
               assistantContent.push({ type: 'output_text', text: part.text });
               break;
@@ -104,12 +116,19 @@ export async function convertToOpenResponsesInput({
           }
         }
 
+        // Push reasoning as separate items
+        for (const reasoningItem of reasoningItems) {
+          input.push(reasoningItem);
+        }
+
         // Push assistant message with text content if any
         if (assistantContent.length > 0) {
           input.push({
             type: 'message',
             role: 'assistant',
             content: assistantContent,
+            // Ark rejects assistant input items without status (#18253); valid for OpenAI too.
+            status: 'completed',
           });
         }
 
diff --git a/src/responses/open-responses-api.ts b/src/responses/open-responses-api.ts
index 08cd2cde60a6271ba108c4338207db275d03afa0..9a2d31a03f08aca53f3d2004c7754cb6aeae9b1e 100644
--- a/src/responses/open-responses-api.ts
+++ b/src/responses/open-responses-api.ts
@@ -145,6 +145,7 @@ export type ReasoningItemParam = {
   summary: ReasoningSummaryContentParam[];
   content?: unknown;
   encrypted_content?: string;
+  status?: string;
 };
 
 /**
diff --git a/src/responses/open-responses-language-model.ts b/src/responses/open-responses-language-model.ts
index 2650edaed75f2bb981f584f702b5da2894241b70..b8c22a7282bdca9a227834845b46e2cc9a49574b 100644
--- a/src/responses/open-responses-language-model.ts
+++ b/src/responses/open-responses-language-model.ts
@@ -106,6 +106,15 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
         ...(tool.strict != null ? { strict: tool.strict } : {}),
       }));
 
+    // Pass provider-defined tools (e.g. web_search) through verbatim: args are
+    // already wire-shaped for Responses dialects; unknown tools are the caller's contract.
+    const providerTools = tools
+      ?.filter(tool => tool.type === 'provider')
+      .map(tool => ({
+        type: tool.name,
+        ...(tool.args && typeof tool.args === 'object' ? tool.args : {}),
+      }));
+
     // Convert tool choice to the Open Responses format
     const convertedToolChoice: ToolChoiceParam | undefined =
       toolChoice == null
@@ -138,6 +147,8 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
     return {
       body: {
         model: this.modelId,
+        // Pre-migration parity: never opt into server-side response storage.
+        store: false,
         input,
         instructions,
         max_output_tokens: maxOutputTokens,
@@ -157,7 +168,10 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
                 }),
               }
             : undefined,
-        tools: functionTools?.length ? functionTools : undefined,
+        tools:
+          functionTools?.length || providerTools?.length
+            ? [...(functionTools ?? []), ...((providerTools ?? []) as never[])]
+            : undefined,
         tool_choice: convertedToolChoice,
         ...(textFormat != null && { text: { format: textFormat } }),
       },
@@ -338,6 +352,7 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
     };
 
     let isActiveReasoning = false;
+    let activeReasoningId: string | null = null;
     let hasToolCalls = false;
     let finishReason: LanguageModelV3FinishReason = {
       unified: 'other',
@@ -440,6 +455,18 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
                 id: chunk.item.id,
               });
               isActiveReasoning = true;
+              activeReasoningId = chunk.item.id;
+            } else if (
+              (chunk as { type: string }).type ===
+              'response.reasoning_summary_text.delta'
+            ) {
+              // Ark streams reasoning as reasoning_summary_text events.
+              const summaryChunk = chunk as { item_id: string; delta: string };
+              controller.enqueue({
+                type: 'reasoning-delta',
+                id: summaryChunk.item_id,
+                delta: summaryChunk.delta,
+              });
             } else if (
               (chunk as { type: string }).type ===
               'response.reasoning_text.delta'
@@ -459,6 +486,7 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
             ) {
               controller.enqueue({ type: 'reasoning-end', id: chunk.item.id });
               isActiveReasoning = false;
+              activeReasoningId = null;
             }
 
             // Text events
@@ -501,8 +529,8 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
           },
 
           flush(controller) {
-            if (isActiveReasoning) {
-              controller.enqueue({ type: 'reasoning-end', id: 'reasoning-0' });
+            if (isActiveReasoning && activeReasoningId != null) {
+              controller.enqueue({ type: 'reasoning-end', id: activeReasoningId });
             }
 
             controller.enqueue({
diff --git a/src/responses/open-responses-options.ts b/src/responses/open-responses-options.ts
index 296a1de1f1122985d7412d3c28bf7f43ae29b543..2ee5f8c9f72b1e80369d36985f6a4b1456a0c0a4 100644
--- a/src/responses/open-responses-options.ts
+++ b/src/responses/open-responses-options.ts
@@ -8,9 +8,7 @@ import { z } from 'zod/v4';
 export const openResponsesOptionsSchema = lazySchema(() =>
   zodSchema(
     z.object({
-      reasoningEffort: z
-        .enum(['none', 'low', 'medium', 'high', 'xhigh'])
-        .nullish(),
+      reasoningEffort: z.string().nullish(),
       /**
        * Controls reasoning summary output from the model.
        * Valid values: 'concise', 'detailed', 'auto'.
