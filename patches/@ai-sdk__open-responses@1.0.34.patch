diff --git a/dist/index.js b/dist/index.js
index 74fedcb995ae1955ba56b5bfd39e3f9785f90385..83f10a1a770adb5049c62db0e8ba1dfeb980c939 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -89,9 +89,18 @@ async function convertToOpenResponsesInput({
       }
       case "assistant": {
         const assistantContent = [];
+        const reasoningItems = [];
         const toolCalls = [];
         for (const part of content) {
           switch (part.type) {
+            case "reasoning": {
+              reasoningItems.push({
+                type: "reasoning",
+                summary: [],
+                content: [{ type: "reasoning_text", text: part.text }]
+              });
+              break;
+            }
             case "text": {
               assistantContent.push({ type: "output_text", text: part.text });
               break;
@@ -108,6 +117,9 @@ async function convertToOpenResponsesInput({
             }
           }
         }
+        for (const reasoningItem of reasoningItems) {
+          input.push(reasoningItem);
+        }
         if (assistantContent.length > 0) {
           input.push({
             type: "message",
@@ -497,6 +509,7 @@ var OpenResponsesLanguageModel = class {
       usage.raw = responseUsage;
     };
     let isActiveReasoning = false;
+    let activeReasoningId = null;
     let hasToolCalls = false;
     let finishReason = {
       unified: "other",
@@ -567,6 +580,7 @@ var OpenResponsesLanguageModel = class {
                 id: chunk.item.id
               });
               isActiveReasoning = true;
+              activeReasoningId = chunk.item.id;
             } else if (chunk.type === "response.reasoning_text.delta") {
               const reasoningChunk = chunk;
               controller.enqueue({
@@ -577,8 +591,11 @@ var OpenResponsesLanguageModel = class {
             } else if (chunk.type === "response.output_item.done" && chunk.item.type === "reasoning") {
               controller.enqueue({ type: "reasoning-end", id: chunk.item.id });
               isActiveReasoning = false;
+              activeReasoningId = null;
             } else if (chunk.type === "response.output_item.added" && chunk.item.type === "message") {
               controller.enqueue({ type: "text-start", id: chunk.item.id });
+            } else if (chunk.type === "response.output_text.delta" && chunk.item_id === "SSE-Keep-Alive" && chunk["SSE-Keep-Alive"] === true) {
+              return;
             } else if (chunk.type === "response.output_text.delta") {
               controller.enqueue({
                 type: "text-delta",
@@ -606,8 +623,8 @@ var OpenResponsesLanguageModel = class {
             }
           },
           flush(controller) {
-            if (isActiveReasoning) {
-              controller.enqueue({ type: "reasoning-end", id: "reasoning-0" });
+            if (isActiveReasoning && activeReasoningId != null) {
+              controller.enqueue({ type: "reasoning-end", id: activeReasoningId });
             }
             controller.enqueue({
               type: "finish",
diff --git a/dist/index.mjs b/dist/index.mjs
index c3b70bc3a46afbe82f704939e4b1ccc6d47706ee..9636569f9016ffd81929b82531a2b7900329c9fb 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -75,9 +75,18 @@ async function convertToOpenResponsesInput({
       }
       case "assistant": {
         const assistantContent = [];
+        const reasoningItems = [];
         const toolCalls = [];
         for (const part of content) {
           switch (part.type) {
+            case "reasoning": {
+              reasoningItems.push({
+                type: "reasoning",
+                summary: [],
+                content: [{ type: "reasoning_text", text: part.text }]
+              });
+              break;
+            }
             case "text": {
               assistantContent.push({ type: "output_text", text: part.text });
               break;
@@ -94,6 +103,9 @@ async function convertToOpenResponsesInput({
             }
           }
         }
+        for (const reasoningItem of reasoningItems) {
+          input.push(reasoningItem);
+        }
         if (assistantContent.length > 0) {
           input.push({
             type: "message",
@@ -486,6 +498,7 @@ var OpenResponsesLanguageModel = class {
       usage.raw = responseUsage;
     };
     let isActiveReasoning = false;
+    let activeReasoningId = null;
     let hasToolCalls = false;
     let finishReason = {
       unified: "other",
@@ -556,6 +569,7 @@ var OpenResponsesLanguageModel = class {
                 id: chunk.item.id
               });
               isActiveReasoning = true;
+              activeReasoningId = chunk.item.id;
             } else if (chunk.type === "response.reasoning_text.delta") {
               const reasoningChunk = chunk;
               controller.enqueue({
@@ -566,8 +580,11 @@ var OpenResponsesLanguageModel = class {
             } else if (chunk.type === "response.output_item.done" && chunk.item.type === "reasoning") {
               controller.enqueue({ type: "reasoning-end", id: chunk.item.id });
               isActiveReasoning = false;
+              activeReasoningId = null;
             } else if (chunk.type === "response.output_item.added" && chunk.item.type === "message") {
               controller.enqueue({ type: "text-start", id: chunk.item.id });
+            } else if (chunk.type === "response.output_text.delta" && chunk.item_id === "SSE-Keep-Alive" && chunk["SSE-Keep-Alive"] === true) {
+              return;
             } else if (chunk.type === "response.output_text.delta") {
               controller.enqueue({
                 type: "text-delta",
@@ -595,8 +612,8 @@ var OpenResponsesLanguageModel = class {
             }
           },
           flush(controller) {
-            if (isActiveReasoning) {
-              controller.enqueue({ type: "reasoning-end", id: "reasoning-0" });
+            if (isActiveReasoning && activeReasoningId != null) {
+              controller.enqueue({ type: "reasoning-end", id: activeReasoningId });
             }
             controller.enqueue({
               type: "finish",
diff --git a/src/responses/convert-to-open-responses-input.ts b/src/responses/convert-to-open-responses-input.ts
index 2e06c35c5d13146dbb42ba9bbe61828bb4bd1a66..4e36fecc5c4a6afddf54a067db4ab5a96ee84617 100644
--- a/src/responses/convert-to-open-responses-input.ts
+++ b/src/responses/convert-to-open-responses-input.ts
@@ -9,6 +9,7 @@ import type {
   OpenResponsesRequestBody,
   OutputTextContentParam,
   RefusalContentParam,
+  ReasoningItemParam,
 } from './open-responses-api';
 
 export async function convertToOpenResponsesInput({
@@ -80,10 +81,20 @@ export async function convertToOpenResponsesInput({
         const assistantContent: Array<
           OutputTextContentParam | RefusalContentParam
         > = [];
+        const reasoningItems: Array<ReasoningItemParam> = [];
         const toolCalls: Array<FunctionCallItemParam> = [];
 
         for (const part of content) {
           switch (part.type) {
+            case 'reasoning': {
+              // Subset Responses servers replay the chain of thought itself (backport of 2.0.27).
+              reasoningItems.push({
+                type: 'reasoning',
+                summary: [],
+                content: [{ type: 'reasoning_text', text: part.text }],
+              });
+              break;
+            }
             case 'text': {
               assistantContent.push({ type: 'output_text', text: part.text });
               break;
@@ -104,6 +115,10 @@ export async function convertToOpenResponsesInput({
           }
         }
 
+        for (const reasoningItem of reasoningItems) {
+          input.push(reasoningItem);
+        }
+
         // Push assistant message with text content if any
         if (assistantContent.length > 0) {
           input.push({
diff --git a/src/responses/open-responses-language-model.ts b/src/responses/open-responses-language-model.ts
index 2650edaed75f2bb981f584f702b5da2894241b70..af2fed1ef181330385afa93a1adba20f4d87a135 100644
--- a/src/responses/open-responses-language-model.ts
+++ b/src/responses/open-responses-language-model.ts
@@ -338,6 +338,7 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
     };
 
     let isActiveReasoning = false;
+    let activeReasoningId: string | null = null;
     let hasToolCalls = false;
     let finishReason: LanguageModelV3FinishReason = {
       unified: 'other',
@@ -440,6 +441,7 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
                 id: chunk.item.id,
               });
               isActiveReasoning = true;
+              activeReasoningId = chunk.item.id;
             } else if (
               (chunk as { type: string }).type ===
               'response.reasoning_text.delta'
@@ -459,6 +461,7 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
             ) {
               controller.enqueue({ type: 'reasoning-end', id: chunk.item.id });
               isActiveReasoning = false;
+              activeReasoningId = null;
             }
 
             // Text events
@@ -467,6 +470,12 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
               chunk.item.type === 'message'
             ) {
               controller.enqueue({ type: 'text-start', id: chunk.item.id });
+            } else if (
+              chunk.type === 'response.output_text.delta' &&
+              chunk.item_id === 'SSE-Keep-Alive' &&
+              (chunk as Record<string, unknown>)['SSE-Keep-Alive'] === true
+            ) {
+              return;
             } else if (chunk.type === 'response.output_text.delta') {
               controller.enqueue({
                 type: 'text-delta',
@@ -501,8 +510,9 @@ export class OpenResponsesLanguageModel implements LanguageModelV3 {
           },
 
           flush(controller) {
-            if (isActiveReasoning) {
-              controller.enqueue({ type: 'reasoning-end', id: 'reasoning-0' });
+            if (isActiveReasoning && activeReasoningId != null) {
+              // Upstream hardcodes 'reasoning-0'; the step assembler only knows the real id.
+              controller.enqueue({ type: 'reasoning-end', id: activeReasoningId });
             }
 
             controller.enqueue({
