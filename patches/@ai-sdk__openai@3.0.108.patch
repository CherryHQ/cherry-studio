diff --git a/dist/index.js b/dist/index.js
index b03e00b5a13b55f21c6d1f77a2651dd8e9836128..48be355a88ef0915264eb40d76f3f6cabd02a87e 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -989,7 +989,7 @@ var OpenAIChatLanguageModel = class {
       // messages:
       messages
     };
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2002,7 +2002,8 @@ var openaiImageResponseSchema = (0, import_provider_utils12.lazySchema)(
       created: import_v48.z.number().nullish(),
       data: import_v48.z.array(
         import_v48.z.object({
-          b64_json: import_v48.z.string(),
+          b64_json: import_v48.z.string().nullish(),
+          url: import_v48.z.string().nullish(),
           revised_prompt: import_v48.z.string().nullish()
         })
       ),
@@ -2190,7 +2191,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2257,7 +2262,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+        if (typeof item.b64_json === "string") return [item.b64_json];
+        if (typeof item.url === "string") return [item.url];
+        return [];
+      }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3420,9 +3429,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3701,6 +3712,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions == null ? void 0 : providerOptions.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -3997,7 +4014,8 @@ ${text}`,
 }
 var openaiResponsesReasoningProviderOptionsSchema = import_v421.z.object({
   itemId: import_v421.z.string().nullish(),
-  reasoningEncryptedContent: import_v421.z.string().nullish()
+  reasoningEncryptedContent: import_v421.z.string().nullish(),
+  rawReasoningContent: import_v421.z.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4635,6 +4653,11 @@ var openaiResponsesChunkSchema = (0, import_provider_utils28.lazySchema)(
         summary_index: import_v422.z.number(),
         delta: import_v422.z.string()
       }),
+      import_v422.z.object({
+        type: import_v422.z.literal("response.reasoning_text.delta"),
+        item_id: import_v422.z.string(),
+        delta: import_v422.z.string()
+      }),
       import_v422.z.object({
         type: import_v422.z.literal("response.reasoning_summary_part.done"),
         item_id: import_v422.z.string(),
@@ -4729,7 +4752,7 @@ var openaiResponsesResponseSchema = (0, import_provider_utils28.lazySchema)(
                       index: import_v422.z.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -5894,7 +5917,7 @@ var OpenAIResponsesLanguageModel = class {
         }
       }
     };
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -5913,6 +5936,19 @@ var OpenAIResponsesLanguageModel = class {
           });
         }
       }
+      if (this.modelId === "gpt-6-astra") {
+        const includeWithoutLogprobs = baseArgs.include == null ? void 0 : baseArgs.include.filter(
+          (value) => value !== "message.output_text.logprobs"
+        );
+        if (baseArgs.top_logprobs != null || (includeWithoutLogprobs == null ? void 0 : includeWithoutLogprobs.length) !== (baseArgs.include == null ? void 0 : baseArgs.include.length)) {
+          baseArgs.top_logprobs = void 0;
+          baseArgs.include = (includeWithoutLogprobs == null ? void 0 : includeWithoutLogprobs.length) === 0 ? void 0 : includeWithoutLogprobs;
+          warnings.push({
+            type: "other",
+            message: "topLogprobs is not supported for GPT-6 Astra"
+          });
+        }
+      }
     } else {
       if ((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) != null) {
         warnings.push({
@@ -7333,6 +7369,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/index.mjs b/dist/index.mjs
index 8b9547674bc3813a5026dd4e2295c1dc7e2486b9..4c2d43d8b8cded8c52b4ceb2c8c04beb419e7c64 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -985,7 +985,7 @@ var OpenAIChatLanguageModel = class {
       // messages:
       messages
     };
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2033,7 +2033,8 @@ var openaiImageResponseSchema = lazySchema7(
       created: z8.number().nullish(),
       data: z8.array(
         z8.object({
-          b64_json: z8.string(),
+          b64_json: z8.string().nullish(),
+          url: z8.string().nullish(),
           revised_prompt: z8.string().nullish()
         })
       ),
@@ -2224,7 +2225,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2291,7 +2296,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+        if (typeof item.b64_json === "string") return [item.b64_json];
+        if (typeof item.url === "string") return [item.url];
+        return [];
+      }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3518,9 +3527,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3799,6 +3810,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions == null ? void 0 : providerOptions.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -4095,7 +4112,8 @@ ${text}`,
 }
 var openaiResponsesReasoningProviderOptionsSchema = z21.object({
   itemId: z21.string().nullish(),
-  reasoningEncryptedContent: z21.string().nullish()
+  reasoningEncryptedContent: z21.string().nullish(),
+  rawReasoningContent: z21.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4736,6 +4754,11 @@ var openaiResponsesChunkSchema = lazySchema20(
         summary_index: z22.number(),
         delta: z22.string()
       }),
+      z22.object({
+        type: z22.literal("response.reasoning_text.delta"),
+        item_id: z22.string(),
+        delta: z22.string()
+      }),
       z22.object({
         type: z22.literal("response.reasoning_summary_part.done"),
         item_id: z22.string(),
@@ -4830,7 +4853,7 @@ var openaiResponsesResponseSchema = lazySchema20(
                       index: z22.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6000,7 +6023,7 @@ var OpenAIResponsesLanguageModel = class {
         }
       }
     };
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -6019,6 +6042,19 @@ var OpenAIResponsesLanguageModel = class {
           });
         }
       }
+      if (this.modelId === "gpt-6-astra") {
+        const includeWithoutLogprobs = baseArgs.include == null ? void 0 : baseArgs.include.filter(
+          (value) => value !== "message.output_text.logprobs"
+        );
+        if (baseArgs.top_logprobs != null || (includeWithoutLogprobs == null ? void 0 : includeWithoutLogprobs.length) !== (baseArgs.include == null ? void 0 : baseArgs.include.length)) {
+          baseArgs.top_logprobs = void 0;
+          baseArgs.include = (includeWithoutLogprobs == null ? void 0 : includeWithoutLogprobs.length) === 0 ? void 0 : includeWithoutLogprobs;
+          warnings.push({
+            type: "other",
+            message: "topLogprobs is not supported for GPT-6 Astra"
+          });
+        }
+      }
     } else {
       if ((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) != null) {
         warnings.push({
@@ -7439,6 +7475,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/internal/index.js b/dist/internal/index.js
index 9efd568db0982f6fbf2437f31034c25ea2441967..a2c3c7ef0a0c9fbadcd7ae6b5f7f6b9b687bb7c5 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -1024,7 +1024,7 @@ var OpenAIChatLanguageModel = class {
       // messages:
       messages
     };
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2037,7 +2037,8 @@ var openaiImageResponseSchema = (0, import_provider_utils12.lazySchema)(
       created: import_v48.z.number().nullish(),
       data: import_v48.z.array(
         import_v48.z.object({
-          b64_json: import_v48.z.string(),
+          b64_json: import_v48.z.string().nullish(),
+          url: import_v48.z.string().nullish(),
           revised_prompt: import_v48.z.string().nullish()
         })
       ),
@@ -2225,7 +2226,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2292,7 +2297,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+        if (typeof item.b64_json === "string") return [item.b64_json];
+        if (typeof item.url === "string") return [item.url];
+        return [];
+      }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3364,9 +3373,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3645,6 +3656,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions == null ? void 0 : providerOptions.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -3941,7 +3958,8 @@ ${text}`,
 }
 var openaiResponsesReasoningProviderOptionsSchema = import_v417.z.object({
   itemId: import_v417.z.string().nullish(),
-  reasoningEncryptedContent: import_v417.z.string().nullish()
+  reasoningEncryptedContent: import_v417.z.string().nullish(),
+  rawReasoningContent: import_v417.z.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4579,6 +4597,11 @@ var openaiResponsesChunkSchema = (0, import_provider_utils26.lazySchema)(
         summary_index: import_v418.z.number(),
         delta: import_v418.z.string()
       }),
+      import_v418.z.object({
+        type: import_v418.z.literal("response.reasoning_text.delta"),
+        item_id: import_v418.z.string(),
+        delta: import_v418.z.string()
+      }),
       import_v418.z.object({
         type: import_v418.z.literal("response.reasoning_summary_part.done"),
         item_id: import_v418.z.string(),
@@ -4673,7 +4696,7 @@ var openaiResponsesResponseSchema = (0, import_provider_utils26.lazySchema)(
                       index: import_v418.z.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6162,7 +6185,7 @@ var OpenAIResponsesLanguageModel = class {
         }
       }
     };
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -6181,6 +6204,19 @@ var OpenAIResponsesLanguageModel = class {
           });
         }
       }
+      if (this.modelId === "gpt-6-astra") {
+        const includeWithoutLogprobs = baseArgs.include == null ? void 0 : baseArgs.include.filter(
+          (value) => value !== "message.output_text.logprobs"
+        );
+        if (baseArgs.top_logprobs != null || (includeWithoutLogprobs == null ? void 0 : includeWithoutLogprobs.length) !== (baseArgs.include == null ? void 0 : baseArgs.include.length)) {
+          baseArgs.top_logprobs = void 0;
+          baseArgs.include = (includeWithoutLogprobs == null ? void 0 : includeWithoutLogprobs.length) === 0 ? void 0 : includeWithoutLogprobs;
+          warnings.push({
+            type: "other",
+            message: "topLogprobs is not supported for GPT-6 Astra"
+          });
+        }
+      }
     } else {
       if ((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) != null) {
         warnings.push({
@@ -7601,6 +7637,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index f30f5bf738a42621a44d64a1beb6e363d891e17e..a99bd120a03e1d2ba8273b8876402984a2755bf9 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -977,7 +977,7 @@ var OpenAIChatLanguageModel = class {
       // messages:
       messages
     };
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2025,7 +2025,8 @@ var openaiImageResponseSchema = lazySchema7(
       created: z8.number().nullish(),
       data: z8.array(
         z8.object({
-          b64_json: z8.string(),
+          b64_json: z8.string().nullish(),
+          url: z8.string().nullish(),
           revised_prompt: z8.string().nullish()
         })
       ),
@@ -2216,7 +2217,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2283,7 +2288,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+        if (typeof item.b64_json === "string") return [item.b64_json];
+        if (typeof item.url === "string") return [item.url];
+        return [];
+      }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3409,9 +3418,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3690,6 +3701,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions == null ? void 0 : providerOptions.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -3986,7 +4003,8 @@ ${text}`,
 }
 var openaiResponsesReasoningProviderOptionsSchema = z17.object({
   itemId: z17.string().nullish(),
-  reasoningEncryptedContent: z17.string().nullish()
+  reasoningEncryptedContent: z17.string().nullish(),
+  rawReasoningContent: z17.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4627,6 +4645,11 @@ var openaiResponsesChunkSchema = lazySchema16(
         summary_index: z18.number(),
         delta: z18.string()
       }),
+      z18.object({
+        type: z18.literal("response.reasoning_text.delta"),
+        item_id: z18.string(),
+        delta: z18.string()
+      }),
       z18.object({
         type: z18.literal("response.reasoning_summary_part.done"),
         item_id: z18.string(),
@@ -4721,7 +4744,7 @@ var openaiResponsesResponseSchema = lazySchema16(
                       index: z18.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6243,7 +6266,7 @@ var OpenAIResponsesLanguageModel = class {
         }
       }
     };
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -6262,6 +6285,19 @@ var OpenAIResponsesLanguageModel = class {
           });
         }
       }
+      if (this.modelId === "gpt-6-astra") {
+        const includeWithoutLogprobs = baseArgs.include == null ? void 0 : baseArgs.include.filter(
+          (value) => value !== "message.output_text.logprobs"
+        );
+        if (baseArgs.top_logprobs != null || (includeWithoutLogprobs == null ? void 0 : includeWithoutLogprobs.length) !== (baseArgs.include == null ? void 0 : baseArgs.include.length)) {
+          baseArgs.top_logprobs = void 0;
+          baseArgs.include = (includeWithoutLogprobs == null ? void 0 : includeWithoutLogprobs.length) === 0 ? void 0 : includeWithoutLogprobs;
+          warnings.push({
+            type: "other",
+            message: "topLogprobs is not supported for GPT-6 Astra"
+          });
+        }
+      }
     } else {
       if ((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) != null) {
         warnings.push({
@@ -7682,6 +7718,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/src/chat/openai-chat-language-model.ts b/src/chat/openai-chat-language-model.ts
index a6ae37a1246ca489643aaa7b305c0528f259382d..30641c6bc4dc883e872df072b16672167e3ce03a 100644
--- a/src/chat/openai-chat-language-model.ts
+++ b/src/chat/openai-chat-language-model.ts
@@ -181,7 +181,7 @@ export class OpenAIChatLanguageModel implements LanguageModelV3 {
 
     // remove unsupported settings for reasoning models
     // see https://platform.openai.com/docs/guides/reasoning#limitations
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       // when reasoning effort is none, gpt-5.1 models allow temperature, topP, logprobs
       //  https://platform.openai.com/docs/guides/latest-model#gpt-5-1-parameter-compatibility
       if (
diff --git a/src/image/openai-image-api.ts b/src/image/openai-image-api.ts
index de779ca46aa2102cc5bef4a733ef6b6807260731..14b724785e15ab95f4cd2ff4d35d35c2ca7605c7 100644
--- a/src/image/openai-image-api.ts
+++ b/src/image/openai-image-api.ts
@@ -9,7 +9,8 @@ export const openaiImageResponseSchema = lazySchema(() =>
       created: z.number().nullish(),
       data: z.array(
         z.object({
-          b64_json: z.string(),
+          b64_json: z.string().nullish(),
+          url: z.string().nullish(),
           revised_prompt: z.string().nullish(),
         }),
       ),
diff --git a/src/image/openai-image-model.ts b/src/image/openai-image-model.ts
index 41b81b27ccc1cd2e821573dab1e28517def1ea95..0591144f6ecd72c88606969157d5b1effff1b70d 100644
--- a/src/image/openai-image-model.ts
+++ b/src/image/openai-image-model.ts
@@ -132,7 +132,11 @@ export class OpenAIImageModel implements ImageModelV3 {
       });
 
       return {
-        images: response.data.map(item => item.b64_json),
+        images: response.data.flatMap(item => {
+          if (typeof item.b64_json === 'string') return [item.b64_json];
+          if (typeof item.url === 'string') return [item.url];
+          return [];
+        }),
         warnings,
         usage:
           response.usage != null
@@ -207,7 +211,11 @@ export class OpenAIImageModel implements ImageModelV3 {
     });
 
     return {
-      images: response.data.map(item => item.b64_json),
+      images: response.data.flatMap(item => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
       warnings,
       usage:
         response.usage != null
diff --git a/src/responses/convert-to-openai-responses-input.ts b/src/responses/convert-to-openai-responses-input.ts
index 5c27d8f988a37082af53de78c0b542a5b8373eed..c5f0bbf159d628a7d8b294a7d7e4c4ee8bf71462 100644
--- a/src/responses/convert-to-openai-responses-input.ts
+++ b/src/responses/convert-to-openai-responses-input.ts
@@ -514,9 +514,14 @@ export async function convertToOpenAIResponsesInput({
               }
 
               input.push({
+                // Ark rejects assistant input items that omit either field (#18253);
+                // both are the canonical Responses shape OpenAI accepts. Only the
+                // assistant item needs it — Ark takes user/system items as-is.
+                type: 'message',
                 role: 'assistant',
                 content: [{ type: 'output_text', text: part.text }],
                 id,
+                status: 'completed',
                 ...(phase != null && { phase }),
               });
 
@@ -955,6 +960,17 @@ export async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts,
                   });
+                } else if (
+                  providerOptions?.rawReasoningContent &&
+                  part.text.length > 0
+                ) {
+                  // Thinking dialects (DeepSeek, …) require the reasoning text itself on
+                  // replay; opt-in because OpenAI rejects `content` on reasoning items.
+                  input.push({
+                    type: 'reasoning',
+                    summary: [],
+                    content: [{ type: 'reasoning_text', text: part.text }],
+                  });
                 } else {
                   warnings.push({
                     type: 'other',
@@ -1335,6 +1351,7 @@ export async function convertToOpenAIResponsesInput({
 const openaiResponsesReasoningProviderOptionsSchema = z.object({
   itemId: z.string().nullish(),
   reasoningEncryptedContent: z.string().nullish(),
+  rawReasoningContent: z.boolean().nullish(),
 });
 
 export type OpenAIResponsesReasoningProviderOptions = z.infer<
diff --git a/src/responses/openai-responses-api.ts b/src/responses/openai-responses-api.ts
index 18412d98b2ba411731b4df1df33186026088c626..bd49ea85badbd884dce86dd9afc1b37f50f55dd5 100644
--- a/src/responses/openai-responses-api.ts
+++ b/src/responses/openai-responses-api.ts
@@ -1198,6 +1198,11 @@ export const openaiResponsesChunkSchema = lazySchema(() =>
         summary_index: z.number(),
         delta: z.string(),
       }),
+      z.object({
+        type: z.literal('response.reasoning_text.delta'),
+        item_id: z.string(),
+        delta: z.string(),
+      }),
       z.object({
         type: z.literal('response.reasoning_summary_part.done'),
         item_id: z.string(),
@@ -1319,7 +1324,7 @@ export const openaiResponsesResponseSchema = lazySchema(() =>
                         index: z.number(),
                       }),
                     ]),
-                  ),
+                  ).nullish().transform((v) => v ?? []),
                 }),
               ),
             }),
diff --git a/src/responses/openai-responses-language-model.ts b/src/responses/openai-responses-language-model.ts
index e0f5f9f90c2813c7b5f850fa41207ba1b87382fd..78757018a349b6fffed1d20cb76c6e0b99778632 100644
--- a/src/responses/openai-responses-language-model.ts
+++ b/src/responses/openai-responses-language-model.ts
@@ -384,7 +384,7 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
 
     // remove unsupported settings for reasoning models
     // see https://platform.openai.com/docs/guides/reasoning#limitations
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       // when reasoning effort is none, gpt-5.1 models allow temperature, topP, logprobs
       //  https://platform.openai.com/docs/guides/latest-model#gpt-5-1-parameter-compatibility
       if (
@@ -411,6 +411,20 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
           });
         }
       }
+
+      if (this.modelId === 'gpt-6-astra') {
+        const includeWithoutLogprobs = baseArgs.include?.filter(
+          value => value !== 'message.output_text.logprobs',
+        );
+        if (baseArgs.top_logprobs != null || includeWithoutLogprobs?.length !== baseArgs.include?.length) {
+          baseArgs.top_logprobs = undefined;
+          baseArgs.include = includeWithoutLogprobs?.length === 0 ? undefined : includeWithoutLogprobs;
+          warnings.push({
+            type: 'other',
+            message: 'topLogprobs is not supported for GPT-6 Astra',
+          });
+        }
+      }
     } else {
       if (openaiOptions?.reasoningEffort != null) {
         warnings.push({
@@ -2171,6 +2185,17 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
                   } satisfies ResponsesReasoningProviderMetadata,
                 },
               });
+            } else if (value.type === 'response.reasoning_text.delta') {
+              controller.enqueue({
+                type: 'reasoning-delta',
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id,
+                  } satisfies ResponsesReasoningProviderMetadata,
+                },
+              });
             } else if (value.type === 'response.reasoning_summary_part.done') {
               // when OpenAI stores the message data, we can immediately conclude the reasoning part
               // since we do not need to send the encrypted content.
