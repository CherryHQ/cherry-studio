diff --git a/dist/index.js b/dist/index.js
index ea415b862b2c36c3771b52ed8cd4fe88a784a46a..87cd45229f41eec385299f31928f3289a40ac8ee 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -257,7 +257,11 @@ function convertToOpenAICompatibleChatMessages(prompt) {
         messages.push({
           role: "assistant",
           content: toolCalls.length > 0 ? text || null : text,
-          ...reasoning.length > 0 ? { reasoning_content: reasoning } : {},
+          // Thinking-mode dialects (DeepSeek, GLM, Kimi, MiniMax, …) reject a request whose
+          // assistant turns dropped `reasoning_content` once one performed a tool call. Send the
+          // key unconditionally — an empty string when the turn produced none, matching
+          // `@ai-sdk/deepseek`; the field is ignored when the turn did not need it.
+          reasoning_content: reasoning,
           tool_calls: toolCalls.length > 0 ? toolCalls : void 0,
           ...metadata
         });
@@ -599,6 +603,27 @@ var OpenAICompatibleChatLanguageModel = class {
         text: reasoning
       });
     }
+    for (const annotation of choice.message.annotations || []) {
+      if (annotation?.type !== "url_citation" || typeof annotation.url_citation?.url !== "string" || !URL.canParse(annotation.url_citation.url) || !["http:", "https:"].includes(new URL(annotation.url_citation.url).protocol)) continue;
+      content.push({
+        type: "source",
+        sourceType: "url",
+        id: (0, import_provider_utils2.generateId)(),
+        url: annotation.url_citation.url,
+        title: typeof annotation.url_citation.title === "string" ? annotation.url_citation.title : void 0
+      });
+    }
+    if (choice.message.images) {
+      for (const image of choice.message.images) {
+        const match1 = image.image_url.url.match(/^data:([^;]+)/);
+        const match2 = image.image_url.url.match(/^data:[^;]*;base64,(.+)$/);
+        content.push({
+          type: 'file',
+          mediaType: match1 ? (match1[1] ?? 'image/jpeg') : 'image/jpeg',
+          data: match2 ? match2[1] : image.image_url.url,
+        });
+      }
+    }
     if (choice.message.tool_calls != null) {
       for (const toolCall of choice.message.tool_calls) {
         const thoughtSignature = (_c = (_b = toolCall.extra_content) == null ? void 0 : _b.google) == null ? void 0 : _c.thought_signature;
@@ -758,6 +783,17 @@ var OpenAICompatibleChatLanguageModel = class {
                 delta: delta.content
               });
             }
+            if (delta.images) {
+              for (const image of delta.images) {
+                const match1 = image.image_url.url.match(/^data:([^;]+)/);
+                const match2 = image.image_url.url.match(/^data:[^;]*;base64,(.+)$/);
+                controller.enqueue({
+                  type: 'file',
+                  mediaType: match1 ? (match1[1] ?? 'image/jpeg') : 'image/jpeg',
+                  data: match2 ? match2[1] : image.image_url.url,
+                });
+              }
+            }
             if (delta.tool_calls != null) {
               if (isActiveReasoning) {
                 controller.enqueue({
@@ -871,6 +907,16 @@ var OpenAICompatibleChatLanguageModel = class {
                 });
               }
             }
+            for (const annotation of delta.annotations || []) {
+              if (annotation?.type !== "url_citation" || typeof annotation.url_citation?.url !== "string" || !URL.canParse(annotation.url_citation.url) || !["http:", "https:"].includes(new URL(annotation.url_citation.url).protocol)) continue;
+              controller.enqueue({
+                type: "source",
+                sourceType: "url",
+                id: (0, import_provider_utils2.generateId)(),
+                url: annotation.url_citation.url,
+                title: typeof annotation.url_citation.title === "string" ? annotation.url_citation.title : void 0
+              });
+            }
           },
           flush(controller) {
             var _a2, _b, _c, _d, _e;
@@ -969,6 +1015,7 @@ var OpenAICompatibleChatResponseSchema = import_v43.z.looseObject({
         content: import_v43.z.string().nullish(),
         reasoning_content: import_v43.z.string().nullish(),
         reasoning: import_v43.z.string().nullish(),
+        annotations: import_v43.z.array(import_v43.z.unknown()).nullish().catch([]),
         tool_calls: import_v43.z.array(
           import_v43.z.object({
             id: import_v43.z.string().nullish(),
@@ -983,6 +1030,14 @@ var OpenAICompatibleChatResponseSchema = import_v43.z.looseObject({
               }).nullish()
             }).nullish()
           })
+        ).nullish(),
+        images: import_v43.z.array(
+          import_v43.z.object({
+            type: import_v43.z.literal('image_url'),
+            image_url: import_v43.z.object({
+              url: import_v43.z.string(),
+            })
+          })
         ).nullish()
       }),
       finish_reason: import_v43.z.string().nullish()
@@ -1003,6 +1058,7 @@ var chunkBaseSchema = import_v43.z.looseObject({
         // providers serving `gpt-oss` set `reasoning`. See #7866
         reasoning_content: import_v43.z.string().nullish(),
         reasoning: import_v43.z.string().nullish(),
+        annotations: import_v43.z.array(import_v43.z.unknown()).nullish().catch([]),
         tool_calls: import_v43.z.array(
           import_v43.z.object({
             index: import_v43.z.number().nullish(),
@@ -1019,6 +1075,14 @@ var chunkBaseSchema = import_v43.z.looseObject({
               }).nullish()
             }).nullish()
           })
+        ).nullish(),
+        images: import_v43.z.array(
+          import_v43.z.object({
+            type: import_v43.z.literal('image_url'),
+            image_url: import_v43.z.object({
+              url: import_v43.z.string(),
+            })
+          })
         ).nullish()
       }).nullish(),
       finish_reason: import_v43.z.string().nullish()
@@ -1577,7 +1641,7 @@ var OpenAICompatibleEmbeddingModel = class {
     return {
       warnings,
       embeddings: response.data.map((item) => item.embedding),
-      usage: response.usage ? { tokens: response.usage.prompt_tokens } : void 0,
+      usage: response.usage ? { tokens: response.usage.prompt_tokens ?? response.usage.total_tokens } : void 0,
       providerMetadata: response.providerMetadata,
       response: { headers: responseHeaders, body: rawValue }
     };
@@ -1585,13 +1649,31 @@ var OpenAICompatibleEmbeddingModel = class {
 };
 var openaiTextEmbeddingResponseSchema = import_v47.z.object({
   data: import_v47.z.array(import_v47.z.object({ embedding: import_v47.z.array(import_v47.z.number()) })),
-  usage: import_v47.z.object({ prompt_tokens: import_v47.z.number() }).nullish(),
+  usage: import_v47.z.object({ prompt_tokens: import_v47.z.number().nullish(), total_tokens: import_v47.z.number().nullish() }).nullish(),
   providerMetadata: import_v47.z.record(import_v47.z.string(), import_v47.z.record(import_v47.z.string(), import_v47.z.any())).optional()
 });
 
 // src/image/openai-compatible-image-model.ts
 var import_provider_utils5 = require("@ai-sdk/provider-utils");
 var import_v48 = require("zod/v4");
+var defaultResponseFormatPrefixes = [
+  "chatgpt-image-",
+  "gpt-image-1-mini",
+  "gpt-image-1.5",
+  "gpt-image-1",
+  "gpt-image-2"
+];
+function hasDefaultResponseFormat(modelId) {
+  return defaultResponseFormatPrefixes.some((prefix) => modelId.startsWith(prefix));
+}
+function isResponseFormatRejection(error) {
+  if (typeof error !== "object" || error == null) return false;
+  const { statusCode, message, responseBody } = error;
+  if (statusCode !== 400 && statusCode !== 422) return false;
+  return [message, responseBody].some(
+    (detail) => typeof detail === "string" && /response(?:_|\s|-)?format/i.test(detail)
+  );
+}
 var OpenAICompatibleImageModel = class {
   constructor(modelId, config) {
     this.modelId = modelId;
@@ -1667,7 +1749,11 @@ var OpenAICompatibleImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === 'string') return [item.b64_json];
+          if (typeof item.url === 'string') return [item.url];
+          return [];
+        }),
         warnings,
         response: {
           timestamp: currentDate,
@@ -1676,7 +1762,7 @@ var OpenAICompatibleImageModel = class {
         }
       };
     }
-    const { value: response, responseHeaders } = await (0, import_provider_utils5.postJsonToApi)({
+    const postImageRequest = (responseFormat) => (0, import_provider_utils5.postJsonToApi)({
       url: this.config.url({
         path: "/images/generations",
         modelId: this.modelId
@@ -1687,8 +1773,8 @@ var OpenAICompatibleImageModel = class {
         prompt,
         n,
         size,
-        ...args,
-        response_format: "b64_json"
+        ...(responseFormat != null ? { response_format: responseFormat } : {}),
+        ...args
       },
       failedResponseHandler: (0, import_provider_utils5.createJsonErrorResponseHandler)(
         (_e = this.config.errorStructure) != null ? _e : defaultOpenAICompatibleErrorStructure
@@ -1699,8 +1785,25 @@ var OpenAICompatibleImageModel = class {
       abortSignal,
       fetch: this.config.fetch
     });
+    const defaultResponseFormat = hasDefaultResponseFormat(this.modelId) || args.response_format !== void 0 ? null : "b64_json";
+    let posted;
+    try {
+      posted = await postImageRequest(defaultResponseFormat);
+    } catch (error) {
+      if (defaultResponseFormat == null || !isResponseFormatRejection(error)) throw error;
+      try {
+        posted = await postImageRequest(null);
+      } catch {
+        throw error;
+      }
+    }
+    const { value: response, responseHeaders } = posted;
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
       warnings,
       response: {
         timestamp: currentDate,
@@ -1711,7 +1814,7 @@ var OpenAICompatibleImageModel = class {
   }
 };
 var openaiCompatibleImageResponseSchema = import_v48.z.object({
-  data: import_v48.z.array(import_v48.z.object({ b64_json: import_v48.z.string() }))
+  data: import_v48.z.array(import_v48.z.object({ b64_json: import_v48.z.string().nullish(), url: import_v48.z.string().nullish() }))
 });
 async function fileToBlob(file) {
   if (file.type === "url") {
diff --git a/dist/index.mjs b/dist/index.mjs
index 66ab6a1aeeb1c4c72a81136a042052e50b55ba4b..30232e97e04389ea1aeb4bee9b1fd9ae6f7fa4b1 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -241,7 +241,11 @@ function convertToOpenAICompatibleChatMessages(prompt) {
         messages.push({
           role: "assistant",
           content: toolCalls.length > 0 ? text || null : text,
-          ...reasoning.length > 0 ? { reasoning_content: reasoning } : {},
+          // Thinking-mode dialects (DeepSeek, GLM, Kimi, MiniMax, …) reject a request whose
+          // assistant turns dropped `reasoning_content` once one performed a tool call. Send the
+          // key unconditionally — an empty string when the turn produced none, matching
+          // `@ai-sdk/deepseek`; the field is ignored when the turn did not need it.
+          reasoning_content: reasoning,
           tool_calls: toolCalls.length > 0 ? toolCalls : void 0,
           ...metadata
         });
@@ -585,6 +589,27 @@ var OpenAICompatibleChatLanguageModel = class {
         text: reasoning
       });
     }
+    for (const annotation of choice.message.annotations || []) {
+      if (annotation?.type !== "url_citation" || typeof annotation.url_citation?.url !== "string" || !URL.canParse(annotation.url_citation.url) || !["http:", "https:"].includes(new URL(annotation.url_citation.url).protocol)) continue;
+      content.push({
+        type: "source",
+        sourceType: "url",
+        id: generateId(),
+        url: annotation.url_citation.url,
+        title: typeof annotation.url_citation.title === "string" ? annotation.url_citation.title : void 0
+      });
+    }
+    if (choice.message.images) {
+      for (const image of choice.message.images) {
+        const match1 = image.image_url.url.match(/^data:([^;]+)/);
+        const match2 = image.image_url.url.match(/^data:[^;]*;base64,(.+)$/);
+        content.push({
+          type: 'file',
+          mediaType: match1 ? (match1[1] ?? 'image/jpeg') : 'image/jpeg',
+          data: match2 ? match2[1] : image.image_url.url,
+        });
+      }
+    }
     if (choice.message.tool_calls != null) {
       for (const toolCall of choice.message.tool_calls) {
         const thoughtSignature = (_c = (_b = toolCall.extra_content) == null ? void 0 : _b.google) == null ? void 0 : _c.thought_signature;
@@ -744,6 +769,17 @@ var OpenAICompatibleChatLanguageModel = class {
                 delta: delta.content
               });
             }
+            if (delta.images) {
+              for (const image of delta.images) {
+                const match1 = image.image_url.url.match(/^data:([^;]+)/);
+                const match2 = image.image_url.url.match(/^data:[^;]*;base64,(.+)$/);
+                controller.enqueue({
+                  type: 'file',
+                  mediaType: match1 ? (match1[1] ?? 'image/jpeg') : 'image/jpeg',
+                  data: match2 ? match2[1] : image.image_url.url,
+                });
+              }
+            }
             if (delta.tool_calls != null) {
               if (isActiveReasoning) {
                 controller.enqueue({
@@ -857,6 +893,16 @@ var OpenAICompatibleChatLanguageModel = class {
                 });
               }
             }
+            for (const annotation of delta.annotations || []) {
+              if (annotation?.type !== "url_citation" || typeof annotation.url_citation?.url !== "string" || !URL.canParse(annotation.url_citation.url) || !["http:", "https:"].includes(new URL(annotation.url_citation.url).protocol)) continue;
+              controller.enqueue({
+                type: "source",
+                sourceType: "url",
+                id: generateId(),
+                url: annotation.url_citation.url,
+                title: typeof annotation.url_citation.title === "string" ? annotation.url_citation.title : void 0
+              });
+            }
           },
           flush(controller) {
             var _a2, _b, _c, _d, _e;
@@ -955,6 +1001,7 @@ var OpenAICompatibleChatResponseSchema = z3.looseObject({
         content: z3.string().nullish(),
         reasoning_content: z3.string().nullish(),
         reasoning: z3.string().nullish(),
+        annotations: z3.array(z3.unknown()).nullish().catch([]),
         tool_calls: z3.array(
           z3.object({
             id: z3.string().nullish(),
@@ -969,6 +1016,14 @@ var OpenAICompatibleChatResponseSchema = z3.looseObject({
               }).nullish()
             }).nullish()
           })
+        ).nullish(),
+        images: z3.array(
+          z3.object({
+            type: z3.literal('image_url'),
+            image_url: z3.object({
+              url: z3.string(),
+            })
+          })
         ).nullish()
       }),
       finish_reason: z3.string().nullish()
@@ -989,6 +1044,7 @@ var chunkBaseSchema = z3.looseObject({
         // providers serving `gpt-oss` set `reasoning`. See #7866
         reasoning_content: z3.string().nullish(),
         reasoning: z3.string().nullish(),
+        annotations: z3.array(z3.unknown()).nullish().catch([]),
         tool_calls: z3.array(
           z3.object({
             index: z3.number().nullish(),
@@ -1005,6 +1061,14 @@ var chunkBaseSchema = z3.looseObject({
               }).nullish()
             }).nullish()
           })
+        ).nullish(),
+        images: z3.array(
+          z3.object({
+            type: z3.literal('image_url'),
+            image_url: z3.object({
+              url: z3.string(),
+            })
+          })
         ).nullish()
       }).nullish(),
       finish_reason: z3.string().nullish()
@@ -1581,7 +1645,7 @@ var OpenAICompatibleEmbeddingModel = class {
     return {
       warnings,
       embeddings: response.data.map((item) => item.embedding),
-      usage: response.usage ? { tokens: response.usage.prompt_tokens } : void 0,
+      usage: response.usage ? { tokens: response.usage.prompt_tokens ?? response.usage.total_tokens } : void 0,
       providerMetadata: response.providerMetadata,
       response: { headers: responseHeaders, body: rawValue }
     };
@@ -1589,7 +1653,7 @@ var OpenAICompatibleEmbeddingModel = class {
 };
 var openaiTextEmbeddingResponseSchema = z7.object({
   data: z7.array(z7.object({ embedding: z7.array(z7.number()) })),
-  usage: z7.object({ prompt_tokens: z7.number() }).nullish(),
+  usage: z7.object({ prompt_tokens: z7.number().nullish(), total_tokens: z7.number().nullish() }).nullish(),
   providerMetadata: z7.record(z7.string(), z7.record(z7.string(), z7.any())).optional()
 });
 
@@ -1605,6 +1669,24 @@ import {
   postJsonToApi as postJsonToApi4
 } from "@ai-sdk/provider-utils";
 import { z as z8 } from "zod/v4";
+var defaultResponseFormatPrefixes = [
+  "chatgpt-image-",
+  "gpt-image-1-mini",
+  "gpt-image-1.5",
+  "gpt-image-1",
+  "gpt-image-2"
+];
+function hasDefaultResponseFormat(modelId) {
+  return defaultResponseFormatPrefixes.some((prefix) => modelId.startsWith(prefix));
+}
+function isResponseFormatRejection(error) {
+  if (typeof error !== "object" || error == null) return false;
+  const { statusCode, message, responseBody } = error;
+  if (statusCode !== 400 && statusCode !== 422) return false;
+  return [message, responseBody].some(
+    (detail) => typeof detail === "string" && /response(?:_|\s|-)?format/i.test(detail)
+  );
+}
 var OpenAICompatibleImageModel = class {
   constructor(modelId, config) {
     this.modelId = modelId;
@@ -1680,7 +1762,11 @@ var OpenAICompatibleImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === 'string') return [item.b64_json];
+          if (typeof item.url === 'string') return [item.url];
+          return [];
+        }),
         warnings,
         response: {
           timestamp: currentDate,
@@ -1689,7 +1775,7 @@ var OpenAICompatibleImageModel = class {
         }
       };
     }
-    const { value: response, responseHeaders } = await postJsonToApi4({
+    const postImageRequest = (responseFormat) => postJsonToApi4({
       url: this.config.url({
         path: "/images/generations",
         modelId: this.modelId
@@ -1700,8 +1786,8 @@ var OpenAICompatibleImageModel = class {
         prompt,
         n,
         size,
-        ...args,
-        response_format: "b64_json"
+        ...(responseFormat != null ? { response_format: responseFormat } : {}),
+        ...args
       },
       failedResponseHandler: createJsonErrorResponseHandler4(
         (_e = this.config.errorStructure) != null ? _e : defaultOpenAICompatibleErrorStructure
@@ -1712,8 +1798,25 @@ var OpenAICompatibleImageModel = class {
       abortSignal,
       fetch: this.config.fetch
     });
+    const defaultResponseFormat = hasDefaultResponseFormat(this.modelId) || args.response_format !== void 0 ? null : "b64_json";
+    let posted;
+    try {
+      posted = await postImageRequest(defaultResponseFormat);
+    } catch (error) {
+      if (defaultResponseFormat == null || !isResponseFormatRejection(error)) throw error;
+      try {
+        posted = await postImageRequest(null);
+      } catch {
+        throw error;
+      }
+    }
+    const { value: response, responseHeaders } = posted;
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
       warnings,
       response: {
         timestamp: currentDate,
@@ -1724,7 +1827,7 @@ var OpenAICompatibleImageModel = class {
   }
 };
 var openaiCompatibleImageResponseSchema = z8.object({
-  data: z8.array(z8.object({ b64_json: z8.string() }))
+  data: z8.array(z8.object({ b64_json: z8.string().nullish(), url: z8.string().nullish() }))
 });
 async function fileToBlob(file) {
   if (file.type === "url") {
diff --git a/src/image/openai-compatible-image-model.ts b/src/image/openai-compatible-image-model.ts
index 408e96c59cc66085ccd0b9e9900f2887e05dd238..6c799d18eff37050b5da35d283361f65368006a0 100644
--- a/src/image/openai-compatible-image-model.ts
+++ b/src/image/openai-compatible-image-model.ts
@@ -23,6 +23,34 @@ import {
 } from '../openai-compatible-error';
 import type { OpenAICompatibleImageModelId } from './openai-compatible-image-settings';
 
+const defaultResponseFormatPrefixes = [
+  'chatgpt-image-',
+  'gpt-image-1-mini',
+  'gpt-image-1.5',
+  'gpt-image-1',
+  'gpt-image-2',
+];
+
+function hasDefaultResponseFormat(modelId: string): boolean {
+  return defaultResponseFormatPrefixes.some(prefix => modelId.startsWith(prefix));
+}
+
+function isResponseFormatRejection(error: unknown): boolean {
+  if (typeof error !== 'object' || error == null) return false;
+
+  const { statusCode, message, responseBody } = error as {
+    statusCode?: unknown;
+    message?: unknown;
+    responseBody?: unknown;
+  };
+  if (statusCode !== 400 && statusCode !== 422) return false;
+
+  return [message, responseBody].some(
+    detail =>
+      typeof detail === 'string' && /response(?:_|\s|-)?format/i.test(detail),
+  );
+}
+
 export type OpenAICompatibleImageModelConfig = {
   provider: string;
   headers: () => Record<string, string | undefined>;
@@ -125,7 +153,11 @@ export class OpenAICompatibleImageModel implements ImageModelV3 {
       });
 
       return {
-        images: response.data.map(item => item.b64_json),
+        images: response.data.flatMap(item => {
+          if (typeof item.b64_json === 'string') return [item.b64_json];
+          if (typeof item.url === 'string') return [item.url];
+          return [];
+        }),
         warnings,
         response: {
           timestamp: currentDate,
@@ -136,7 +168,8 @@ export class OpenAICompatibleImageModel implements ImageModelV3 {
     }
 
     // Standard image generation mode - use JSON and /images/generations endpoint
-    const { value: response, responseHeaders } = await postJsonToApi({
+    const postImageRequest = (responseFormat: string | null) =>
+      postJsonToApi({
       url: this.config.url({
         path: '/images/generations',
         modelId: this.modelId,
@@ -147,8 +180,10 @@ export class OpenAICompatibleImageModel implements ImageModelV3 {
         prompt,
         n,
         size,
+        ...(responseFormat != null
+          ? { response_format: responseFormat }
+          : {}),
         ...args,
-        response_format: 'b64_json',
       },
       failedResponseHandler: createJsonErrorResponseHandler(
         this.config.errorStructure ?? defaultOpenAICompatibleErrorStructure,
@@ -158,10 +193,36 @@ export class OpenAICompatibleImageModel implements ImageModelV3 {
       ),
       abortSignal,
       fetch: this.config.fetch,
-    });
+      });
+
+    const defaultResponseFormat =
+      hasDefaultResponseFormat(this.modelId) || args.response_format !== undefined
+        ? null
+        : 'b64_json';
+    let posted;
+    try {
+      posted = await postImageRequest(defaultResponseFormat);
+    } catch (error) {
+      if (
+        defaultResponseFormat == null ||
+        !isResponseFormatRejection(error)
+      ) {
+        throw error;
+      }
+      try {
+        posted = await postImageRequest(null);
+      } catch {
+        throw error;
+      }
+    }
+    const { value: response, responseHeaders } = posted;
 
     return {
-      images: response.data.map(item => item.b64_json),
+      images: response.data.flatMap(item => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
       warnings,
       response: {
         timestamp: currentDate,
@@ -175,7 +236,12 @@ export class OpenAICompatibleImageModel implements ImageModelV3 {
 // minimal version of the schema, focussed on what is needed for the implementation
 // this approach limits breakages when the API changes and increases efficiency
 const openaiCompatibleImageResponseSchema = z.object({
-  data: z.array(z.object({ b64_json: z.string() })),
+  data: z.array(
+    z.object({
+      b64_json: z.string().nullish(),
+      url: z.string().nullish(),
+    }),
+  ),
 });
 
 type OpenAICompatibleFormDataInput = {
