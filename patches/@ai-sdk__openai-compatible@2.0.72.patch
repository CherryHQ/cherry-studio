diff --git a/dist/index.js b/dist/index.js
index ea415b862b2c36c3771b52ed8cd4fe88a784a46a..13825304a8132a0c43ace8c123fdb27b3da848f4 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -257,7 +257,11 @@ function convertToOpenAICompatibleChatMessages(prompt) {
         messages.push({
           role: "assistant",
           content: toolCalls.length > 0 ? text || null : text,
-          ...reasoning.length > 0 ? { reasoning_content: reasoning } : {},
+          // Thinking-mode dialects (DeepSeek, GLM, Kimi, MiniMax, …) reject a request whose
+          // assistant turns dropped `reasoning_content` once one performed a tool call. Send the
+          // key unconditionally — an empty string when the turn produced none, matching
+          // `@ai-sdk/deepseek`; the field is ignored when the turn did not need it.
+          reasoning_content: reasoning,
           tool_calls: toolCalls.length > 0 ? toolCalls : void 0,
           ...metadata
         });
@@ -599,6 +603,17 @@ var OpenAICompatibleChatLanguageModel = class {
         text: reasoning
       });
     }
+    if (choice.message.images) {
+      for (const image of choice.message.images) {
+        const match1 = image.image_url.url.match(/^data:([^;]+)/);
+        const match2 = image.image_url.url.match(/^data:[^;]*;base64,(.+)$/);
+        content.push({
+          type: 'file',
+          mediaType: match1 ? (match1[1] ?? 'image/jpeg') : 'image/jpeg',
+          data: match2 ? match2[1] : image.image_url.url,
+        });
+      }
+    }
     if (choice.message.tool_calls != null) {
       for (const toolCall of choice.message.tool_calls) {
         const thoughtSignature = (_c = (_b = toolCall.extra_content) == null ? void 0 : _b.google) == null ? void 0 : _c.thought_signature;
@@ -758,6 +773,17 @@ var OpenAICompatibleChatLanguageModel = class {
                 delta: delta.content
               });
             }
+            if (delta.images) {
+              for (const image of delta.images) {
+                const match1 = image.image_url.url.match(/^data:([^;]+)/);
+                const match2 = image.image_url.url.match(/^data:[^;]*;base64,(.+)$/);
+                controller.enqueue({
+                  type: 'file',
+                  mediaType: match1 ? (match1[1] ?? 'image/jpeg') : 'image/jpeg',
+                  data: match2 ? match2[1] : image.image_url.url,
+                });
+              }
+            }
             if (delta.tool_calls != null) {
               if (isActiveReasoning) {
                 controller.enqueue({
@@ -966,7 +992,23 @@ var OpenAICompatibleChatResponseSchema = import_v43.z.looseObject({
     import_v43.z.object({
       message: import_v43.z.object({
         role: import_v43.z.literal("assistant").nullish(),
-        content: import_v43.z.string().nullish(),
+        // Volcano Ark sends array-form `content` on thinking+tool turns; fold text/refusal
+        // parts to text (empty → null), throw on any other shape so nothing drops silently.
+        content: import_v43.z.union([
+          import_v43.z.string(),
+          import_v43.z.array(import_v43.z.union([import_v43.z.string(), import_v43.z.looseObject({})])),
+          import_v43.z.null()
+        ]).nullish().transform((content) => {
+          if (!Array.isArray(content)) return content;
+          if (content.length === 0) return null;
+          const text = content.map((part) => {
+            if (typeof part === "string") return part;
+            if (typeof part === "object" && part !== null && typeof part.text === "string") return part.text;
+            if (typeof part === "object" && part !== null && typeof part.refusal === "string") return part.refusal;
+            throw new Error("Unsupported content part shape");
+          }).join("");
+          return text.length > 0 ? text : null;
+        }),
         reasoning_content: import_v43.z.string().nullish(),
         reasoning: import_v43.z.string().nullish(),
         tool_calls: import_v43.z.array(
@@ -983,6 +1025,14 @@ var OpenAICompatibleChatResponseSchema = import_v43.z.looseObject({
               }).nullish()
             }).nullish()
           })
+        ).nullish(),
+        images: import_v43.z.array(
+          import_v43.z.object({
+            type: import_v43.z.literal('image_url'),
+            image_url: import_v43.z.object({
+              url: import_v43.z.string(),
+            })
+          })
         ).nullish()
       }),
       finish_reason: import_v43.z.string().nullish()
@@ -998,7 +1048,23 @@ var chunkBaseSchema = import_v43.z.looseObject({
     import_v43.z.object({
       delta: import_v43.z.object({
         role: import_v43.z.enum(["assistant", ""]).nullish(),
-        content: import_v43.z.string().nullish(),
+        // Volcano Ark sends array-form `content` on thinking+tool turns; fold text/refusal
+        // parts to text (empty → null), throw on any other shape so nothing drops silently.
+        content: import_v43.z.union([
+          import_v43.z.string(),
+          import_v43.z.array(import_v43.z.union([import_v43.z.string(), import_v43.z.looseObject({})])),
+          import_v43.z.null()
+        ]).nullish().transform((content) => {
+          if (!Array.isArray(content)) return content;
+          if (content.length === 0) return null;
+          const text = content.map((part) => {
+            if (typeof part === "string") return part;
+            if (typeof part === "object" && part !== null && typeof part.text === "string") return part.text;
+            if (typeof part === "object" && part !== null && typeof part.refusal === "string") return part.refusal;
+            throw new Error("Unsupported content part shape");
+          }).join("");
+          return text.length > 0 ? text : null;
+        }),
         // Most openai-compatible models set `reasoning_content`, but some
         // providers serving `gpt-oss` set `reasoning`. See #7866
         reasoning_content: import_v43.z.string().nullish(),
@@ -1019,6 +1085,14 @@ var chunkBaseSchema = import_v43.z.looseObject({
               }).nullish()
             }).nullish()
           })
+        ).nullish(),
+        images: import_v43.z.array(
+          import_v43.z.object({
+            type: import_v43.z.literal('image_url'),
+            image_url: import_v43.z.object({
+              url: import_v43.z.string(),
+            })
+          })
         ).nullish()
       }).nullish(),
       finish_reason: import_v43.z.string().nullish()
@@ -1577,7 +1651,7 @@ var OpenAICompatibleEmbeddingModel = class {
     return {
       warnings,
       embeddings: response.data.map((item) => item.embedding),
-      usage: response.usage ? { tokens: response.usage.prompt_tokens } : void 0,
+      usage: response.usage ? { tokens: response.usage.prompt_tokens ?? response.usage.total_tokens } : void 0,
       providerMetadata: response.providerMetadata,
       response: { headers: responseHeaders, body: rawValue }
     };
@@ -1585,13 +1659,23 @@ var OpenAICompatibleEmbeddingModel = class {
 };
 var openaiTextEmbeddingResponseSchema = import_v47.z.object({
   data: import_v47.z.array(import_v47.z.object({ embedding: import_v47.z.array(import_v47.z.number()) })),
-  usage: import_v47.z.object({ prompt_tokens: import_v47.z.number() }).nullish(),
+  usage: import_v47.z.object({ prompt_tokens: import_v47.z.number().nullish(), total_tokens: import_v47.z.number().nullish() }).nullish(),
   providerMetadata: import_v47.z.record(import_v47.z.string(), import_v47.z.record(import_v47.z.string(), import_v47.z.any())).optional()
 });
 
 // src/image/openai-compatible-image-model.ts
 var import_provider_utils5 = require("@ai-sdk/provider-utils");
 var import_v48 = require("zod/v4");
+var defaultResponseFormatPrefixes = [
+  "chatgpt-image-",
+  "gpt-image-1-mini",
+  "gpt-image-1.5",
+  "gpt-image-1",
+  "gpt-image-2"
+];
+function hasDefaultResponseFormat(modelId) {
+  return defaultResponseFormatPrefixes.some((prefix) => modelId.startsWith(prefix));
+}
 var OpenAICompatibleImageModel = class {
   constructor(modelId, config) {
     this.modelId = modelId;
@@ -1667,7 +1751,11 @@ var OpenAICompatibleImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === 'string') return [item.b64_json];
+          if (typeof item.url === 'string') return [item.url];
+          return [];
+        }),
         warnings,
         response: {
           timestamp: currentDate,
@@ -1676,7 +1764,7 @@ var OpenAICompatibleImageModel = class {
         }
       };
     }
-    const { value: response, responseHeaders } = await (0, import_provider_utils5.postJsonToApi)({
+    const postImageRequest = (responseFormat) => (0, import_provider_utils5.postJsonToApi)({
       url: this.config.url({
         path: "/images/generations",
         modelId: this.modelId
@@ -1687,8 +1775,8 @@ var OpenAICompatibleImageModel = class {
         prompt,
         n,
         size,
-        ...args,
-        response_format: "b64_json"
+        ...(responseFormat != null ? { response_format: responseFormat } : {}),
+        ...args
       },
       failedResponseHandler: (0, import_provider_utils5.createJsonErrorResponseHandler)(
         (_e = this.config.errorStructure) != null ? _e : defaultOpenAICompatibleErrorStructure
@@ -1699,8 +1787,28 @@ var OpenAICompatibleImageModel = class {
       abortSignal,
       fetch: this.config.fetch
     });
+    const defaultResponseFormat = hasDefaultResponseFormat(this.modelId) || args.response_format !== void 0 ? null : "b64_json";
+    let posted;
+    try {
+      posted = await postImageRequest(defaultResponseFormat);
+    } catch (error) {
+      // Models that do not implement `response_format` reject it with 400 or 422; retry once
+      // without it, and surface the ORIGINAL rejection when the retry fails too.
+      const isRejectedRequest = !!error && (error.statusCode === 400 || error.statusCode === 422);
+      if (defaultResponseFormat == null || !isRejectedRequest) throw error;
+      try {
+        posted = await postImageRequest(null);
+      } catch (retryError) {
+        throw error;
+      }
+    }
+    const { value: response, responseHeaders } = posted;
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
       warnings,
       response: {
         timestamp: currentDate,
@@ -1711,7 +1819,7 @@ var OpenAICompatibleImageModel = class {
   }
 };
 var openaiCompatibleImageResponseSchema = import_v48.z.object({
-  data: import_v48.z.array(import_v48.z.object({ b64_json: import_v48.z.string() }))
+  data: import_v48.z.array(import_v48.z.object({ b64_json: import_v48.z.string().nullish(), url: import_v48.z.string().nullish() }))
 });
 async function fileToBlob(file) {
   if (file.type === "url") {
diff --git a/dist/index.mjs b/dist/index.mjs
index 66ab6a1aeeb1c4c72a81136a042052e50b55ba4b..c763f9a9e00f6fadcfea697ff42ae610499af428 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -241,7 +241,11 @@ function convertToOpenAICompatibleChatMessages(prompt) {
         messages.push({
           role: "assistant",
           content: toolCalls.length > 0 ? text || null : text,
-          ...reasoning.length > 0 ? { reasoning_content: reasoning } : {},
+          // Thinking-mode dialects (DeepSeek, GLM, Kimi, MiniMax, …) reject a request whose
+          // assistant turns dropped `reasoning_content` once one performed a tool call. Send the
+          // key unconditionally — an empty string when the turn produced none, matching
+          // `@ai-sdk/deepseek`; the field is ignored when the turn did not need it.
+          reasoning_content: reasoning,
           tool_calls: toolCalls.length > 0 ? toolCalls : void 0,
           ...metadata
         });
@@ -585,6 +589,17 @@ var OpenAICompatibleChatLanguageModel = class {
         text: reasoning
       });
     }
+    if (choice.message.images) {
+      for (const image of choice.message.images) {
+        const match1 = image.image_url.url.match(/^data:([^;]+)/);
+        const match2 = image.image_url.url.match(/^data:[^;]*;base64,(.+)$/);
+        content.push({
+          type: 'file',
+          mediaType: match1 ? (match1[1] ?? 'image/jpeg') : 'image/jpeg',
+          data: match2 ? match2[1] : image.image_url.url,
+        });
+      }
+    }
     if (choice.message.tool_calls != null) {
       for (const toolCall of choice.message.tool_calls) {
         const thoughtSignature = (_c = (_b = toolCall.extra_content) == null ? void 0 : _b.google) == null ? void 0 : _c.thought_signature;
@@ -744,6 +759,17 @@ var OpenAICompatibleChatLanguageModel = class {
                 delta: delta.content
               });
             }
+            if (delta.images) {
+              for (const image of delta.images) {
+                const match1 = image.image_url.url.match(/^data:([^;]+)/);
+                const match2 = image.image_url.url.match(/^data:[^;]*;base64,(.+)$/);
+                controller.enqueue({
+                  type: 'file',
+                  mediaType: match1 ? (match1[1] ?? 'image/jpeg') : 'image/jpeg',
+                  data: match2 ? match2[1] : image.image_url.url,
+                });
+              }
+            }
             if (delta.tool_calls != null) {
               if (isActiveReasoning) {
                 controller.enqueue({
@@ -952,7 +978,23 @@ var OpenAICompatibleChatResponseSchema = z3.looseObject({
     z3.object({
       message: z3.object({
         role: z3.literal("assistant").nullish(),
-        content: z3.string().nullish(),
+        // Volcano Ark sends array-form `content` on thinking+tool turns; fold text/refusal
+        // parts to text (empty → null), throw on any other shape so nothing drops silently.
+        content: z3.union([
+          z3.string(),
+          z3.array(z3.union([z3.string(), z3.looseObject({})])),
+          z3.null()
+        ]).nullish().transform((content) => {
+          if (!Array.isArray(content)) return content;
+          if (content.length === 0) return null;
+          const text = content.map((part) => {
+            if (typeof part === "string") return part;
+            if (typeof part === "object" && part !== null && typeof part.text === "string") return part.text;
+            if (typeof part === "object" && part !== null && typeof part.refusal === "string") return part.refusal;
+            throw new Error("Unsupported content part shape");
+          }).join("");
+          return text.length > 0 ? text : null;
+        }),
         reasoning_content: z3.string().nullish(),
         reasoning: z3.string().nullish(),
         tool_calls: z3.array(
@@ -969,6 +1011,14 @@ var OpenAICompatibleChatResponseSchema = z3.looseObject({
               }).nullish()
             }).nullish()
           })
+        ).nullish(),
+        images: z3.array(
+          z3.object({
+            type: z3.literal('image_url'),
+            image_url: z3.object({
+              url: z3.string(),
+            })
+          })
         ).nullish()
       }),
       finish_reason: z3.string().nullish()
@@ -984,7 +1034,23 @@ var chunkBaseSchema = z3.looseObject({
     z3.object({
       delta: z3.object({
         role: z3.enum(["assistant", ""]).nullish(),
-        content: z3.string().nullish(),
+        // Volcano Ark sends array-form `content` on thinking+tool turns; fold text/refusal
+        // parts to text (empty → null), throw on any other shape so nothing drops silently.
+        content: z3.union([
+          z3.string(),
+          z3.array(z3.union([z3.string(), z3.looseObject({})])),
+          z3.null()
+        ]).nullish().transform((content) => {
+          if (!Array.isArray(content)) return content;
+          if (content.length === 0) return null;
+          const text = content.map((part) => {
+            if (typeof part === "string") return part;
+            if (typeof part === "object" && part !== null && typeof part.text === "string") return part.text;
+            if (typeof part === "object" && part !== null && typeof part.refusal === "string") return part.refusal;
+            throw new Error("Unsupported content part shape");
+          }).join("");
+          return text.length > 0 ? text : null;
+        }),
         // Most openai-compatible models set `reasoning_content`, but some
         // providers serving `gpt-oss` set `reasoning`. See #7866
         reasoning_content: z3.string().nullish(),
@@ -1005,6 +1071,14 @@ var chunkBaseSchema = z3.looseObject({
               }).nullish()
             }).nullish()
           })
+        ).nullish(),
+        images: z3.array(
+          z3.object({
+            type: z3.literal('image_url'),
+            image_url: z3.object({
+              url: z3.string(),
+            })
+          })
         ).nullish()
       }).nullish(),
       finish_reason: z3.string().nullish()
@@ -1581,7 +1655,7 @@ var OpenAICompatibleEmbeddingModel = class {
     return {
       warnings,
       embeddings: response.data.map((item) => item.embedding),
-      usage: response.usage ? { tokens: response.usage.prompt_tokens } : void 0,
+      usage: response.usage ? { tokens: response.usage.prompt_tokens ?? response.usage.total_tokens } : void 0,
       providerMetadata: response.providerMetadata,
       response: { headers: responseHeaders, body: rawValue }
     };
@@ -1589,7 +1663,7 @@ var OpenAICompatibleEmbeddingModel = class {
 };
 var openaiTextEmbeddingResponseSchema = z7.object({
   data: z7.array(z7.object({ embedding: z7.array(z7.number()) })),
-  usage: z7.object({ prompt_tokens: z7.number() }).nullish(),
+  usage: z7.object({ prompt_tokens: z7.number().nullish(), total_tokens: z7.number().nullish() }).nullish(),
   providerMetadata: z7.record(z7.string(), z7.record(z7.string(), z7.any())).optional()
 });
 
@@ -1605,6 +1679,16 @@ import {
   postJsonToApi as postJsonToApi4
 } from "@ai-sdk/provider-utils";
 import { z as z8 } from "zod/v4";
+var defaultResponseFormatPrefixes = [
+  "chatgpt-image-",
+  "gpt-image-1-mini",
+  "gpt-image-1.5",
+  "gpt-image-1",
+  "gpt-image-2"
+];
+function hasDefaultResponseFormat(modelId) {
+  return defaultResponseFormatPrefixes.some((prefix) => modelId.startsWith(prefix));
+}
 var OpenAICompatibleImageModel = class {
   constructor(modelId, config) {
     this.modelId = modelId;
@@ -1680,7 +1764,11 @@ var OpenAICompatibleImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === 'string') return [item.b64_json];
+          if (typeof item.url === 'string') return [item.url];
+          return [];
+        }),
         warnings,
         response: {
           timestamp: currentDate,
@@ -1689,7 +1777,7 @@ var OpenAICompatibleImageModel = class {
         }
       };
     }
-    const { value: response, responseHeaders } = await postJsonToApi4({
+    const postImageRequest = (responseFormat) => postJsonToApi4({
       url: this.config.url({
         path: "/images/generations",
         modelId: this.modelId
@@ -1700,8 +1788,8 @@ var OpenAICompatibleImageModel = class {
         prompt,
         n,
         size,
-        ...args,
-        response_format: "b64_json"
+        ...(responseFormat != null ? { response_format: responseFormat } : {}),
+        ...args
       },
       failedResponseHandler: createJsonErrorResponseHandler4(
         (_e = this.config.errorStructure) != null ? _e : defaultOpenAICompatibleErrorStructure
@@ -1712,8 +1800,28 @@ var OpenAICompatibleImageModel = class {
       abortSignal,
       fetch: this.config.fetch
     });
+    const defaultResponseFormat = hasDefaultResponseFormat(this.modelId) || args.response_format !== void 0 ? null : "b64_json";
+    let posted;
+    try {
+      posted = await postImageRequest(defaultResponseFormat);
+    } catch (error) {
+      // Models that do not implement `response_format` reject it with 400 or 422; retry once
+      // without it, and surface the ORIGINAL rejection when the retry fails too.
+      const isRejectedRequest = !!error && (error.statusCode === 400 || error.statusCode === 422);
+      if (defaultResponseFormat == null || !isRejectedRequest) throw error;
+      try {
+        posted = await postImageRequest(null);
+      } catch (retryError) {
+        throw error;
+      }
+    }
+    const { value: response, responseHeaders } = posted;
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
       warnings,
       response: {
         timestamp: currentDate,
@@ -1724,7 +1832,7 @@ var OpenAICompatibleImageModel = class {
   }
 };
 var openaiCompatibleImageResponseSchema = z8.object({
-  data: z8.array(z8.object({ b64_json: z8.string() }))
+  data: z8.array(z8.object({ b64_json: z8.string().nullish(), url: z8.string().nullish() }))
 });
 async function fileToBlob(file) {
   if (file.type === "url") {
