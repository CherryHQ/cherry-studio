diff --git a/dist/index.d.mts b/dist/index.d.mts
index 976b903d5b9bd006b56213566dcb9726134ac691..0f2d7d8e0883167863bda45b196a95ef24298ea7 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -48,27 +48,27 @@ type OpenAIEmbeddingModelOptions = InferSchema<typeof openaiEmbeddingModelOption
 
 type OpenAIImageModelId = 'dall-e-3' | 'dall-e-2' | 'gpt-image-1' | 'gpt-image-1-mini' | 'gpt-image-1.5' | 'gpt-image-2' | 'chatgpt-image-latest' | (string & {});
 declare const openaiImageModelOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
 }>;
 type OpenAIImageModelOptions = InferSchema<typeof openaiImageModelOptions>;
 declare const openaiImageModelGenerationOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
-    style?: "vivid" | "natural" | undefined;
-    moderation?: "low" | "auto" | undefined;
+    style?: string | undefined;
+    moderation?: string | undefined;
 }>;
 type OpenAIImageModelGenerationOptions = InferSchema<typeof openaiImageModelGenerationOptions>;
 declare const openaiImageModelEditOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
     inputFidelity?: "low" | "high" | undefined;
@@ -857,18 +857,18 @@ declare const openaiTools: {
      * @param size - The size of the generated image. One of 'auto', '1024x1024', '1024x1536', or '1536x1024'.
      */
     imageGeneration: (args?: {
-        background?: "auto" | "opaque" | "transparent";
+        background?: string | undefined;
         inputFidelity?: "low" | "high";
         inputImageMask?: {
             fileId?: string;
             imageUrl?: string;
         };
         model?: string;
-        moderation?: "auto";
+        moderation?: string | undefined;
         outputCompression?: number;
-        outputFormat?: "png" | "jpeg" | "webp";
+        outputFormat?: string | undefined;
         partialImages?: number;
-        quality?: "auto" | "low" | "medium" | "high";
+        quality?: string | undefined;
         size?: "auto" | "1024x1024" | "1024x1536" | "1536x1024";
     }) => _ai_sdk_provider_utils.Tool<{}, {
         result: string;
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 976b903d5b9bd006b56213566dcb9726134ac691..0f2d7d8e0883167863bda45b196a95ef24298ea7 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -48,27 +48,27 @@ type OpenAIEmbeddingModelOptions = InferSchema<typeof openaiEmbeddingModelOption
 
 type OpenAIImageModelId = 'dall-e-3' | 'dall-e-2' | 'gpt-image-1' | 'gpt-image-1-mini' | 'gpt-image-1.5' | 'gpt-image-2' | 'chatgpt-image-latest' | (string & {});
 declare const openaiImageModelOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
 }>;
 type OpenAIImageModelOptions = InferSchema<typeof openaiImageModelOptions>;
 declare const openaiImageModelGenerationOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
-    style?: "vivid" | "natural" | undefined;
-    moderation?: "low" | "auto" | undefined;
+    style?: string | undefined;
+    moderation?: string | undefined;
 }>;
 type OpenAIImageModelGenerationOptions = InferSchema<typeof openaiImageModelGenerationOptions>;
 declare const openaiImageModelEditOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
     inputFidelity?: "low" | "high" | undefined;
@@ -857,18 +857,18 @@ declare const openaiTools: {
      * @param size - The size of the generated image. One of 'auto', '1024x1024', '1024x1536', or '1536x1024'.
      */
     imageGeneration: (args?: {
-        background?: "auto" | "opaque" | "transparent";
+        background?: string | undefined;
         inputFidelity?: "low" | "high";
         inputImageMask?: {
             fileId?: string;
             imageUrl?: string;
         };
         model?: string;
-        moderation?: "auto";
+        moderation?: string | undefined;
         outputCompression?: number;
-        outputFormat?: "png" | "jpeg" | "webp";
+        outputFormat?: string | undefined;
         partialImages?: number;
-        quality?: "auto" | "low" | "medium" | "high";
+        quality?: string | undefined;
         size?: "auto" | "1024x1024" | "1024x1536" | "1536x1024";
     }) => _ai_sdk_provider_utils.Tool<{}, {
         result: string;
diff --git a/dist/index.js b/dist/index.js
index 48ea06eecf6397b44f4176acea05664541cc4e1f..78e623d5ea0860fb9137924abb87f30dade77b12 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1012,7 +1012,7 @@ var OpenAIChatLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2025,7 +2025,8 @@ var openaiImageResponseSchema = (0, import_provider_utils12.lazySchema)(
       created: import_v48.z.number().nullish(),
       data: import_v48.z.array(
         import_v48.z.object({
-          b64_json: import_v48.z.string(),
+          b64_json: import_v48.z.string().nullish(),
+          url: import_v48.z.string().nullish(),
           revised_prompt: import_v48.z.string().nullish()
         })
       ),
@@ -2074,18 +2075,18 @@ var baseImageModelOptionsObject = import_v49.z.object({
    *
    * Valid values: `standard`, `hd`, `low`, `medium`, `high`, `auto`.
    */
-  quality: import_v49.z.enum(["standard", "hd", "low", "medium", "high", "auto"]).optional(),
+  quality: import_v49.z.string().optional(),
   /**
    * Background behavior for the generated image(s).
    *
    * If `transparent`, the output format must support transparency
    * (i.e. `png` or `webp`).
    */
-  background: import_v49.z.enum(["transparent", "opaque", "auto"]).optional(),
+  background: import_v49.z.string().optional(),
   /**
    * Format in which the generated image(s) are returned.
    */
-  outputFormat: import_v49.z.enum(["png", "jpeg", "webp"]).optional(),
+  outputFormat: import_v49.z.string().optional(),
   /**
    * Compression level (0-100) for the generated image(s). Applies to the
    * `jpeg` and `webp` output formats.
@@ -2108,12 +2109,12 @@ var openaiImageModelGenerationOptions = (0, import_provider_utils13.lazySchema)(
        * dramatic images; `natural` produces more subdued, less hyper-real
        * looking images.
        */
-      style: import_v49.z.enum(["vivid", "natural"]).optional(),
+      style: import_v49.z.string().optional(),
       /**
        * Content moderation level for the generated image(s). `low` applies
        * less restrictive filtering.
        */
-      moderation: import_v49.z.enum(["auto", "low"]).optional()
+      moderation: import_v49.z.string().optional()
     })
   )
 );
@@ -2213,7 +2214,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2280,7 +2285,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3443,9 +3452,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3681,6 +3692,12 @@ async function convertToOpenAIResponsesInput({
                       summary: []
                     };
                   }
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   const summaryParts = [];
                   if (part.text.length > 0) {
@@ -3724,6 +3741,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -4006,21 +4029,22 @@ ${text}`,
     }
   }
   if (!store && input.some(
-    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null
+    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null && !("content" in item && item.content != null)
   )) {
     warnings.push({
       type: "other",
       message: "Reasoning parts without encrypted content are not supported when store is false. Skipping reasoning parts."
     });
     input = input.filter(
-      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null
+      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null || "content" in item && item.content != null
     );
   }
   return { input, warnings };
 }
 var openaiResponsesReasoningProviderOptionsSchema = import_v421.z.object({
   itemId: import_v421.z.string().nullish(),
-  reasoningEncryptedContent: import_v421.z.string().nullish()
+  reasoningEncryptedContent: import_v421.z.string().nullish(),
+  rawReasoningContent: import_v421.z.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4658,6 +4682,11 @@ var openaiResponsesChunkSchema = (0, import_provider_utils28.lazySchema)(
         summary_index: import_v422.z.number(),
         delta: import_v422.z.string()
       }),
+      import_v422.z.object({
+        type: import_v422.z.literal("response.reasoning_text.delta"),
+        item_id: import_v422.z.string(),
+        delta: import_v422.z.string()
+      }),
       import_v422.z.object({
         type: import_v422.z.literal("response.reasoning_summary_part.done"),
         item_id: import_v422.z.string(),
@@ -4752,7 +4781,7 @@ var openaiResponsesResponseSchema = (0, import_provider_utils28.lazySchema)(
                       index: import_v422.z.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -5960,7 +5989,7 @@ var OpenAIResponsesLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -7411,6 +7440,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/index.mjs b/dist/index.mjs
index 26483821603be3057bd67afc71143f97e8c33ac9..ec0f329df4fe68293de0ff09c64149a21a10c514 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1008,7 +1008,7 @@ var OpenAIChatLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2056,7 +2056,8 @@ var openaiImageResponseSchema = lazySchema7(
       created: z8.number().nullish(),
       data: z8.array(
         z8.object({
-          b64_json: z8.string(),
+          b64_json: z8.string().nullish(),
+          url: z8.string().nullish(),
           revised_prompt: z8.string().nullish()
         })
       ),
@@ -2108,18 +2109,18 @@ var baseImageModelOptionsObject = z9.object({
    *
    * Valid values: `standard`, `hd`, `low`, `medium`, `high`, `auto`.
    */
-  quality: z9.enum(["standard", "hd", "low", "medium", "high", "auto"]).optional(),
+  quality: z9.string().optional(),
   /**
    * Background behavior for the generated image(s).
    *
    * If `transparent`, the output format must support transparency
    * (i.e. `png` or `webp`).
    */
-  background: z9.enum(["transparent", "opaque", "auto"]).optional(),
+  background: z9.string().optional(),
   /**
    * Format in which the generated image(s) are returned.
    */
-  outputFormat: z9.enum(["png", "jpeg", "webp"]).optional(),
+  outputFormat: z9.string().optional(),
   /**
    * Compression level (0-100) for the generated image(s). Applies to the
    * `jpeg` and `webp` output formats.
@@ -2142,12 +2143,12 @@ var openaiImageModelGenerationOptions = lazySchema8(
        * dramatic images; `natural` produces more subdued, less hyper-real
        * looking images.
        */
-      style: z9.enum(["vivid", "natural"]).optional(),
+      style: z9.string().optional(),
       /**
        * Content moderation level for the generated image(s). `low` applies
        * less restrictive filtering.
        */
-      moderation: z9.enum(["auto", "low"]).optional()
+      moderation: z9.string().optional()
     })
   )
 );
@@ -2247,7 +2248,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2314,7 +2319,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3541,9 +3550,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3779,6 +3790,12 @@ async function convertToOpenAIResponsesInput({
                       summary: []
                     };
                   }
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   const summaryParts = [];
                   if (part.text.length > 0) {
@@ -3822,6 +3839,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -4104,21 +4127,22 @@ ${text}`,
     }
   }
   if (!store && input.some(
-    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null
+    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null && !("content" in item && item.content != null)
   )) {
     warnings.push({
       type: "other",
       message: "Reasoning parts without encrypted content are not supported when store is false. Skipping reasoning parts."
     });
     input = input.filter(
-      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null
+      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null || "content" in item && item.content != null
     );
   }
   return { input, warnings };
 }
 var openaiResponsesReasoningProviderOptionsSchema = z21.object({
   itemId: z21.string().nullish(),
-  reasoningEncryptedContent: z21.string().nullish()
+  reasoningEncryptedContent: z21.string().nullish(),
+  rawReasoningContent: z21.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4759,6 +4783,11 @@ var openaiResponsesChunkSchema = lazySchema20(
         summary_index: z22.number(),
         delta: z22.string()
       }),
+      z22.object({
+        type: z22.literal("response.reasoning_text.delta"),
+        item_id: z22.string(),
+        delta: z22.string()
+      }),
       z22.object({
         type: z22.literal("response.reasoning_summary_part.done"),
         item_id: z22.string(),
@@ -4853,7 +4882,7 @@ var openaiResponsesResponseSchema = lazySchema20(
                       index: z22.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6066,7 +6095,7 @@ var OpenAIResponsesLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -7517,6 +7546,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/internal/index.d.mts b/dist/internal/index.d.mts
index ee9ebcd66892da332f8bc1b3f5dc44b9e756e4dc..538876735f3899266e497e32737ba46a5efbb888 100644
--- a/dist/internal/index.d.mts
+++ b/dist/internal/index.d.mts
@@ -127,27 +127,27 @@ declare const modelMaxImagesPerCall: Record<OpenAIImageModelId, number>;
 declare function hasDefaultResponseFormat(modelId: string): boolean;
 declare function getMaxImagesPerCall(modelId: OpenAIImageModelId): number;
 declare const openaiImageModelOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
 }>;
 type OpenAIImageModelOptions = InferSchema<typeof openaiImageModelOptions>;
 declare const openaiImageModelGenerationOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
-    style?: "vivid" | "natural" | undefined;
-    moderation?: "low" | "auto" | undefined;
+    style?: string | undefined;
+    moderation?: string | undefined;
 }>;
 type OpenAIImageModelGenerationOptions = InferSchema<typeof openaiImageModelGenerationOptions>;
 declare const openaiImageModelEditOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
     inputFidelity?: "low" | "high" | undefined;
@@ -1098,18 +1098,18 @@ declare const fileSearch: _ai_sdk_provider_utils.ProviderToolFactoryWithOutputSc
 }>;
 
 declare const imageGenerationArgsSchema: _ai_sdk_provider_utils.LazySchema<{
-    background?: "auto" | "transparent" | "opaque" | undefined;
+    background?: string | undefined;
     inputFidelity?: "low" | "high" | undefined;
     inputImageMask?: {
         fileId?: string | undefined;
         imageUrl?: string | undefined;
     } | undefined;
     model?: string | undefined;
-    moderation?: "auto" | undefined;
+    moderation?: string | undefined;
     outputCompression?: number | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    outputFormat?: string | undefined;
     partialImages?: number | undefined;
-    quality?: "low" | "medium" | "high" | "auto" | undefined;
+    quality?: string | undefined;
     size?: "auto" | "1024x1024" | "1024x1536" | "1536x1024" | undefined;
 }>;
 declare const imageGenerationOutputSchema: _ai_sdk_provider_utils.LazySchema<{
diff --git a/dist/internal/index.d.ts b/dist/internal/index.d.ts
index ee9ebcd66892da332f8bc1b3f5dc44b9e756e4dc..538876735f3899266e497e32737ba46a5efbb888 100644
--- a/dist/internal/index.d.ts
+++ b/dist/internal/index.d.ts
@@ -127,27 +127,27 @@ declare const modelMaxImagesPerCall: Record<OpenAIImageModelId, number>;
 declare function hasDefaultResponseFormat(modelId: string): boolean;
 declare function getMaxImagesPerCall(modelId: OpenAIImageModelId): number;
 declare const openaiImageModelOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
 }>;
 type OpenAIImageModelOptions = InferSchema<typeof openaiImageModelOptions>;
 declare const openaiImageModelGenerationOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
-    style?: "vivid" | "natural" | undefined;
-    moderation?: "low" | "auto" | undefined;
+    style?: string | undefined;
+    moderation?: string | undefined;
 }>;
 type OpenAIImageModelGenerationOptions = InferSchema<typeof openaiImageModelGenerationOptions>;
 declare const openaiImageModelEditOptions: _ai_sdk_provider_utils.LazySchema<{
-    quality?: "low" | "medium" | "high" | "auto" | "standard" | "hd" | undefined;
-    background?: "auto" | "transparent" | "opaque" | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    quality?: string | undefined;
+    background?: string | undefined;
+    outputFormat?: string | undefined;
     outputCompression?: number | undefined;
     user?: string | undefined;
     inputFidelity?: "low" | "high" | undefined;
@@ -1098,18 +1098,18 @@ declare const fileSearch: _ai_sdk_provider_utils.ProviderToolFactoryWithOutputSc
 }>;
 
 declare const imageGenerationArgsSchema: _ai_sdk_provider_utils.LazySchema<{
-    background?: "auto" | "transparent" | "opaque" | undefined;
+    background?: string | undefined;
     inputFidelity?: "low" | "high" | undefined;
     inputImageMask?: {
         fileId?: string | undefined;
         imageUrl?: string | undefined;
     } | undefined;
     model?: string | undefined;
-    moderation?: "auto" | undefined;
+    moderation?: string | undefined;
     outputCompression?: number | undefined;
-    outputFormat?: "png" | "jpeg" | "webp" | undefined;
+    outputFormat?: string | undefined;
     partialImages?: number | undefined;
-    quality?: "low" | "medium" | "high" | "auto" | undefined;
+    quality?: string | undefined;
     size?: "auto" | "1024x1024" | "1024x1536" | "1536x1024" | undefined;
 }>;
 declare const imageGenerationOutputSchema: _ai_sdk_provider_utils.LazySchema<{
diff --git a/dist/internal/index.js b/dist/internal/index.js
index e5b1f96b49d488c7ef553b9dda77e3fbc3945681..70718969407a9b36ea3e49d4a07588c534b984ce 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -1047,7 +1047,7 @@ var OpenAIChatLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2060,7 +2060,8 @@ var openaiImageResponseSchema = (0, import_provider_utils12.lazySchema)(
       created: import_v48.z.number().nullish(),
       data: import_v48.z.array(
         import_v48.z.object({
-          b64_json: import_v48.z.string(),
+          b64_json: import_v48.z.string().nullish(),
+          url: import_v48.z.string().nullish(),
           revised_prompt: import_v48.z.string().nullish()
         })
       ),
@@ -2109,18 +2110,18 @@ var baseImageModelOptionsObject = import_v49.z.object({
    *
    * Valid values: `standard`, `hd`, `low`, `medium`, `high`, `auto`.
    */
-  quality: import_v49.z.enum(["standard", "hd", "low", "medium", "high", "auto"]).optional(),
+  quality: import_v49.z.string().optional(),
   /**
    * Background behavior for the generated image(s).
    *
    * If `transparent`, the output format must support transparency
    * (i.e. `png` or `webp`).
    */
-  background: import_v49.z.enum(["transparent", "opaque", "auto"]).optional(),
+  background: import_v49.z.string().optional(),
   /**
    * Format in which the generated image(s) are returned.
    */
-  outputFormat: import_v49.z.enum(["png", "jpeg", "webp"]).optional(),
+  outputFormat: import_v49.z.string().optional(),
   /**
    * Compression level (0-100) for the generated image(s). Applies to the
    * `jpeg` and `webp` output formats.
@@ -2143,12 +2144,12 @@ var openaiImageModelGenerationOptions = (0, import_provider_utils13.lazySchema)(
        * dramatic images; `natural` produces more subdued, less hyper-real
        * looking images.
        */
-      style: import_v49.z.enum(["vivid", "natural"]).optional(),
+      style: import_v49.z.string().optional(),
       /**
        * Content moderation level for the generated image(s). `low` applies
        * less restrictive filtering.
        */
-      moderation: import_v49.z.enum(["auto", "low"]).optional()
+      moderation: import_v49.z.string().optional()
     })
   )
 );
@@ -2248,7 +2249,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2315,7 +2320,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3387,9 +3396,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3625,6 +3636,12 @@ async function convertToOpenAIResponsesInput({
                       summary: []
                     };
                   }
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   const summaryParts = [];
                   if (part.text.length > 0) {
@@ -3668,6 +3685,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -3950,21 +3973,22 @@ ${text}`,
     }
   }
   if (!store && input.some(
-    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null
+    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null && !("content" in item && item.content != null)
   )) {
     warnings.push({
       type: "other",
       message: "Reasoning parts without encrypted content are not supported when store is false. Skipping reasoning parts."
     });
     input = input.filter(
-      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null
+      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null || "content" in item && item.content != null
     );
   }
   return { input, warnings };
 }
 var openaiResponsesReasoningProviderOptionsSchema = import_v417.z.object({
   itemId: import_v417.z.string().nullish(),
-  reasoningEncryptedContent: import_v417.z.string().nullish()
+  reasoningEncryptedContent: import_v417.z.string().nullish(),
+  rawReasoningContent: import_v417.z.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4602,6 +4626,11 @@ var openaiResponsesChunkSchema = (0, import_provider_utils26.lazySchema)(
         summary_index: import_v418.z.number(),
         delta: import_v418.z.string()
       }),
+      import_v418.z.object({
+        type: import_v418.z.literal("response.reasoning_text.delta"),
+        item_id: import_v418.z.string(),
+        delta: import_v418.z.string()
+      }),
       import_v418.z.object({
         type: import_v418.z.literal("response.reasoning_summary_part.done"),
         item_id: import_v418.z.string(),
@@ -4696,7 +4725,7 @@ var openaiResponsesResponseSchema = (0, import_provider_utils26.lazySchema)(
                       index: import_v418.z.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6228,7 +6257,7 @@ var OpenAIResponsesLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -7679,6 +7708,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index 714e23772d3d042b4bd88e69c0dc37d6e11187c5..25c7637775478da9535c8e29eab253987004f67f 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -1000,7 +1000,7 @@ var OpenAIChatLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2048,7 +2048,8 @@ var openaiImageResponseSchema = lazySchema7(
       created: z8.number().nullish(),
       data: z8.array(
         z8.object({
-          b64_json: z8.string(),
+          b64_json: z8.string().nullish(),
+          url: z8.string().nullish(),
           revised_prompt: z8.string().nullish()
         })
       ),
@@ -2100,18 +2101,18 @@ var baseImageModelOptionsObject = z9.object({
    *
    * Valid values: `standard`, `hd`, `low`, `medium`, `high`, `auto`.
    */
-  quality: z9.enum(["standard", "hd", "low", "medium", "high", "auto"]).optional(),
+  quality: z9.string().optional(),
   /**
    * Background behavior for the generated image(s).
    *
    * If `transparent`, the output format must support transparency
    * (i.e. `png` or `webp`).
    */
-  background: z9.enum(["transparent", "opaque", "auto"]).optional(),
+  background: z9.string().optional(),
   /**
    * Format in which the generated image(s) are returned.
    */
-  outputFormat: z9.enum(["png", "jpeg", "webp"]).optional(),
+  outputFormat: z9.string().optional(),
   /**
    * Compression level (0-100) for the generated image(s). Applies to the
    * `jpeg` and `webp` output formats.
@@ -2134,12 +2135,12 @@ var openaiImageModelGenerationOptions = lazySchema8(
        * dramatic images; `natural` produces more subdued, less hyper-real
        * looking images.
        */
-      style: z9.enum(["vivid", "natural"]).optional(),
+      style: z9.string().optional(),
       /**
        * Content moderation level for the generated image(s). `low` applies
        * less restrictive filtering.
        */
-      moderation: z9.enum(["auto", "low"]).optional()
+      moderation: z9.string().optional()
     })
   )
 );
@@ -2239,7 +2240,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2306,7 +2311,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3432,9 +3441,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3670,6 +3681,12 @@ async function convertToOpenAIResponsesInput({
                       summary: []
                     };
                   }
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   const summaryParts = [];
                   if (part.text.length > 0) {
@@ -3713,6 +3730,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -3995,21 +4018,22 @@ ${text}`,
     }
   }
   if (!store && input.some(
-    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null
+    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null && !("content" in item && item.content != null)
   )) {
     warnings.push({
       type: "other",
       message: "Reasoning parts without encrypted content are not supported when store is false. Skipping reasoning parts."
     });
     input = input.filter(
-      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null
+      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null || "content" in item && item.content != null
     );
   }
   return { input, warnings };
 }
 var openaiResponsesReasoningProviderOptionsSchema = z17.object({
   itemId: z17.string().nullish(),
-  reasoningEncryptedContent: z17.string().nullish()
+  reasoningEncryptedContent: z17.string().nullish(),
+  rawReasoningContent: z17.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4650,6 +4674,11 @@ var openaiResponsesChunkSchema = lazySchema16(
         summary_index: z18.number(),
         delta: z18.string()
       }),
+      z18.object({
+        type: z18.literal("response.reasoning_text.delta"),
+        item_id: z18.string(),
+        delta: z18.string()
+      }),
       z18.object({
         type: z18.literal("response.reasoning_summary_part.done"),
         item_id: z18.string(),
@@ -4744,7 +4773,7 @@ var openaiResponsesResponseSchema = lazySchema16(
                       index: z18.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6309,7 +6338,7 @@ var OpenAIResponsesLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -7760,6 +7789,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/src/chat/openai-chat-language-model.ts b/src/chat/openai-chat-language-model.ts
index e5bda94b0f26b69796d74900f975cb1f1b9ac234..f31c9ff0665366dcbd8e0dda83d15ac076749f9d 100644
--- a/src/chat/openai-chat-language-model.ts
+++ b/src/chat/openai-chat-language-model.ts
@@ -211,7 +211,7 @@ export class OpenAIChatLanguageModel implements LanguageModelV3 {
 
     // remove unsupported settings for reasoning models
     // see https://platform.openai.com/docs/guides/reasoning#limitations
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       // when reasoning effort is none, gpt-5.1 models allow temperature, topP, logprobs
       //  https://platform.openai.com/docs/guides/latest-model#gpt-5-1-parameter-compatibility
       if (
diff --git a/src/image/openai-image-api.ts b/src/image/openai-image-api.ts
index de779ca46aa2102cc5bef4a733ef6b6807260731..14b724785e15ab95f4cd2ff4d35d35c2ca7605c7 100644
--- a/src/image/openai-image-api.ts
+++ b/src/image/openai-image-api.ts
@@ -9,7 +9,8 @@ export const openaiImageResponseSchema = lazySchema(() =>
       created: z.number().nullish(),
       data: z.array(
         z.object({
-          b64_json: z.string(),
+          b64_json: z.string().nullish(),
+          url: z.string().nullish(),
           revised_prompt: z.string().nullish(),
         }),
       ),
diff --git a/src/image/openai-image-model-options.ts b/src/image/openai-image-model-options.ts
index 935f6f0907f21b515a9d2c645ceb61f0079d71a1..0078fc2ea69af1734a8e5ba25b9b014495804892 100644
--- a/src/image/openai-image-model-options.ts
+++ b/src/image/openai-image-model-options.ts
@@ -45,10 +45,9 @@ const baseImageModelOptionsObject = z.object({
   /**
    * Quality of the generated image(s).
    *
-   * Valid values: `standard`, `hd`, `low`, `medium`, `high`, `auto`.
+   * Valid values: `standard`, `hd`, `low`, `medium`, `high`, `auto`, `xhigh`, `max`.
    */
-  quality: z
-    .enum(['standard', 'hd', 'low', 'medium', 'high', 'auto'])
+  quality: z.string()
     .optional(),
 
   /**
@@ -57,12 +56,12 @@ const baseImageModelOptionsObject = z.object({
    * If `transparent`, the output format must support transparency
    * (i.e. `png` or `webp`).
    */
-  background: z.enum(['transparent', 'opaque', 'auto']).optional(),
+  background: z.string().optional(),
 
   /**
    * Format in which the generated image(s) are returned.
    */
-  outputFormat: z.enum(['png', 'jpeg', 'webp']).optional(),
+  outputFormat: z.string().optional(),
 
   /**
    * Compression level (0-100) for the generated image(s). Applies to the
@@ -93,13 +92,13 @@ export const openaiImageModelGenerationOptions = lazySchema(() =>
        * dramatic images; `natural` produces more subdued, less hyper-real
        * looking images.
        */
-      style: z.enum(['vivid', 'natural']).optional(),
+      style: z.string().optional(),
 
       /**
        * Content moderation level for the generated image(s). `low` applies
        * less restrictive filtering.
        */
-      moderation: z.enum(['auto', 'low']).optional(),
+      moderation: z.string().optional(),
     }),
   ),
 );
diff --git a/src/image/openai-image-model.ts b/src/image/openai-image-model.ts
index 41b81b27ccc1cd2e821573dab1e28517def1ea95..f0858ff8d6f53ef0ba1bdf898d881d01dae517c5 100644
--- a/src/image/openai-image-model.ts
+++ b/src/image/openai-image-model.ts
@@ -132,7 +132,11 @@ export class OpenAIImageModel implements ImageModelV3 {
       });
 
       return {
-        images: response.data.map(item => item.b64_json),
+        images: response.data.flatMap(item => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
         warnings,
         usage:
           response.usage != null
@@ -207,7 +211,11 @@ export class OpenAIImageModel implements ImageModelV3 {
     });
 
     return {
-      images: response.data.map(item => item.b64_json),
+      images: response.data.flatMap(item => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
       warnings,
       usage:
         response.usage != null
diff --git a/src/responses/convert-to-openai-responses-input.ts b/src/responses/convert-to-openai-responses-input.ts
index 5c27d8f988a37082af53de78c0b542a5b8373eed..c31edcb071d021d954fcb3d6b912a868458ae737 100644
--- a/src/responses/convert-to-openai-responses-input.ts
+++ b/src/responses/convert-to-openai-responses-input.ts
@@ -514,9 +514,14 @@ export async function convertToOpenAIResponsesInput({
               }
 
               input.push({
+                // Ark rejects assistant input items that omit either field (#18253);
+                // both are the canonical Responses shape OpenAI accepts. Only the
+                // assistant item needs it — Ark takes user/system items as-is.
+                type: 'message',
                 role: 'assistant',
                 content: [{ type: 'output_text', text: part.text }],
                 id,
+                status: 'completed',
                 ...(phase != null && { phase }),
               });
 
@@ -893,6 +898,15 @@ export async function convertToOpenAIResponsesInput({
                       summary: [],
                     };
                   }
+                } else if (
+                  providerOptions?.rawReasoningContent &&
+                  part.text.length > 0
+                ) {
+                  input.push({
+                    type: 'reasoning',
+                    summary: [],
+                    content: [{ type: 'reasoning_text', text: part.text }],
+                  });
                 } else {
                   const summaryParts: Array<{
                     type: 'summary_text';
@@ -955,6 +969,17 @@ export async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts,
                   });
+                } else if (
+                  providerOptions?.rawReasoningContent &&
+                  part.text.length > 0
+                ) {
+                  // Thinking dialects (DeepSeek, …) require the reasoning text itself on
+                  // replay; opt-in because OpenAI rejects `content` on reasoning items.
+                  input.push({
+                    type: 'reasoning',
+                    summary: [],
+                    content: [{ type: 'reasoning_text', text: part.text }],
+                  });
                 } else {
                   warnings.push({
                     type: 'other',
@@ -1313,7 +1338,8 @@ export async function convertToOpenAIResponsesInput({
       item =>
         'type' in item &&
         item.type === 'reasoning' &&
-        item.encrypted_content == null,
+        item.encrypted_content == null &&
+        !('content' in item && item.content != null),
     )
   ) {
     warnings.push({
@@ -1325,7 +1351,8 @@ export async function convertToOpenAIResponsesInput({
       item =>
         !('type' in item) ||
         item.type !== 'reasoning' ||
-        item.encrypted_content != null,
+        item.encrypted_content != null ||
+        ('content' in item && item.content != null),
     );
   }
 
@@ -1335,6 +1362,7 @@ export async function convertToOpenAIResponsesInput({
 const openaiResponsesReasoningProviderOptionsSchema = z.object({
   itemId: z.string().nullish(),
   reasoningEncryptedContent: z.string().nullish(),
+  rawReasoningContent: z.boolean().nullish(),
 });
 
 export type OpenAIResponsesReasoningProviderOptions = z.infer<
diff --git a/src/responses/openai-responses-api.ts b/src/responses/openai-responses-api.ts
index aef872bfbed2218f104c6cf864cde8005173ff6d..b34fcfd8c530c7cfc33e04ffe2ec5efbd3bc435c 100644
--- a/src/responses/openai-responses-api.ts
+++ b/src/responses/openai-responses-api.ts
@@ -1206,6 +1206,11 @@ export const openaiResponsesChunkSchema = lazySchema(() =>
         summary_index: z.number(),
         delta: z.string(),
       }),
+      z.object({
+        type: z.literal('response.reasoning_text.delta'),
+        item_id: z.string(),
+        delta: z.string(),
+      }),
       z.object({
         type: z.literal('response.reasoning_summary_part.done'),
         item_id: z.string(),
@@ -1327,7 +1332,7 @@ export const openaiResponsesResponseSchema = lazySchema(() =>
                         index: z.number(),
                       }),
                     ]),
-                  ),
+                  ).nullish().transform((v) => v ?? []),
                 }),
               ),
             }),
diff --git a/src/responses/openai-responses-language-model.ts b/src/responses/openai-responses-language-model.ts
index 485880b51bbb55f9bf2b563afe24330f5a309c37..e25dd4f5cc5f793bbcc71d1c70983bb0c122221f 100644
--- a/src/responses/openai-responses-language-model.ts
+++ b/src/responses/openai-responses-language-model.ts
@@ -436,7 +436,7 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
 
     // remove unsupported settings for reasoning models
     // see https://platform.openai.com/docs/guides/reasoning#limitations
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       // when reasoning effort is none, gpt-5.1 models allow temperature, topP, logprobs
       //  https://platform.openai.com/docs/guides/latest-model#gpt-5-1-parameter-compatibility
       if (
@@ -2243,6 +2243,17 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
                   } satisfies ResponsesReasoningProviderMetadata,
                 },
               });
+            } else if (value.type === 'response.reasoning_text.delta') {
+              controller.enqueue({
+                type: 'reasoning-delta',
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id,
+                  } satisfies ResponsesReasoningProviderMetadata,
+                },
+              });
             } else if (value.type === 'response.reasoning_summary_part.done') {
               // when OpenAI stores the message data, we can immediately conclude the reasoning part
               // since we do not need to send the encrypted content.
