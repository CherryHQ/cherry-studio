claude-flow swarm "Review how pages are loaded when clicked from the sidebar. build a plan to create a tab system, like a web browser, where each click opens a new
tab, and can open multipal instances of the same page/app (unless there is so reason not to). we want it to be like a modern advanced browser tab system, but it launches apps or in some cases it does obviously launch external pages as you'll see. . Ultrathink as this requires extra effort to make sure you dont fuckup. word, big bird? :)" --max-agents 5 --parallel --monitor

claude-flow swarm "lets start working through all your type and linting error that you left behind from this implementation" --max-agents 5 --parallel --monitor
