---
---

No package release is required: the file preview now serves pdf.js CMaps and standard fonts from the already-packaged `pdfjs-dist` tree, fixing blank CJK text in PDFs with non-embedded CID-keyed fonts.
