---
'@cherrystudio/ai-core': minor
---

Add video-generation runtime support. Exposes a `generateVideo` runtime helper (plus `generateVideoParams` / `generateVideoResult` types and `RuntimeExecutor.generateVideo`) and provider video-model wiring, so providers can serve video generation through the standard runtime.
