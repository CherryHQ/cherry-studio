# PR12258 合并冲突处理记录（feat/proxy-api-server -> feat/v2-api-gateway-migration-pr12258-2）

## 处理原则

- 以“新旧都尽量保留”为主。
- 仅在必须二选一时才做取舍。
- i18n 冲突按 HEAD 保留。

## 已处理冲突概览

### i18n（按 HEAD）

- `src/renderer/src/i18n/locales/de-de.ts`
- `src/renderer/src/i18n/locales/el-gr.ts`
- `src/renderer/src/i18n/locales/es-es.ts`
- `src/renderer/src/i18n/locales/fr-fr.ts`
- `src/renderer/src/i18n/locales/ja-jp.ts`
- `src/renderer/src/i18n/locales/pt-pt.ts`
- `src/renderer/src/i18n/locales/ro-ro.ts`
- `src/renderer/src/i18n/locales/ru-ru.ts`

### Main / Preload / Shared

- `src/main/services/ApiGatewayService.ts`
  - 仅保留 `ApiGateway_*` IPC handlers，避免与 `ApiServerService` 职责重叠。
- `src/main/ipc.ts`
  - 合并并清理冲突标记，保留 gateway 迁移后的调用链。
- `src/preload/index.ts`
  - 保留 `cache` / `preference` / `dataApi` 与 `apiGateway` 通道，清理旧冲突残留。
- `packages/shared/utils/index.ts`
  - 合并 `AA` 冲突：保留 JSONC、URL、env sanitize 等能力，并保留新增 re-export。

### Agents / ClaudeCode

- `src/main/services/agents/BaseService.ts`
  - 对齐 `@main/apiGateway/services/mcp` 与相关 gateway 工具引用。
- `src/main/services/agents/services/claudecode/claude-stream-state.ts`
  - 合并 `expectingSkillContent` 与 `_streamFinished` 状态逻辑，`resetStep()` 同步重置。
- `src/main/services/agents/services/claudecode/index.ts`
  - 统一读取 gateway 配置，保留语言指令与调用流程。
- `src/main/services/agents/services/claudecode/transform.ts`
  - 保留 result success/failure 分支，并吸收 finished-state 去重处理。

### Renderer（Store / 页面 / Hooks）

- `src/renderer/src/store/settings.ts`
  - 统一为 `apiGateway` 设置结构与 actions，并保留已有设置项。
- `src/renderer/src/store/migrate.ts`
  - `193` 同时保留 `addPreprocessProviders(state, 'paddleocr')` 与 `apiServer -> apiGateway` 迁移。
  - 保留 194~200 迁移链。
- `src/renderer/src/store/index.ts`
  - persist `version` 采用 `200`（先核对迁移链后确定）。
- `src/renderer/src/store/thunk/messageThunk.ts`
  - 统一到 `ApiGatewayConfig` 类型，保留 agent thinking/effort 相关类型。
- 其他批量冲突清理并保留双方有效变更：
  - `src/renderer/src/hooks/agents/useAgents.ts`
  - `src/renderer/src/pages/home/Tabs/components/UnifiedAddButton.tsx`
  - `src/renderer/src/pages/home/Chat.tsx`
  - `src/renderer/src/pages/home/Tabs/AssistantsTab.tsx`
  - `src/renderer/src/aiCore/provider/providerConfig.ts`
  - `src/renderer/src/pages/code/CodeToolsPage.tsx`
  - `src/renderer/src/pages/settings/SettingsPage.tsx`
  - `src/renderer/src/utils/naming.ts`

### 最后两处冲突收尾

- `tests/main.setup.ts`
  - 合并 electron mock，补齐 `net.fetch` mock。
  - 保留 `@main/services/AnthropicService` mock。
  - 去掉重复的旧 `node:os` mock，保留更完整的 async `importActual` 版本。
- `tsconfig.node.json`
  - `include` 同时保留：
    - `tests/__mocks__/**/*`
    - `packages/ai-sdk-provider/**/*`
  - `paths` 同时保留：
    - `@test-mocks/*`
    - `@cherrystudio/ai-core*` / `@cherrystudio/ai-sdk-provider`

## 当前状态

- 所有已识别冲突文件已完成冲突标记清理并加入暂存。
- 合并分支：`feat/proxy-api-server` 到 `feat/v2-api-gateway-migration-pr12258-2`。

