/** Webview-domain IpcApi error codes. Import directly from this module on both sides. */
export const webviewErrorCodes = {
  /** The caller's host window does not own the requested site webview. */
  NOT_OWNED: 'WEBVIEW_NOT_OWNED'
} as const
